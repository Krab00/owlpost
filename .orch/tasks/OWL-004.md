---
id: OWL-004
title: Contact providers `repo` and `local`, policy overlay, `owl contact`
status: done
deps: [OWL-002]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `contacts/` per `docs/technical-design.md §5`. `repo.rs` finds the git root from the
current directory (walk up to a `.git`) and loads every `.agents/peers/*.json`; `local.rs` loads
`$OWLPOST_HOME/contacts/*.json`; `ContactBook::load(home, cwd)` merges them (repo data wins for
name/emails/endpoints, local file with the same pubkey contributes only `policy`). Implement
`resolve(query)` (fingerprint → email → unique name prefix; ambiguity error listing candidates),
`policy_for(fingerprint) -> Option<Policy>`, `set_policy(fingerprint, Policy)` writing the overlay.
CLI: `owl contact list` (table with name, fingerprint, source, policy mode), `owl contact show
<peer>`, `owl contact export` (this machine's repo entry built from config + key, JSON to stdout).
Malformed contact files are skipped with a warning on stderr, never a panic.

## Acceptance criteria
- [ ] AC1: integration test `tests/contacts.rs::repo_provider_finds_peers_from_subdir` creates a fixture git repo with two peer files, runs `ContactBook::load` from a nested subdirectory, and asserts both contacts with `source == "repo"` (modality: test)
- [ ] AC2: integration test `tests/contacts.rs::local_overlay_only_contributes_policy` writes a local file with the same pubkey as a repo contact but a different name and `policy.mode = "auto"`; asserts the merged contact keeps the repo name and has mode auto (modality: test)
- [ ] AC3: integration test `tests/contacts.rs::resolve_orders_and_ambiguity` asserts resolution by fingerprint, by email, by unique name prefix, and that a prefix matching two names errors with both names in the message (modality: test)
- [ ] AC4: integration test `tests/contacts.rs::malformed_file_is_skipped` places an invalid JSON file in `.agents/peers/` and asserts loading succeeds with the valid peers (modality: test)
- [ ] AC5: `owl --home <tmp> contact export` after `owl init` prints JSON with exactly the keys `name`, `emails`, `pubkey`, `endpoints` whose pubkey equals `owl whoami --json` pubkey (modality: test)
- [ ] AC6: `owl --home <tmp> contact list` run inside the fixture repo prints one row per merged contact including the policy column (modality: test)

## Verification — task/OWL-004-contacts (bfe100b, base task/OWL-002-identity 86c30a8)
Env: verify worktree slot 2, offline. Evidence: `.orch/tasks/OWL-004.evidence/{gates.txt,probes.txt,probe_tests.rs,mutants.txt}`.

| Criterion | Tool | Evidence | Verdict |
|---|---|---|---|
| AC1 `tests/contacts.rs::repo_provider_finds_peers_from_subdir` | cargo test | green; asserts 2 contacts, both `source=="repo"`, exact name/emails/endpoints/fingerprint from `repo/src/deep` | PASS |
| AC2 `local_overlay_only_contributes_policy` | cargo test | green; overlay "Wrong Name"+auto → merged name Maciek, mode Auto, emails/endpoints/added_at from repo; mutants M4a-d (copy endpoints/emails/name/added_at) all killed | PASS |
| AC3 `resolve_orders_and_ambiguity` | cargo test | green; fingerprint, email (beats exact-name), unique ci prefix, ambiguity "ma" → message lists Maciek, Marek; M3 (one name) killed | PASS |
| AC4 `malformed_file_is_skipped` | cargo test | green; `{not json`, bad pubkey, missing pubkey, README.md skipped; local `[]` skipped too; M8/M9 killed | PASS |
| AC5 `owl contact export` after init | cargo test `cli::contact_export_matches_whoami` + probe ROW52 | keys exactly emails/endpoints/name/pubkey; pubkey == `whoami --json`; probe with name "T" prints `"name":"T"`; no-init → exit 1 "owl init" | PASS |
| AC6 `owl contact list` inside fixture repo | cargo test `cli::contact_list_in_fixture_repo` + probe ROW45/48 | header `NAME FINGERPRINT SOURCE POLICY`; one row per merged contact; policy column `-`/`auto`/`manual`/`never`; `--json` array | PASS |
| Gate: cargo build --locked | cargo | Finished, Cargo.lock unchanged (no new deps) | PASS |
| Gate: cargo test --workspace | cargo | 17 unit + 21 cli + 8 contacts, 0 failed | PASS |
| Gate: clippy -D warnings / fmt --check | cargo | clean | PASS |
| Hygiene: tracked fixtures, no vendored paths | git ls-files / diff --name-only | fixtures self-created in tempdirs; diff = src/{lib,main,contacts/*}.rs + tests/{cli,contacts}.rs | PASS |
| Test gate: test-reviewer | subagent | **GAPS**: (1) set_policy branch C (local contact under slug filename) unreached; (2) `.git` as file untested; (3) empty-book `list` untested; (4) fp-vs-email tier swap undetectable; (5) require_identity `\|\|`→`&&` undetectable via export | FAIL |
| Matrix 1,15 repo load from subdir | test | see AC1 | PASS |
| Matrix 2 git root without `.agents/` | probe ROW2 | header only, exit 0, stderr empty | PASS |
| Matrix 3 extension filter (`.JSON`, `.bak`, `.hidden.json`) | probe ROW3 | `.JSON`/`.bak` ignored silently, `.hidden.json` loaded; mutant M18 (filter removed) SURVIVED shipped suite | FAIL (coverage) |
| Matrix 4 filename sort order | probe p_sort_order_by_filename | `a.json`(Zed) before `b.json`(Alice); mutant M19 (sort removed) SURVIVED | FAIL (coverage) |
| Matrix 5 malformed repo file warning | test cli + contacts | `owl: warning: skipping …/bad.json: …`, exit 0 | PASS |
| Matrix 6 malformed + `--json` | probe ROW6 | stdout valid JSON array (4), 4 warnings on stderr only | PASS |
| Matrix 7 directory named `dir.json` | probe ROW7 | warning "Is a directory", no panic, exit 0 | PASS |
| Matrix 8 unreadable peers dir (chmod 000) | probe ROW8 | silently empty, exit 0, no warning | PM-REVIEW (silent; design unspecified) |
| Matrix 9,12 missing/short pubkey | test | skipped with warning | PASS |
| Matrix 10,11 wrong prefix / empty / bad base64 pubkey | probe ROW10/11 | each skipped with specific warning | PASS |
| Matrix 13 non-curve-point 32 bytes | — | not induced (needs random search); parse path identical to 10-12 | PM-REVIEW (not induced) |
| Matrix 14 `fingerprint`/`source` in file ignored | probe p_loader_edges + test Liar | fingerprint derived, source from provider; M20 killed | PASS |
| Matrix 16 cwd == git root | probe p_cwd_is_root | 2 contacts | PASS |
| Matrix 17 `.git` as file (worktree) | probe p_git_as_file_worktree | 2 contacts found; mutant M7 (`is_dir`) SURVIVED shipped suite | FAIL (coverage) |
| Matrix 18 nearest root wins | probe p_nearest_root_wins | inner repo w/o peers → 0 contacts; M6 (cwd only) killed | PASS |
| Matrix 19 no git root | test ×2 | 0 repo contacts, local still listed | PASS |
| Matrix 20,24 overlay / local-only full contact | test | see AC2; Ola full contact source local | PASS |
| Matrix 21 overlay w/o `policy` over repo file carrying `policy` | probe ROW21 | repo-file policy honoured (Auto) when no overlay; overlay `{pubkey}` only → policy None (wiped). §5 does not say repo files may carry policy | PM-REVIEW |
| Matrix 22 duplicate pubkey across files | probe ROW22 | both kept; `resolve("Maciek")` → "ambiguous … Maciek, Maciek" | PM-REVIEW (no dedupe; unspecified) |
| Matrix 23 orphan overlay outside repo | probe ROW23 | row with empty NAME, `local auto` | PM-REVIEW (phantom contact; unspecified) |
| Matrix 25-29 resolve tiers + ambiguity + no-hit | test + probe | as specified; M3/M17/M23 killed | PASS |
| Matrix 30 exact name vs longer name | probe ROW30 | `resolve("Maciek")` with "Maciek Nowak" present → ambiguous (implementer flagged for human review) | PM-REVIEW |
| Matrix 31 empty query, 1-contact book | probe ROW31 | `resolve("")` returns the single contact (prefix of everything) | PM-REVIEW |
| Matrix 32 fingerprint/email exactness | probe | uppercase fp and `Marek@company.com` → Err; mutant M27 (ci email) SURVIVED | FAIL (coverage) |
| Matrix 33 policy_for known/none/unknown | test + probe | Some(auto)/None/None; M21, M32 killed | PASS |
| Matrix 34,35 set_policy repo-contact file / update existing | test | keys exactly added_at,policy,pubkey,source; added_at 20 chars `…Z` preserved on update; M11, M25, M26 killed | PASS |
| Matrix 36,37 set_policy branch C (local under slug name) | probe p_set_policy_branch_c | new `<fp>.json` keeps name/emails/endpoints/added_at, no `fingerprint` key; BUT `ola.json` + `<fp>.json` → contact listed twice after reload; mutants M10, M24 SURVIVED shipped suite | FAIL (coverage) + PM-REVIEW (duplicate) |
| Matrix 38 set_policy on corrupt overlay | probe ROW38 | Err "parsing …", file untouched | PASS |
| Matrix 39 set_policy on non-object overlay (`[]`) | probe ROW39 | **panic** `cannot access key "policy" in JSON array` at `src/contacts/mod.rs:225` (`v["policy"] = …` runs before the `as_object_mut` guard on 226) | FAIL |
| Matrix 40 set_policy unknown fingerprint | test | Err, no file | PASS |
| Matrix 41 unwritable home | probe ROW41 | Err permission denied, in-memory policy unchanged | PASS |
| Matrix 42 rfc3339 boundaries | unit + probe_rfc | 0, 2000-02-29, 2024-12-31, 2026-09-01 (shipped) + 2025-01-01, 1972-02-29, 2100-02-28/03-01, -1 (probe) exact; M22 killed | PASS |
| Matrix 43 now_secs pre-epoch | review | unreachable on real clocks | PM-REVIEW (not inducible) |
| Matrix 44 Mode/Scope serde | unit | `"Auto"` rejected; scope default `["*"]`; M28 killed | PASS |
| Matrix 45,46 list text / --json | test | exact rows; M14, M30 killed | PASS |
| Matrix 47 empty book | probe ROW47 | header only / `[]`; mutant M13 (no header) SURVIVED | FAIL (coverage) |
| Matrix 48 policy column manual/never | probe + test | shown | PASS |
| Matrix 49,50 show by fp / prefix / overlaid | probe ROW50 | JSON incl. policy, source repo, no added_at | PASS |
| Matrix 51,60 show error exit | test + probe | exit 1, `owl: ambiguous peer "ma": matches Maciek, Marek` / `owl: no contact matches "nobody"`, stdout empty; M15 killed | PASS |
| Matrix 52,53 export / both missing | test | see AC5 | PASS |
| Matrix 54,55 key-only / config-only missing | probe | exit 1, `no identity found in <home> — run \`owl init\` first`; mutant M16 (`&&`) SURVIVED shipped suite | FAIL (coverage) |
| Matrix 56,57 truncated key / bad config | probe | exit 1 `expected 32 bytes, got 16` / `parsing …config.json: EOF…`, stdout empty | PASS |
| Matrix 58 clap subcommand | probe ROW58 | `contact`, `contact bogus`, `contact list extra` → exit 2; `--help` lists list/show/export | PASS |
| Matrix 59 cwd deleted | — | not inducible via Command | PM-REVIEW |
| Mutant M1 email tier before fingerprint | mutants.txt | SURVIVED (behaviour correct per probe: email == other's fp resolves by fp) | FAIL (coverage) |
| Mutant M2 prefix tier matches emails | mutants.txt | SURVIVED (behaviour correct per probe `qq` → Err) | FAIL (coverage) |
| Mutant M31 export name hardcoded "Test" | mutants.txt | SURVIVED — cli fixture always inits with name "Test" (provenance lesson) | FAIL (coverage) |
| Killed mutants | mutants.txt | M3-M6, M8, M9, M11, M12, M14, M15, M17, M20-M23, M25, M26, M28-M30, M32 (21) | PASS |
| Design §5 conformance | code review | overlay format, Mode manual/auto/never, resolve order, repo-wins rule, `<fingerprint>.json` overlay path all match. `set_policy` does not enforce the local-auto `--i-verified-the-fingerprint` rule (OWL-007 CLI concern; noted, fine) | PASS |

## Implementer report — task/OWL-004-contacts
**Summary.** `src/lib.rs` (library crate: config, contacts, identity); `src/contacts/{mod,repo,local}.rs` — `Contact`, `Policy`, `Mode`, `Scope`, `ContactBook::{load,resolve,policy_for,set_policy}`, `load_dir` with warning-skip, hand-rolled RFC3339 for `added_at`; `src/main.rs` `Contact` arm → nested `List | Show | Export`, `require_identity` reuse; `tests/contacts.rs` 8 tests; `tests/cli.rs` 6 new tests.
**Assumptions.** `fingerprint` is a computed field (`skip_deserializing`); `source` always set by the provider; empty-name default for overlay files; list format `{:<20} {:<20} {:<6} POLICY`; pure prefix resolve (a name that is a prefix of another is ambiguous).
**Open questions.** none.
**Blockers.** none.
**For human review.** list text format; prefix-ambiguity semantics.

## Verification — task/OWL-004-contacts (round 2)
Branch at ef488e7 (one commit on bfe100b). Rechecked only the round-1 failing rows. Evidence: `.orch/tasks/OWL-004.evidence/{gates-r2.txt,probe-r2.txt,mutants-r2.txt}`.

| Criterion | Tool | Evidence | Verdict |
|---|---|---|---|
| set_policy on non-object overlay (`[]`, `"x"`) | probe p2_non_object_overlay | `Err("<path> is not a JSON object")`, no panic, file bytes unchanged, in-memory policy still None; new guard mutant M33 (unwrap) killed by `set_policy_rejects_non_object_overlay_file` | PASS |
| Mutant M1 email tier before fingerprint | mutants-r2.txt | KILLED by `fingerprint_tier_beats_email_tier` | PASS |
| Mutant M2 prefix tier matches emails | mutants-r2.txt | KILLED by `prefix_tier_matches_names_only`, `email_match_is_exact_case_sensitive` | PASS |
| Mutant M7 `.git` must be dir | mutants-r2.txt | KILLED by `git_file_marks_worktree_root` | PASS |
| Mutants M10, M24 set_policy branch C / fingerprint key | mutants-r2.txt | KILLED by `set_policy_on_slug_named_local_contact_writes_fp_file` | PASS |
| Mutant M13 empty list no header | mutants-r2.txt | KILLED by `contact_list_empty_book_prints_header_only` | PASS |
| Mutant M16 require_identity (`&&`, ignore key, ignore config) | mutants-r2.txt | all three KILLED by `contact_export_with_config_only_or_key_only_asks_for_init` | PASS |
| Mutant M18 extension filter removed | mutants-r2.txt | KILLED by `contact_list_ignores_non_json_extensions_without_warning` | PASS |
| Mutant M19 sort removed | mutants-r2.txt | SURVIVED — equivalent mutant on APFS (`read_dir` already returns sorted names); `contacts_are_ordered_by_filename` and `contact_list_rows_follow_filename_order` pin the exact order and would kill it on hash-ordered filesystems | PASS (environmental; noted) |
| Mutant M27 email tier case-insensitive | mutants-r2.txt | KILLED by `email_match_is_exact_case_sensitive` | PASS |
| Mutant M31 export name hardcoded "Test" | mutants-r2.txt | KILLED by `contact_export_matches_whoami` (fixture now uses a nonce name) | PASS |
| Test-reviewer (diff bfe100b...ef488e7) | subagent | OK — all five round-1 gaps closed with exact assertions; guard genuinely exercised | PASS |
| Gates on ef488e7: build --locked / test / clippy -D warnings / fmt | cargo | Finished; 17 unit + 25 cli + 16 contacts green; clippy clean; fmt ok; worktree clean | PASS |

PR: https://github.com/Krab00/owlpost/pull/6 (draft, base task/OWL-002-identity)
