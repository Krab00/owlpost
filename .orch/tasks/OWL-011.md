---
id: OWL-011
title: Consent and policy — allow/deny, unavailable, auto-accept with outgoing log
status: done
deps: [OWL-006, OWL-009, OWL-010]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `cli/allow.rs`, `cli/deny.rs` and the auto-accept scheduler in `daemon.rs` per
`docs/concept.md` (Trust) and `docs/architecture.md §3.2, §3.4`. `owl allow <peer> --once`
releases every held `consent` record from that peer to `pending` without writing a policy;
`owl allow <peer>` (default) writes policy `manual` and releases; `owl allow <peer> --always`
writes `auto` — for a `local`-source contact this requires `--i-verified-the-fingerprint`,
otherwise exit 1 with an explanation. `owl deny <peer>` writes `never` and moves held records
to `done/` (state `denied`). `owl inbox` renders consent records as a prompt line
(`<name> wants to ask your agent about <project> — owl allow <fp> [--once|--always] / owl deny <fp>`).
Daemon auto-accept (`auto.rs`, hooked into `daemon.rs`): per `architecture.md §3.4` ("Auto:
send immediately") the `Question{auto}` event triggers an attempt on arrival, and a periodic
spool scan (every `pull_interval_secs`) retries `pending` questions from `auto` peers that carry
no `auto_error` yet (records released by `owl allow --always`, or stranded by a restart). For
each attempt, re-check the policy, run the runner, redact, sign, send (same code path as `owl send`), append one JSON line to
`$OWLPOST_HOME/log/outgoing.jsonl` (`ts, to, question_id, harness, redactions, answer_sha256,
mode: "auto"`), and notify; failures (timeout, unknown project, extract_failed) leave the record
`pending` with an `auto_error` field so the human sees it in `owl inbox`. `owl send` in manual
mode also appends to the outgoing log with `mode: "manual"`.

## Acceptance criteria
- [ ] AC1: integration test `tests/policy.rs::allow_once_releases_without_policy` holds a consent record, runs `owl allow <fp> --once`, asserts state `pending` and `policy_for(fp) == None` (modality: test)
- [ ] AC2: integration test `tests/policy.rs::allow_writes_manual_and_always_writes_auto` asserts the overlay file contents after each command, and that `--always` on a local-source contact without the verification flag exits 1 and writes nothing (modality: test)
- [ ] AC3: integration test `tests/policy.rs::deny_makes_peer_unavailable` runs `owl deny`, asserts held records are `denied`, and a new `POST /v1/questions` from that peer returns 403 `unavailable` byte-identical to the responder-disabled response body (modality: api)
- [ ] AC4: integration test `tests/policy.rs::auto_accept_answers_without_human` with peer A set to `auto` on B, fake harness, B's daemon running: A's question is answered into B's outbox within 5 s, the envelope verifies, `outgoing.jsonl` has one line with `mode == "auto"` and `redactions == 1`, and no `consent`/`pending` record remains (modality: test)
- [ ] AC5: integration test `tests/policy.rs::auto_accept_failure_is_visible` with an unmapped project asserts the record stays `pending` with `auto_error` containing `unknown project` and `owl inbox` prints it (modality: test)
- [ ] AC6: integration test `tests/policy.rs::manual_send_logs_outgoing` asserts `owl send` appends a line with `mode == "manual"` (modality: test)
- [ ] AC7: `owl inbox` output for a consent record contains both `owl allow` and `owl deny` with the peer fingerprint (modality: test)

## Verification — task/OWL-011-policy
Round 1, 2026-09-03, branch `task/OWL-011-policy` @ ed1bb7f (diff vs `task/OWL-008-pull` @ 507f9d2), worktree `.worktrees/OWL-011`. Evidence: `.orch/tasks/OWL-011.evidence/`.
Gate: `cargo build` 0, `cargo test --workspace` 0 (all bins green), `cargo clippy --all-targets -- -D warnings` 0, `cargo fmt --check` 0 (`gate.txt`). `cargo test --test policy` 3×: 8/8 each run, ~0.07 s (`policy-3x.txt`) — no flakiness.
Criteria byte-identical to the branch copy; the branch's Description edit (event trigger + periodic scan) ported into this file.

| AC | Verdict | Evidence |
|---|---|---|
| AC1 `allow --once` releases, no policy | PASS | `tests/policy.rs::allow_once_releases_without_policy` (negative twins: other peer, other state; overlay bytes unchanged; `--json`; unknown peer). Mutants M4 (`--once` writes manual), M20 (release ignores peer) killed. |
| AC2 manual / auto / local guard | PASS | `allow_writes_manual_and_always_writes_auto` asserts overlay contents; inline: refused `--always` on a local contact → rc 1, overlay bytes unchanged, record still `consent` (`ac2-refused-always.txt`, `ac2-overlay-{before,after}.json`). Mutants M3a/M3b killed. |
| AC3 deny → denied, 403 byte-identical | FAIL (test gap) | Behaviour verified: `deny_makes_peer_unavailable` + inline `cmp ac3-never.body ac3-off.body` identical (`{"error":"unavailable"}`, 403, same content-type/length; `ac3-*.headers`). Mutants M5, M6 killed. Gap: `deny_held` is a new caller of `finish()` with no failure-path test (blocked `done/<id>.json` directory, `chmod 0o555 inbox/`) — OWL-010 rule; `cli/deny.rs` ships no unit tests, no `deny nobody` / `--json` / zero-held cases. |
| AC4 auto-accept answers within 5 s, envelope verifies, log line, no consent/pending left | FAIL (test gaps) | Behaviour verified: `auto_accept_answers_without_human` + inline `ac4-report.txt` (answered in 59 ms, envelope verifies with B's key and is rejected with A's, `in_reply_to`/`to`/`from` right, 1 `[redacted]`, 0 secrets, inbox empty, done `answered`); `ac4-outgoing.jsonl` via jq: `mode=="auto"`, `redactions==1`, `answer_sha256` == sha256sum of `ac4-answer-text.txt`. Scan/event interplay inline (`scan-dedupe.txt`, `pull_interval_secs=1`, `FAKE_SLEEP=3`, `timeout_secs=1`): exactly 1 harness run — event+scan dedupe works and the errored record is not retried. Mutants M1, M2, M7a/b, M8, M9, M17 (event hook), M18 (scan), M19 killed. Gaps (reviewer): (a) `auto::attempt` with blocked `done/` untested — `send` has already written outbox+cache, `fail()` resets to `pending`, leaving a signed envelope served on `/v1/outbox` and later reused by a manual `owl send`; (b) `pull_interval_secs` never tested at a non-default value / no test releases a record with `owl allow --always` while the daemon runs; (c) Scheduler in-flight claim only tested as a HashSet, never under a real event+scan overlap; (d) `Kind::AutoAnswered` never fired through the daemon (text-only unit test). |
| AC5 failure visible: pending + `auto_error` + inbox | PASS | `auto_accept_failure_is_visible`; inline `ac5-record.json` (`state=pending`, `draft=null`, `auto_error="unknown project github.com/company/monorepo"`), `ac5-inbox-plain.txt` prints the note, `ac5-inbox-json.txt` has `auto_error`. Mutants M10, M11, M12, M14, M15 killed. Advisory: daemon-level "not retried" is proven only by the verifier's inline check (harness counter), the shipped test relies on `candidates()`. |
| AC6 `owl send` logs `mode == "manual"` | FAIL (test gap + design question) | Behaviour verified: `manual_send_logs_outgoing` (sha of text as sent, second line appended, failed send logs nothing). Gap: the "log after finish so a retry logs exactly once" claim ships no failure-path test for the log append through `send` (unit test covers `append_outgoing` with `log` as a file only). Inline reproduction (`log-append.txt`): `mkdir log/outgoing.jsonl` then `owl send` → rc 1 `appending to …: Is a directory`, but the question is already `done/answered` with the envelope in outbox; retry → `no inbox record`. The answer ships unlogged and can never be logged. O_APPEND checked: 4 threads × 250 concurrent appends → 1000 parseable lines. |
| AC7 inbox prompt has `owl allow <fp>` and `owl deny <fp>` | PASS | `inbox_prompts_for_consent_records` + unit `consent_prompt_names_peer_project_and_both_commands` (exact equality); inline `ac7-inbox-plain.txt`. Mutant M13 killed. |

test-planner: 70 rows (`planner` output in the lead's transcript); the rows it marked UNCOVERED that matter are the ones listed above (A13/A14, B5/B6, D2/D3, E11, E13, F3/F4/F5/F6/F10).
test-reviewer: **GAPS** (10 items). Moved helpers (`cli/mod.rs` → `answer.rs`, draft/send bodies) lost no coverage: `tests/inbox.rs` and `tests/cli.rs` are byte-identical and still pass; the moved unit tests are in `answer.rs`.
Mutant sweep (`mutants.txt`, `mutant-*.diff`; 20 mutants, each run against `--lib`, `--test policy`, `--test inbox`, `--test cli`, `--test server` separately): 19 killed, 1 survived — **M16** (`remove(AUTO_ERROR)` on the success path deleted). Judged equivalent in production: the scan excludes records that carry `auto_error` and the event only fires for fresh records, so the scheduler never re-attempts an errored record; the branch is reachable only by calling `attempt()` directly. Advisory only. Related UX wart (planner C5 / reviewer 10): a human `owl draft` after an auto failure does not clear `meta.auto_error`, so `owl inbox` keeps printing "auto-accept failed … owl draft <id> to retry by hand" for a record that is already `drafted`.
Fixture side effects: `tests/common` keeps `notify: false` (asserted in the AC4 test). The real harness is kept off only because the default `Config` maps no project (unknown project fails before spawn) — no explicit fixture switch and no test asserting it; advisory (`ac4-fixture-config.txt`).

FAILING: AC3 (deny finish failure-path tests), AC4 (blocked-done through `attempt` + stale envelope; `pull_interval_secs` non-default / release-while-running; in-flight overlap; AutoAnswered notify through daemon), AC6 (blocked log append through `send`: failure-path test missing, and the answer ships unlogged with no retry — decide log-before-finish or best-effort logging)

### Round 2 — 2026-09-03, `task/OWL-011-policy` @ b0ec9c2 (re-verify AC3, AC4, AC6)
Gate: build 0, `cargo test --workspace` 0 (all bins green, `r2-gate.txt`), clippy -D warnings 0, fmt 0. 3× each: `--test policy` 18/18 (~4.5 s), `--test policy_notify` 1/1, `--test pull` 8/8 — no flakiness. `tests/pull.rs` is byte-identical to `task/OWL-008-pull` @ 564b61b (no regression from the merge; `tests/ask.rs` 21/21 also green). Criteria unchanged.

| AC | Verdict | Evidence |
|---|---|---|
| AC3 | PASS | `cli/deny.rs` unit tests: peer/state negative twins, zero held, blocked `done/` (inbox bytes untouched, retry), `chmod 0o555 inbox/` (asserts the `removing` error so an unengaged guard fails loudly, retry). CLI: `deny nobody` exit 1 with overlay untouched, zero held, `--json` shape. Mutants R8 (state), R9 (no finish) killed. |
| AC4 | FAIL (one test gap) | Fixed and proven: `attempt` failing after the envelope exists leaves `drafted` + `auto_error`, manual `owl send` reuses the envelope, log keeps exactly one `auto` line (`r2-attempt-repro.txt`); scan at `pull_interval_secs=1` sees `owl allow --always` while running (R10 hard-coded interval killed); event+scan overlap counted via `FAKE_HARNESS_LOG` (R7 claim bypass killed); `tests/policy_notify.rs` asserts one `auto-answered Ana about …` line (R11 killed); fixture default asserted by `common_fixture_runs_no_harness_by_default`. Gap: the *pre*-envelope failure arm (`fail` → back to `pending`, draft dropped) has no test — mutant **R5** (`fail` → `fail_as_is`) survived all six targets; not equivalent (record would sit `drafted` with a draft instead of `pending`). Behaviour verified inline with `chmod 0o555 spool/outbox`: `Failed("writing …outbox/….json.tmp: Permission denied")`, record `pending`, `auto_error` set, no new envelope, no log line. Fix: one unit test in `src/auto.rs` (`send_failure_before_envelope_resets_to_pending`). Advisory hole (OWL-010 shape, "every command allowed between failure and retry"): after a post-envelope failure the inbox note says `owl draft <id> to retry by hand`; `owl draft` has no `existing_answer` guard (unlike `owl edit`), so it re-runs the harness, replaces the draft and clears `auto_error`, and the next `owl send` silently ships the *old* envelope. Either point the note at `owl send` when an envelope exists, or make `draft` refuse like `edit` does; test it. |
| AC6 | PASS | Order is now outbox+cache → `has_outgoing_line` → append → `finish`. Inline (`r2-send-repro.txt`): blocked `log/outgoing.jsonl` → `owl send` rc 1, record still `drafted`, envelope spooled, no log line; retry rc 0, exactly one line, one envelope. Blocked `done/` → rc 1 with the line already written; retry finishes without a second line or envelope. Mutants R1 (log after finish), R2 (dedup removed), R3 (dedup inverted), R12 (dedup ignores question_id), R13 killed; unit `has_outgoing_line_tolerates_missing_file_and_garbage` covers missing file, garbage lines, wrong-type id, unreadable path. |

test-reviewer round 2: **GAPS** (1 item = the R5 arm above); everything else from round 1 confirmed closed with real assertions. Mutants: 13 run against `--lib`, `--test policy`, `--test policy_notify`, `--test inbox`, `--test cli`, `--test server` separately (`r2-mutants.txt`, `r2-mutant-*.diff`): 12 killed, 1 survived (R5). M16 re-run as R6: killed by `success_clears_stale_auto_error` and `manual_draft_clears_auto_error`.

FAILING: AC4 (untested pre-envelope failure arm in `auto::attempt` — mutant R5 survives; plus the advisory `owl draft`-after-envelope hole)

### Round 3 — 2026-09-03, `task/OWL-011-policy` @ db5182a (re-verify AC4)
Gate: build 0, `cargo test --workspace` 0, clippy -D warnings 0, fmt 0 (`r3-gate.txt`). 3× each: `--test policy` 20/20 (~4.5 s), `--test inbox` 23/23, `--lib auto` 13/13 — no flakiness.

| AC | Verdict | Evidence |
|---|---|---|
| AC4 | PASS | `src/auto.rs::send_failure_before_envelope_resets_to_pending` (outbox `chmod 0o555`; `pending`, draft dropped, `auto_error` set and containing the error, meta keys kept, no envelope / log / done, not a scan candidate) — round-2 survivor **R5** (`fail` → `fail_as_is`) now dies on it. `owl draft` refuses when `existing_answer` finds an envelope: `tests/policy.rs::draft_refuses_when_envelope_exists` (plain and `--json`, exit 1, stdout empty, names `outbox/<aid>.json` and `owl send <id>`, harness log absent, record byte-equal before/after; retry `owl send` ships the spooled envelope, one log line; negative twin: a drafted record without an envelope re-drafts). `inbox_note_points_to_send_when_envelope_exists` asserts `owl send <id>` with an envelope and `owl draft <id>` without, each excluding the other. Mutants (`r3-mutants.txt`, each against `--lib`, `--test policy`, `--test inbox`, `--test cli`): R5, S1 (draft guard removed), S2 (note branch inverted), S3 (note drops the id), S4 (refusal drops the id) — all KILLED. |

FAILING: none

PR: https://github.com/Krab00/owlpost/pull/15 (draft, base task/OWL-008-pull)
