---
id: OWL-040
title: "Tool-call request: a peer sends a tool name and input, I approve it by hand, the tool runs on my machine when I draft, and the result goes back in the thread"
status: todo
deps: [OWL-039]
source: adhoc
source_updated_at: 2026-09-14
criteria_source: derived
---
## Description

Decision (2026-09-14): the third and last kind of request in a thread. A peer sends the **name
of a tool** from a registry I configured and an **input object**; the request is held for my
consent like every content request; when I run `owl draft <id>` the tool runs on my machine; the
result is redacted, stored on the record and, on my explicit `owl send`, goes back to the peer.
Both sides then see in the thread what ran, with what input, and what came out.

The rules of OWL-039 hold unchanged and are the reason this task is safe at all:

1. **Manual consent, per request, every time** — policy `auto` never applies to a tool-call
   request; `never` still short-circuits to `403`.
2. **The registry is the allowlist** — only a tool the owner configured by name can be named,
   and the input is never interpolated into its argv. An unknown tool is a `4xx` at the API and
   never reaches consent.
3. **Nothing runs at arrival, and nothing runs on `allow`.** The daemon never spawns a child for
   a tool-call request. The only process that ever executes the tool is the CLI, inside
   `owl draft <id>`, which a human types after allowing the request.

### 1. Envelope (§6) — two more `Kind` variants

For the reason given in OWL-039 §1 (`Body` is `#[serde(untagged)]`, so a body discriminator
fails silently while a `Kind` variant fails at compile time), the tag is again the kind:

```rust
#[serde(rename = "tool-call")]  ToolCall,
#[serde(rename = "tool-reply")] ToolReply,
```

```json
{ "tool": "test", "input": { "package": "auth", "filter": "session" }, "project": "github.com/company/monorepo" }
{ "output": "…", "exit_code": 0, "duration_ms": 4120, "truncated": false, "redactions": 1, "harness": "human" }
```

```rust
ToolCall {
    tool: String,
    input: serde_json::Map<String, Value>,
    #[serde(default, skip_serializing_if = "Option::is_none")] project: Option<String>,
},
ToolReply {
    output: String,
    exit_code: i32,
    duration_ms: u64,
    truncated: bool,
    redactions: u32,
    harness: String,      // always "human"
},
```

- `input` is a JSON **object** (never a bare string or array), capped at the tool's
  `max_input_bytes` measured on its compact serialisation.
- `MAX_OUTPUT_BYTES: usize = 262_144`, the same cap and the same character-boundary cut as
  OWL-039's content, with `truncated: true` when it bites. The output carries no `sha256`: it is
  the record of one run, not a file the asker can re-derive.
- `exit_code` is the child's code, or `-1` when the tool was killed by a signal or by the
  timeout (`duration_ms` then equals the timeout).
- `context_id` threads exactly as for a question and a content request; `envelope::a2a_state`
  again gains **no row**.

### 2. Config (§3) — the tool registry

```json
"responder": {
  "tools": {
    "test":  { "argv": ["cargo", "test", "--workspace"], "cwd": "github.com/company/monorepo", "timeout_ms": 600000, "max_input_bytes": 4096 },
    "build": { "argv": ["cargo", "build"], "cwd": null, "timeout_ms": 300000, "max_input_bytes": 1024 }
  }
}
```

`BTreeMap<String, Tool>`, empty by default (`owl init` writes `{}`), so the feature is off until
the owner adds a tool by hand. `Tool { argv: Vec<String>, cwd: Option<String>, timeout_ms: u64,
max_input_bytes: usize }`. Rules, each of them a refusal the tests pin:

- `argv` must be non-empty; `argv[0]` is resolved on `PATH` like a harness command.
- **The input never touches argv.** It is written to the child's stdin as the compact JSON of the
  `input` object followed by one `\n`, and stdin is closed. No `{input}` placeholder exists, and
  a config whose `argv` contains the literal `{input}` is refused at load
  (`config: tools.<name>.argv must not interpolate the input`). The tool reads its arguments from
  stdin or it is not usable here.
- `cwd` names a key of `config.projects` (the checkout the tool runs in) or is `null` (then
  `$OWLPOST_HOME`). A `cwd` naming an unknown project is refused at load.
- Environment: `OWLPOST_TOOL=1`, `OWLPOST_TOOL_NAME=<name>`, `OWLPOST_PEER=<fingerprint>`, PATH
  inherited; nothing else is added and nothing from the request reaches the environment.

### 3. Server (§7) — same route, same consent, one more validation arm

`validate_request` (OWL-039 §3) gains `tool-call`; `type` may now be `question`, `content` or
`tool-call` (`400 type must be question, content or tool-call`).

| Check | Response |
|---|---|
| `body.tool` missing, empty, or not a key of `responder.tools` | `400 unknown tool` |
| `body.input` missing or not a JSON object | `400 body.input must be an object` |
| compact `body.input` longer than the tool's `max_input_bytes` | `400 input is <n> bytes, max <m>` |
| `body.project` present and not a key of `config.projects` | `400 unknown project` |
| `responder.tools` empty | `400 unknown tool` (no registry, nothing to name) |

`unknown tool` deliberately does not distinguish "no such tool" from "no registry": a peer must
not be able to enumerate the owner's tools by their error messages.

Then, as in OWL-039: `never`/responder disabled → `403`; otherwise the record is spooled
**`consent`** for every policy (`record_state_for(Kind::ToolCall, _) == ("consent", false)`); no
cache read, no cache write; `202 … TASK_STATE_SUBMITTED`. `auto::not_a_candidate`'s kind guard
covers `Kind::ToolCall` with `record <id> is a tool-call request — consent only`.

Nothing in `src/server.rs`, `src/daemon.rs`, `src/auto.rs` or `src/pull.rs` ever constructs a
`std::process::Command` for a tool. The spawn lives in `src/tools.rs` (new), called only from
`src/cli/draft.rs`.

### 4. CLI (§9)

- **Asker** — `owl call <peer> <tool> --input <file|-> [--project <id>] [--reply-to <id>]`.
  The input file must parse as a JSON object (`exit 1 input must be a JSON object`), is sent
  verbatim, and the `asks/` record gets the event `tool-requested`. Prints
  `accepted <id> — waiting for the owner's consent`; `--json` adds `"state"`. No `--wait`, for
  the reason given in OWL-039.
- **Responder** — `owl inbox` lists the record with `type: "tool-call"` and `summary` =
  `<tool> <compact input, cut to 60 chars>`; `owl show <id>` prints the tool name, the resolved
  `argv`, the `cwd` and the input pretty-printed, so the human sees exactly what would run before
  allowing anything. `owl allow` / `owl deny` behave as always.
  - `owl draft <id>` on a tool-call record is the **only** thing that runs the tool. It refuses a
    record still in `consent` with the existing message, spawns `argv` in `cwd` with the input on
    stdin, bounds the run by `timeout_ms` (kill on expiry, `exit_code: -1`), captures stdout and
    stderr into one stream (stdout first, then stderr under a line `--- stderr ---` when stderr
    is non-empty), runs `runner::redact`, cuts at `MAX_OUTPUT_BYTES` and stores the result as the
    draft together with `exit_code`, `duration_ms`, `truncated`, `redactions` and
    `harness: "human"`. It prints
    `ran test in 4.1s — exit 0, 3120 bytes, 1 redaction` (`truncated` appended when cut) and then
    the output. A non-zero exit is **not** a CLI failure: the draft is stored and `owl draft`
    exits 0, because a failing build is a legitimate answer; only a tool that could not be
    started at all is exit 1 (`tool test: no such file or directory`). `--harness`, `--text`,
    `--agent` and `--prompt` are refused as in OWL-039.
  - `owl edit <id>` on a tool-call record is refused (implementation note, OWL-040:
    `record <id> is a tool-call request — its draft is what the tool printed and cannot be
    edited; owl reject <id> declines it`). The task file did not name it, but the draft is what
    the tool printed next to the exit code it printed it with, so an edited output would be a
    claim about a run that never happened — the same rule OWL-039 applies to a content draft.
  - `owl send <id>` signs a `tool-reply` and queues it; `owl reject <id>` discards it. As in
    OWL-039, `send` omits `envelope::question_hash` and the outbox record's `meta.hash` for this
    kind (`answer::question_body` errors on a `ToolCall` body), not only the cache write.
  - **The result stays on my side.** `answer::finish` for a tool-call record keeps the draft on
    the `done/` record (it is not cleared on send), so `owl thread` and `owl show` still show
    what ran, with what input and what came out, after the reply has gone.
- **Asker again** — the pulled reply lands in `inbox/` as a `tool-reply` record; `owl show` prints
  `exit <code> · <duration> · <n> bytes` and the output in a plain ```` ```text ```` block, with
  OWL-039's 200-line display cap.
- **Thread** — the event kinds `tool-requested` (out), `tool-run` (`by: "human"`,
  `detail: {"tool": "test", "exit_code": 0, "duration_ms": 4120}`), `tool-sent` and
  `tool-received`, all plain strings in `meta.events`.

### 5. The mod and the plugin text

- `owlpost.tsx`: the timeline renders the four new kinds; the open tool-call request shows the
  tool name and the input (cut to three lines) with **Run (owl draft)**, **Send** and **Reject**.
  **Run** goes straight to `act($, ['draft', id])` — no model, no `/owlpost:draft`. The note line
  shows the command's first output line, so a long run is visibly in flight.
- `commands/inbox.md` and `skills/owlpost/SKILL.md` gain, next to OWL-039's block:

  > **A tool-call request runs nothing until the human says so.** Show the tool name, the argv
  > it resolves to, the working directory and the input in full, and wait.
  > owl draft <id> is what executes the tool, so run it only after the human allowed this
  > request and asked for the run.
  > Never invent a tool name, never edit the input, and never run `owl send` before the human
  > has read the output.

  Implementation note (OWL-040): `owl draft <id>` in the second sentence is written **without**
  backticks on purpose. AC10 pins the literal `owl draft <id> is what executes the tool` on one
  line, and the closing backtick after `<id>` breaks that substring, so the shipped block and
  the pin can only agree in the plain form.

- `plugins/claude-code/commands/call.md` (new, `allowed-tools: Bash(owl call:*)`) with its README
  row and `SKILL.md` mention, so the OWL-021 parity tests stay green.

### 6. Docs

`docs/technical-design.md`: §3 (`responder.tools`, the four keys, the no-interpolation rule, the
environment), §6 (the two kinds, both bodies, `MAX_OUTPUT_BYTES`, `exit_code: -1`), §7 (the new
validation rows and why `unknown tool` is deliberately coarse), §8 (the tool-call record's path
and the four event kinds, and that the draft survives `send`), §9 (`owl call`, `owl draft` on a
tool-call record, `owl show`), §10 (redaction over tool output; the responder runner is not
involved), §13 (a `ponytail:` line: one tool run at a time, no streaming of its output).
`docs/architecture.md` §3.8 "Tool-call request" and §4's Leakage row naming the registry.
`docs/guide.md` a section "Let a colleague run one of your tools". `tests/docs.rs` pins the new
literals.

### 7. Test rules specific to this task

Implementation note (OWL-040): the fixture takes its knobs as **arguments**
(`FAKE_TOOL_LOG=<path>`, `FAKE_TOOL_EXIT=<n>`, `FAKE_TOOL_SLEEP=<secs>`, `FAKE_TOOL_STDERR=<text>`,
`FAKE_TOOL_SECRET=1`, `FAKE_TOOL_BIG=<bytes>`), not as environment variables. §2 clears the child's
environment down to `OWLPOST_TOOL`, `OWLPOST_TOOL_NAME`, `OWLPOST_PEER` and `PATH`, so no
environment knob could reach the script at all; the knobs travel in the owner-controlled `argv`
instead, and the log's `env=` field is what pins the clearing rule. The script writes one `start`
line (argv, environment, stdin, cwd) and, after any sleep, one `done` line, so the timeout test can
tell a run that started from one that finished.

Every automated test uses a **fixture script** as the tool — `tests/fixtures/fake-tool.sh`, which
echoes its stdin, writes a line to `$FAKE_TOOL_LOG`, and honours `FAKE_TOOL_EXIT` and
`FAKE_TOOL_SLEEP` — never a real build or test command. Each test has its own `$OWLPOST_HOME`
and binds `127.0.0.1:0`, as §12 requires.

## Acceptance criteria
- [ ] AC1: a `tool-call` payload round-trips with the wire keys `type: "tool-call"`, `tool`, `input`, `project`, and a `tool-reply` with `type: "tool-reply"`, `output`, `exit_code`, `duration_ms`, `truncated`, `redactions`, `harness`; questions, answers and OWL-039's content bodies still parse unchanged; `Payload::tool_reply` copies the request's `context_id` (modality: test)
- [ ] AC2: `POST /v1/questions` answers `400` with the exact message for each validation row — an unknown tool name, an empty registry, a missing `input`, an `input` that is a string and one that is an array, an `input` one byte over `max_input_bytes`, an unknown `project` — and in each case `spool/inbox/` stays empty and `$FAKE_TOOL_LOG` is never created; a valid request answers `202 … TASK_STATE_SUBMITTED` (modality: test)
- [ ] AC3: a tool-call request from a peer with policy `auto` lands in `consent`, `record_state_for(Kind::ToolCall, Some(Mode::Auto))` returns `("consent", false)`, the scheduler skips it with `record <id> is a tool-call request — consent only`, and a `never` peer gets `403`; the positive twin (a question from the same peer) is still auto-answered (modality: test)
- [ ] AC4: the daemon never spawns the tool — with a valid request delivered, `owl allow <peer>` run, and the daemon left running for two scheduler ticks, `$FAKE_TOOL_LOG` does not exist; it appears only after `owl draft <id>` in the CLI process, and the log's single line proves exactly one run (modality: test)
- [ ] AC5: `owl draft <id>` passes the request's `input` to the tool **on stdin** as compact JSON plus `\n` and passes no part of it in argv — `tests/fixtures/fake-tool.sh` records its own `$@` and its stdin, the recorded argv equals the configured `argv` exactly, and the recorded stdin equals the compact `input`; a config whose `argv` contains `{input}` is refused at load with `config: tools.<name>.argv must not interpolate the input`, and a `cwd` naming an unknown project is refused too (modality: test)
- [ ] AC6: a tool exiting 3 stores the draft with `exit_code: 3` and `owl draft` exits 0; a tool sleeping past `timeout_ms` is killed, stores `exit_code: -1` with `duration_ms` at the timeout, and `owl draft` itself returns within a bounded wall time after the timeout (per OWL-029 every wait on a child is bounded) while `$FAKE_TOOL_LOG` shows the tool started and never wrote its completion line; a tool whose `argv[0]` does not exist exits 1 with `tool <name>: no such file or directory` and stores no draft (modality: test)
- [ ] AC7: the output is redacted (`responder.redact` turns the fixture's secret line into `[redacted]`, `redactions == 1`), stderr is appended under `--- stderr ---` only when non-empty, and an output longer than `MAX_OUTPUT_BYTES` is cut at a character boundary with `truncated: true` (multi-byte fixture, per OWL-032) (modality: test)
- [ ] AC8: end to end over two daemons — A runs `owl call B test --input in.json`, B's record is `consent`, `owl show` on B prints the tool name, the resolved argv, the cwd and the input in full, then `owl allow`, `owl draft`, `owl send`; A's pull ingests the reply and `owl show` on A prints `exit 0`, the duration and the output; B's `done/` record still carries the draft and the input after the send (modality: test)
- [ ] AC9: `owl thread <peer> --json` shows `tool-requested`, `tool-run` (with `by: "human"` and `detail` carrying `tool`, `exit_code` and `duration_ms`), `tool-sent` on B and `tool-requested`, `tool-received` on A, in `ts` order, interleaved correctly with a question and a content request of the same `context_id` (modality: test)
- [ ] AC10: `tests/plugin.rs` pins, single-line: `A tool-call request runs nothing until the human says so`, `owl draft <id> is what executes the tool` and `Never invent a tool name, never edit the input` in both `commands/inbox.md` and `skills/owlpost/SKILL.md`, and `owl call <peer> <tool> --input` in `commands/call.md`; the block sits inside the consent section of each file (pinned by position, bounded by the next heading); the OWL-021 parity tests pass with `call` added (modality: test)
- [ ] AC11: `docs/technical-design.md` §3/§6/§7/§8/§9/§10/§13 and `docs/architecture.md` §3.8/§4 are updated as described and `docs/guide.md` gains the section; `tests/docs.rs` pins `responder.tools`, `tool-reply`, `max_input_bytes`, `must not interpolate the input`, `owl call` and `MAX_OUTPUT_BYTES` in the design doc, each on its own table/executable line (modality: test)
- [ ] AC12: with mods on, the thread timeline shows the tool-call request with its tool name and input, **Run (owl draft)** runs the tool with no model turn and the note line reports the exit code, the output is visible before **Send**, and the buttons appear next to the open request only (checked by the user; the transcript is the evidence) (modality: manual)
- [ ] AC13: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

### Open questions for the operator
1. `owl draft` blocks for the whole run, so a ten-minute build blocks the terminal and the mod's press. Is a background run with a `running` state wanted later, or is blocking acceptable for the first version?
2. The registry is global: every contact who reaches consent may name any configured tool. Should a tool be restrictable to named fingerprints in the same config block?
