---
id: OWL-029
title: "Plugin: the inbox watch arms for real — per-session re-arm until live, owl update replaces the active binary, claude -p proof"
status: done
deps: [OWL-026]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
Finding from 2026-09-06 (fourth recurrence of "the watch was not armed": OWL-023, b9fd1e6,
8ecd3b3, OWL-026). Two root causes, neither closed by the earlier fixes:

1. **The fixes never reached the machine.** `owl update --source <dir>` runs
   `cargo install --path`, which writes `~/.cargo/bin/owl`, while the release installer
   (`scripts/install.sh`) put `owl` in `~/.local/bin`; PATH and the systemd unit
   (`ExecStart=/home/kris/.local/bin/owl`) resolve that copy. After every `owl update` the old
   binary stayed active and the hook kept printing the old sentence, although `cargo test` on
   the checkout was green. Observed today: `~/.cargo/bin/owl` 14:22 (new), `~/.local/bin/owl`
   00:04 (old), hook output = old pointer sentence.
2. **One skipped turn loses the watch for the whole session.** The arm sentence is injected once
   (`SessionStart`); when the model treats it as ambient context (it did again today, first
   turn `/owlpost:ask`), nothing ever asks again. A hook cannot see whether a Monitor runs —
   but the Monitor's own process can leave a marker the hook can see.

Fix (three parts; the design doc §9 and §11 are updated in the same PR, and where this file
and the design doc disagree the design doc wins and this file is corrected):

### A. `owl update` replaces the binary that is actually running
- `run_update` computes `active = install::owl_path()` (canonicalised `current_exe`) before
  anything else. `--source <dir>` builds with `cargo install --path <dir> --locked --root <tmp>`
  (so nothing lands in `~/.cargo/bin`), then replaces `active` with `<tmp>/bin/owl` by
  temp-file + `rename` in the same directory (a plain copy fails with `ETXTBSY` while the
  daemon runs the file; rename does not). Without `--source` the release installer is run with
  `--prefix <tmp>` and the same copy + `rename` step replaces the active copy (corrected after
  round 1 to match design doc §11: both paths go through the temp root so the byte
  verification covers the release path too). After the replacement the
  command verifies that `active` is byte-identical to the built file (sha256 or a full read
  compare) and prints `installed <active>`; a mismatch is a hard error naming both paths.
  `--dry-run` prints `would replace <active>`. The unit reinstall stays as is (it already
  points at `owl_path()`).
- `owl doctor` gains a `binary` check: `ok binary: <path>` when the `owl` that PATH resolves
  (first `owl` in `$PATH`, canonicalised) is the same file as `current_exe` and the daemon unit
  (if installed) names that path; `warn binary: PATH resolves <p>, this owl is <q>` /
  `warn binary: no owl on PATH, this owl is <q>` / `warn binary: daemon unit runs <r>`
  otherwise (three warn forms per design doc §9, corrected after round 1), never `fail`. The
  PATH lookup is a small
  function that takes the `PATH` string and is unit-tested with a temp dir.

### B. Per-session re-arm until the watch is live
- New `owl inbox --count --follow --session <id>` (plain format only): on start writes a marker
  `$OWLPOST_HOME/watch/<session id>` containing its own pid, then loops: every 5 s runs the
  count, prints the counter sentence (with the owl icon, OWL-027) only when it changed since the
  previous poll, prints nothing at zero (so "back to zero" is silent), never marks anything
  seen; on a clean exit it removes the marker (a killed process leaves it — that is fine, see
  staleness). The session id is validated as `[A-Za-z0-9._-]{1,128}`; anything else is a usage
  error, so the marker path can never escape `watch/`. This replaces the shell one-liner of
  OWL-023/026; `commands/watch.md`, SKILL.md and the design doc quote the new command instead
  and the OWL-026 "one-liner stated twice, pinned byte-identical" rule now applies to the
  command template `owl inbox --count --follow --session <session id>`.
- A marker is **live** when its pid is alive (`/proc/<pid>` on Linux, `kill -0` elsewhere —
  implementer's choice, tested with a spawned `sleep` child and with a pid that cannot exist);
  stale markers are ignored and may be removed by the hook path.
- `owl inbox --count --format claude` reads the hook's stdin JSON when stdin is not a terminal
  (Claude Code sends `{"session_id": "...", "hook_event_name": "...", ...}`; only `session_id`
  is used; unparseable or missing stdin means "no session id"). The arm sentence is emitted
  on `SessionStart` **and** `UserPromptSubmit` (never `PostToolUse`) when the stored default
  is on (`plugin.json`, unchanged semantics) and no live marker exists for that session id.
  Without a session id the sentence is emitted on `SessionStart` only (old behaviour, so a
  harness that sends no stdin keeps working). `--session-start` is removed from `hooks.json`
  and from the CLI (or kept as an ignored no-op with a `// ponytail:` — implementer's call, but
  the tests must not depend on it).
- `ARM_SENTENCE` becomes a template carrying the session id:
  `owlpost: before handling this prompt, arm the inbox watch for this session: call Monitor
  with persistent: true, description "owlpost inbox" and this command (verbatim):
  \`owl inbox --count --follow --session <id>\`. Then handle the prompt.` It must still start
  with `owlpost: before handling this prompt`, stay on one line and keep `--format claude`
  valid JSON.
- `commands/watch.md`: `status` arms (as OWL-026) using the command from **this turn's**
  sentence; when this turn carries no sentence and the default is on, the watch is already
  live — say `owlpost watch: running; default on`. `on` writes `{"watch": true}`, arms from
  this turn's sentence when there is one, otherwise says
  `owlpost watch: on; it arms on your next prompt` (the next `UserPromptSubmit` injects the
  sentence). `off` unchanged (TaskStop + `{"watch": false}`; the marker disappears with the
  process). SKILL.md "Live watch": the sentence may appear on any turn, not only the first;
  whenever it is present, arm — its presence means no watch is live for this session, even if
  one was armed earlier and died.

### C. Proof on the real harness: `scripts/e2e-watch-arm.sh`
A script for humans **and** for this task's verifier. For each first prompt in
`/owlpost:watch status`, `/owlpost:whoami`, `what is 2+2? answer with the number only`:
1. creates a temp `OWLPOST_HOME` (`owl init` there, no daemon needed — `--count` reads the
   spool only);
2. runs `claude -p "<prompt>" --output-format stream-json --verbose --max-turns 6` in the
   background (the persistent Monitor keeps `claude -p` alive, so the run is bounded by
   `timeout 180`), optionally with `--plugin-dir <repo>/plugins/claude-code` and `PATH` led by
   the branch build (`E2E_PLUGIN_DIR`, default: the installed plugin and the `owl` on PATH);
3. takes the session id from the first stream event, waits up to 120 s for
   `$OWLPOST_HOME/watch/<session id>` with a live pid, and asserts the stream holds one
   `tool_use` named `Monitor` whose input has `"description":"owlpost inbox"` and
   `--session <that id>`;
4. kills the run, prints `PASS <prompt>` / `FAIL <prompt> (<reason>)` and exits non-zero on
   any FAIL. The stream files are kept under `E2E_OUT` (default: a temp dir printed at the
   end) as evidence.
The verifier runs it against the task branch build and attaches the output; after the merge
the orchestrator runs `owl update --source .` on this machine and runs it once more against the
installed binary and plugin (AC8). Cost: three `claude -p` calls per run.

Out of scope: Codex/Kimi; OS notifications; anything that makes the hook itself start a
process (hooks must stay pure and fast).

## Acceptance criteria
- [x] AC1: `owl update --source <dir>` (and the release path) replaces the file at `install::owl_path()`, not `~/.cargo/bin`: a test drives `run_update`'s replacement step with a temp "active" file that a child process holds open/executing (e.g. a copied `/bin/sleep` running) and asserts the file at the active path is byte-identical to the built file afterwards, the child is unaffected, and `installed <active>` is printed; a forced mismatch (built file altered before the compare) is an error naming both paths; `--dry-run` prints `would replace <active>` (modality: test)
- [x] AC2: `owl doctor` prints `ok binary: <path>` when PATH's first `owl` canonicalises to `current_exe` and the unit (when present) names it, `warn binary: PATH resolves <p>, this owl is <q>` when a different `owl` precedes it in `PATH`, `warn binary: daemon unit runs <r>` when the unit names another path; tested with a temp dir on `PATH` and a temp unit file; never `fail` (modality: test)
- [x] AC3: `owl inbox --count --follow --session <id>` writes `$OWLPOST_HOME/watch/<id>` containing its pid at start, prints the counter only on change and nothing at zero (driven with a fake home whose spool the test mutates between polls; poll interval overridable for tests via `OWLPOST_FOLLOW_SECS` or a hidden flag), removes the marker on clean exit; an id outside `[A-Za-z0-9._-]{1,128}` exits 2 with a usage error and writes nothing (modality: test)
- [x] AC4: `owl inbox --count --format claude` with hook stdin `{"session_id":"S",...}`: emits the arm sentence on `--hook-event SessionStart` and `UserPromptSubmit` when no live marker exists for `S`, omits it when `watch/S` holds a live pid (spawned `sleep`), emits it again when `watch/S` holds a dead pid (and removes the stale marker), never emits it on `PostToolUse`, never when `plugin.json` is `{"watch": false}`; without stdin (a terminal or empty input) the old rule holds (`SessionStart` only); every output line is valid JSON (serde_json in the test) and `additionalContext` contains `--session S` and `description "owlpost inbox"` (modality: test)
- [x] AC5: the command template `owl inbox --count --follow --session <session id>` is stated once in Rust (`ARM_SENTENCE` or the `const` it embeds) and once in `commands/watch.md`, pinned byte-identical by a test; `commands/watch.md` `on`/`status` sections and SKILL.md "Live watch" carry the lines named in part B (`it arms on your next prompt`, `whenever it is present, arm`), pinned in `tests/docs.rs` / `tests/plugin.rs`; `hooks.json` has no `--session-start`; design doc §9 hook-injection bullets and §11 describe the marker and the per-turn re-arm (modality: test)
- [x] AC6: `tests/plugin.rs` runs the real `hooks/owl-count.sh` through `env -i /bin/sh` with the Claude Code stdin JSON on all three hook events and parses the output with jq/serde_json; the `--follow` command from the emitted sentence is executed for real against the same home and the marker appears with a live pid (modality: test)
- [x] AC7: `scripts/e2e-watch-arm.sh` run by the verifier against the task branch (`E2E_PLUGIN_DIR=plugins/claude-code`, `PATH` led by the branch's `target/release`) prints `PASS` for all three first prompts; the three stream files and the script output are attached as evidence (modality: manual)
- [ ] AC8: after the merge, on this machine: `owl update --source .` prints `installed /home/kris/.local/bin/owl`, `owl doctor` prints `ok binary: /home/kris/.local/bin/owl`, `owl inbox --count --format claude --hook-event SessionStart <<< '{"session_id":"x"}'` prints the new sentence, and `scripts/e2e-watch-arm.sh` with no overrides (installed plugin, installed binary) prints `PASS` three times — run by the orchestrator, output kept in this file (modality: manual)
- [x] AC9: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-029-watch-arm-real

Round 1, 2026-09-06, branch at 85e4d88 (base main bbb82a4), clean detached worktree `.worktrees/verify-OWL-029` with a private `target/` (removed afterwards). Evidence under `.orch/tasks/OWL-029.evidence/`. `cargo test -- --list` named all 24 new tests before any run was trusted. test-planner: 70 rows (12 GAP rows run by hand, see `manual-rows.txt`). test-reviewer: **GAPS** (AC1 end-to-end drive has no executing child; AC2 unit arm never reaches the real `owl doctor`; two minor arms). Verifier sweep: 60 mutants, 49 RED, 7 genuine SURVIVED, 2 hangs (`mutants.md`).

| AC | verdict | evidence |
|---|---|---|
| AC1 | **FAIL** (test modality; behaviour correct) | Behaviour: `manual-rows.txt` U9-real — `owl update --source` with `OWLPOST_UPDATE_ACTIVE` = a running copy of `/bin/sleep` replaces the file (child keeps the old inode, active holds the built bytes, mode 0755, `.new` gone, `installed <active>` printed, unit reinstalled after); U13/U15 failure paths stop before the unit steps and leave no temp dir; `--dry-run` prints `would replace <active>` (`tests/cli.rs`, `tests/setup.rs`). Tests: `tests/setup.rs` drives `run_update` with an inert text file, so **M01** (`install_binary` → plain `fs::copy`, the exact ETXTBSY regression) SURVIVED; **M06** (build step produced no file), **M10** (unit reinstall still runs after a failed replacement — an ordering claim without a failure-path test), **M07** (`OWLPOST_UPDATE_ACTIVE=""`) SURVIVED. Also the new unit test `replace_binary_swaps_a_running_executable` flakes (2 hits in the sweep, 1/25 plain runs: `ETXTBSY` on spawning its own sleep copy under parallel threads). |
| AC2 | **FAIL** (test modality; behaviour correct) | Behaviour: `manual-rows.txt` D16 — real `owl doctor` with a unit under `HOME`: `warn binary: daemon unit runs /opt/other/owl` (text and `--json`), `ok binary: <path>` when the unit names the binary, two warn rows for no-owl + wrong unit, never `fail`; `tests/doctor.rs` covers ok / PATH resolves (text + `--json`) / no owl on PATH (text). Tests: **M15** (`run_checks` passes `None` for the unit) SURVIVED — the `daemon unit runs` arm is proven only on the pure fn, never through the binary; no `--json` row through the binary for `no owl on PATH` and `daemon unit runs` (OWL-025 rule); **M17** (directory named `owl` on PATH) SURVIVED, minor. |
| AC3 | PASS | `tests/follow.rs` (10 tests): marker holds the child's pid, prints only on change, silent at zero and on 3→0, marker removed on clean exit, exit on marker removal (M35 RED in 22 s) and on stdout close (M36 RED in 21 s), never marks seen, `OWLPOST_FOLLOW_SECS` (M39), bad ids exit 2 and write nothing (M21–M23, M33, M43); hand run in `real-hook-grid.txt`; F10 (`watch` is a file → exit 1 `creating …`). Note: **M42/M44** (clap `conflicts_with`/`requires` removed) hang the suite for the full 300 s — `follow_rejects_bad_session_ids_and_flag_combinations_with_exit_2` waits with an unbounded `.output()`; bound it like the other follow waits. |
| AC4 | PASS | 54-cell grid `hook_arms_per_event_marker_state_plugin_json_and_stdin` (event × marker none/live/dead × plugin.json × stdin) with every line parsed by serde_json, `--session S` and `description "owlpost inbox"` pinned; stale marker removed, live marker kept, PostToolUse never (M27), no-stdin rule (M28/M29), watch:false (M30), unvalidated stdin id (M31) all RED; tty stdin checked by hand (`real-hook-grid.txt`, `script -qc`). |
| AC5 | PASS | Template pinned byte-identical in `src/cli/inbox.rs` unit test and `tests/plugin.rs` (M47 RED); `it arms on your next prompt` (M48), `whenever it is present, arm` (M50), status line 5 (M49), hooks.json without `--session-start` (M46), design §9/§11/§12 literals (M51, M52, M54, M57), README (M56), SKILL wording all RED. M53 (design §9 "never on `PostToolUse`" sentence) SURVIVED — not AC-named, behaviour pinned by M27; optional pin. |
| AC6 | PASS | `hook_script_via_env_i_arms_until_the_follow_runs_for_real` runs the real script under `env -i /bin/sh` with Claude Code's stdin JSON on all three events, parses with serde_json, executes the command from the sentence and checks the marker pid; M55 (script drops `"$@"`) RED. Own run with jq: `real-hook-grid.txt`. |
| AC7 | PASS | `e2e-watch-arm-output.txt`: `PASS` ×3 against the branch's `target/release/owl` and `--plugin-dir <wt>/plugins/claude-code`; `prompt{1,2,3}.jsonl` kept; `e2e-streams-analysis.md`: Monitor tool_use at lines 21/18/17, `--session <id>` equals each stream's `session_id`, no answer precedes the arm (only preamble text in prompts 1–2). The installed user-scope plugins also loaded and called the `owl` on PATH, i.e. the branch build. |
| AC8 | PENDING (post-merge) | Orchestrator step on this machine after the merge; not run. |
| AC9 | PASS | `gates-build-fmt-clippy.log`: build, fmt `--check`, clippy `-D warnings` rc 0; `cargo-test-workspace.log`: 489 tests, all green (no flake in that run); `Cargo.toml`/`Cargo.lock` unchanged (no new crate). Caveat: the AC1 unit-test flake (~1/25) can turn this gate red. |

PM-REVIEW (task file vs design doc — design doc wins, task file to be corrected by the orchestrator; the branch does not carry the task file):
- Part A says the release path runs the installer with `--prefix <dir of active>`; the design doc §11 (and the code) run it with `--prefix <tmp>` → `<tmp>/owl` and then the same copy + `rename` step. Same outcome, different mechanism.
- Part A / AC2 list two `warn binary` forms; the design doc §9 adds the third, `warn binary: no owl on PATH, this owl is <q>` (implemented and tested).

FAILING: AC1, AC2 (test gaps only — see rows). Fix shape for round 2: (1) in `tests/setup.rs` `run()` make `active-owl` a 0755 copy of `/bin/sleep` with a child executing it, assert the bytes, `installed <active>`, and that the child still runs; add one variant where the fake `cargo` produces no file (exit ≠ 0, `did not produce`, active untouched, no `owl install`/plugin calls) and one dry-run with `OWLPOST_UPDATE_ACTIVE=""`; (2) in `tests/doctor.rs` one cell with `HOME` holding `.config/systemd/user/owlpost.service` written by `install::unit_text` naming another path, asserting the text line and the `--json` row, plus `--json` rows for the no-owl cell; (3) bound the `.output()` calls in `follow_rejects_bad_session_ids_and_flag_combinations_with_exit_2` (spawn + `wait_exit`), and de-flake `replace_binary_swaps_a_running_executable` (retry the spawn on `ETXTBSY`, or serialise the copy and the spawn behind a test-local mutex).

## Verification — round 2

2026-09-06, branch at c60c043 (one tests-only commit on 85e4d88; the two `src/` hunks are inside `#[cfg(test)] mod tests`, no app code changed, `Cargo.*` untouched), fresh detached worktree with a private `target/` (removed afterwards). `cargo test -- --list` named the four new rows (`update_replaces_a_running_active_binary_end_to_end`, `update_without_a_built_file_stops_before_the_unit_and_plugin_steps`, `update_with_a_failed_replacement_runs_no_unit_or_plugin_step`, `doctor_binary_check_reads_the_daemon_unit_in_text_and_json`) before any run was trusted. test-reviewer on `85e4d88...c60c043`: **OK** (one non-blocking note: the two ETXTBSY spawn-retry loops have no iteration cap). Evidence: `OWL-029.evidence/gates-round2.log`, `cargo-test-workspace-round2.log`, `mutants-round2-reruns.json`.

Re-run of every round-1 survivor and hang (`timeout 300`, same driver): all RED by a named test, none by timeout.

| mutant | round 1 | round 2 | killer |
|---|---|---|---|
| M01 plain copy in `install_binary` | SURVIVED | RED (46 s) | `update_replaces_a_running_active_binary_end_to_end`, `update_with_a_failed_replacement_runs_no_unit_or_plugin_step` |
| M06 no built file not an error | SURVIVED | RED | `update_without_a_built_file_stops_before_the_unit_and_plugin_steps` |
| M07 `OWLPOST_UPDATE_ACTIVE=""` | SURVIVED | RED | `update_dry_run_lists_steps` |
| M10 unit steps after a failed replacement | SURVIVED | RED | both failure-path rows |
| M15 `run_checks` never passes the unit | SURVIVED | RED | `doctor_binary_check_reads_the_daemon_unit_in_text_and_json` (the second listed failure, `doctor_reports_each_check` at `tests/doctor.rs:347`, is the known pull-age timing flake under sweep load, not this branch's path) |
| M17 directory named `owl` on PATH | SURVIVED | RED | `binary_checks_compare_path_and_unit_with_the_running_file` |
| M42 `--follow` with `--format` accepted | hang 300 s | RED (23 s, bounded) | `follow_rejects_bad_session_ids_and_flag_combinations_with_exit_2` |
| M44 `--follow` without `--count` accepted | hang 300 s | RED (23 s, bounded) | same |
| M53 design §9 "never on `PostToolUse`" | SURVIVED | RED | `watch_status_arms_and_docs_quote_the_arm_prefix` |

Flake check: `cargo test --bin owl` 30× with default threads — 0 failures (round 1: 1/25). Gates: build, `fmt --check`, `clippy -D warnings` rc 0; `cargo test --workspace` 492 passed, 0 failed.

| AC | round 1 | round 2 | evidence |
|---|---|---|---|
| AC1 | FAIL | **PASS** | end-to-end row with a child executing the active file (survives, bytes, 0755, no `.new`, `installed <active>` before the plugin steps), both failure-path rows, `OWLPOST_UPDATE_ACTIVE=""` dry-run; M01/M06/M07/M10 RED; de-flaked unit test 30/30 |
| AC2 | FAIL | **PASS** | `owl doctor` with a real unit under a temp `HOME`: `daemon unit runs` and `no owl on PATH` in text and `--json`, single `ok` row when the unit names the binary; M15/M17 RED |
| AC3 | PASS | PASS | clap-guard rows bounded: M42/M44 RED in 23 s |
| AC4 | PASS | PASS | unchanged |
| AC5 | PASS | PASS | M53 now pinned |
| AC6 | PASS | PASS | unchanged |
| AC7 | PASS | PASS (not re-run: no app code, plugin or script changed between 85e4d88 and c60c043; round-1 streams stand) | `e2e-watch-arm-output.txt`, `prompt{1,2,3}.jsonl` |
| AC8 | PENDING | PENDING (post-merge) | orchestrator step |
| AC9 | PASS | PASS | round-2 gates green, no new crate |

PM-REVIEW items from round 1 stand (task-file wording vs design doc §9/§11; design doc wins).

FAILING: none.

Draft PR: https://github.com/Krab00/owlpost/pull/36 (status `done` 2026-09-06; AC8 runs after the human merges it).

## Verification — AC8 (post-merge, installed build, 2026-09-06 19:13–19:16 UTC, gx10)

Merged as PR #36 (be303b9), orch record PR #37 (e1b0278). Run by the orchestrator on the dev machine.

| step | result | evidence |
|---|---|---|
| first `owl update --source .` | ran the OLD `owl update` (the `owl` on PATH was still pre-OWL-029): `cargo install` → `~/.cargo/bin/owl`, no `installed` line. One-time bootstrap needed for installs older than OWL-029: `cp ~/.cargo/bin/owl ~/.local/bin/owl.new && mv -f ~/.local/bin/owl.new ~/.local/bin/owl`. | terminal |
| second `owl update --source .` (new binary) | printed `installed /home/kris/.local/bin/owl`; `~/.local/bin/owl` byte-identical to the build | terminal |
| hook sentence | `owl inbox --count --format claude --hook-event SessionStart <<< '{"session_id":"x"}'` → `owlpost: before handling this prompt, arm the inbox watch for this session…` | terminal |
| installed plugin | `hooks.json` in the plugin cache has no `--session-start` | terminal |
| `scripts/e2e-watch-arm.sh` (installed owl + user-scope plugin, no overrides) | **PASS ×3** (`/owlpost:watch status` 14 s, `/owlpost:whoami` 14 s, `what is 2+2?…` 13 s), streams in `OWL-029.evidence/ac8/` | `ac8/prompt{1,2,3}.jsonl` |
| `owl doctor` after the update | **FAIL**: `warn binary: daemon unit runs /home/kris/.local/bin/owl (deleted)` and `fail daemon: … Connection refused`; `systemctl --user status owlpost` in a 203/EXEC restart loop (counter 12); the unit read `ExecStart="/home/kris/.local/bin/owl (deleted)"` | journalctl |

Root cause of the doctor FAIL: `run_update` computes `active` before the replacement, but `install::install()` (called afterwards to rewrite the unit) calls `install::owl_path()` again; after the rename over the running binary, Linux `current_exe()` reads `/proc/self/exe` as `<path> (deleted)`, `canonicalize` fails and the raw string lands in `ExecStart`. Manual repair on gx10: `sed` the `(deleted)` out of the unit, `daemon-reload`, restart → `ok binary`, `ok daemon`. Hotfix: **OWL-030**.

AC8 verdict: **FAIL** (watch arming proven ×3 on the installed build; the update itself breaks the daemon unit) → OWL-030.
