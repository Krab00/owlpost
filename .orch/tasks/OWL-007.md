---
id: OWL-007
title: `owl ask` — contact resolution, blame routing, cache, send, `--wait`
status: done
deps: [OWL-006]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `client.rs` and `cli/ask.rs` per `docs/technical-design.md §9` and
`docs/architecture.md §3.1`. `client::send_question(book, identity, contact, envelope)` tries
each endpoint in order with a pinned reqwest client (connect timeout 2 s) and maps responses:
200 → verified answer, 202 → accepted id, 403 → Unavailable, 429 → RateLimited(retry_after),
connect errors on all endpoints → Offline. `owl ask <peer> <path> "<question>"`: resolve the
peer, determine `project` (`--project` or the normalised `origin` remote of the current git
repo, else the directory name), check the asker-side cache unless `--no-cache`, send, print the
answer or `accepted <id>` and write `asks/<id>.json`. `--wait <secs>` polls the peer's outbox
every 2 s until the answer arrives (verify, store in inbox, ack, cache) or the deadline passes
(exit 4). `owl ask --file <path> "<question>"` runs `git blame --line-porcelain` on the file,
aggregates author emails by line count, maps them through contact emails, and prints the top 3
as candidates (`--json` list; without `--json` an interactive numbered pick, or `--peer` to
skip). Exit codes per §9.

## Acceptance criteria
- [ ] AC1: integration test `tests/ask.rs::ask_accepted_writes_ask_record` runs two daemons (A, B) from `tests/common`, executes `owl ask` as A against B (policy manual), asserts stdout `accepted <id>`, exit 0, and `asks/<id>.json` in A's spool (modality: test)
- [ ] AC2: integration test `tests/ask.rs::cache_hit_skips_network` pre-populates A's cache for the question hash, points B's endpoint at a closed port, and asserts the answer is printed with exit 0 (modality: test)
- [ ] AC3: integration test `tests/ask.rs::offline_exits_2_and_queues_nothing` uses an endpoint on a closed port and asserts exit 2, stderr containing `offline`, and no `asks/` record (modality: test)
- [ ] AC4: integration test `tests/ask.rs::endpoints_are_tried_in_order` gives the contact a dead endpoint followed by the live one and asserts success (modality: test)
- [ ] AC5: integration test `tests/ask.rs::wait_returns_answer` places a signed answer in B's outbox addressed to A after 1 s; `owl ask --wait 10` prints the answer, exits 0, the answer is in A's inbox and cache, and B's outbox record is in `done/` (modality: test)
- [ ] AC6: integration test `tests/ask.rs::unavailable_and_rate_limited_exit_codes` asserts exit 2 for a 403 and exit 3 for a 429 (modality: test)
- [ ] AC7: integration test `tests/ask.rs::blame_candidates` creates a fixture repo with commits by two emails that match two contacts and one that matches none; `owl ask --file <f> "q" --json` lists the two contacts ordered by line share with their line counts (modality: test)

## Verification — task/OWL-007-ask

**Round 1 @68034a0 (2026-09-02):** gate green (`build.txt`, `clippy.txt`, `fmt.txt`, `test-workspace.txt`); mutant sweep `mutants.md` 30 mutants, 6 survived (M10, M21, M23, M24, M25, M26) → AC1, AC2, AC5, AC7 FAIL.

**Round 2 @0672ff3 (final, cap 2):** implementer changed only `tests/ask.rs` (scripted fake peer over pinned mTLS, 4 new tests) and a unit fixture in `src/cli/ask.rs`; no production code. Gate green: `r2-build.txt`, `r2-clippy.txt`, `r2-fmt.txt`, `r2-test-workspace.txt` all exit 0; second full-parallel run `r2b-test-workspace-2.txt` exit 0. Mutant sweep `r2-mutants.md` (raw `r2-mutants-log.txt`): 17 mutants, 13 killed (all six round-1 survivors dead), 1 equivalent (M10a), 3 survived (N2, N4, N10). test-planner `r2b-test-planner.md`: 58 rows, 23 uncovered. test-reviewer `r2b-test-reviewer.md`: GAPS (4). Fake peer requires A's pinned client cert (`tests/ask.rs:282-285` → `src/tls.rs:105-118`, `client_auth_mandatory` = true), so fake-peer tests are not vacuous. Hygiene `r2b-hygiene.txt`: `notify` fixture N/A (field unread on this branch), suite green twice, no stray `owl` processes.

| AC | Test (exists, drives `owl`, asserts criterion) | R1 | R2 | Evidence / gap |
|---|---|---|---|---|
| AC1 accepted → `accepted <id>`, exit 0, `asks/<id>.json` | `tests/ask.rs::ask_accepted_writes_ask_record` ✔ | FAIL | **PASS** | M10b/M26 killed; M10a equivalent (immutable `Value` index → Null, no panic; bad-body sweep passes under it) |
| AC2 cache hit skips network | `tests/ask.rs::cache_hit_skips_network` ✔ (dead port proven dead in-test) | FAIL | **FAIL** | M23 killed. N4 survived: `cache_lookup` `Err` arm (`src/cli/ask.rs:204-209`, unreadable record → warn+miss) has no test; lessons rule "every fallback/error arm gets a test". Planner #9, reviewer gap 4 |
| AC3 offline → exit 2, `offline`, no record | `tests/ask.rs::offline_exits_2_and_queues_nothing` ✔ (+ no-endpoints and unknown-peer twins) | PASS | **PASS** | — |
| AC4 endpoints tried in order | `tests/ask.rs::endpoints_are_tried_in_order` ✔ (both orders) | PASS | **PASS** | — |
| AC5 `--wait` answer → stdout, exit 0, inbox, cache, B `done/` | `tests/ask.rs::wait_returns_answer` ✔ (all five asserted, `:712-757`) | FAIL | **PASS** | M24/M25/N1/N3/N8/N9 killed. Nit (non-blocking): N2 survived — `wait_keeps_polling_through_transient_outbox_errors` fails the fake for 1.5 s so only one poll fails and warn-once is indistinguishable from warn-per-poll; raise `outbox_fails_until` ≥ 3.5 s |
| AC6 403 → exit 2, 429 → exit 3 | `tests/ask.rs::unavailable_and_rate_limited_exit_codes` ✔ (policy never, responder off, rate 1/h) | PASS | **FAIL (gap)** | Criterion asserted; N5 killed. Reviewer gap 2 / planner #29: `RateLimited { retry_after_secs: None }` arm (`src/cli/ask.rs:177`) untested — daemon always sends Retry-After; fake `(429, ..)` sends none. Expect exit 3, plain `rate limited` |
| AC7 blame candidates ordered by line share | `tests/ask.rs::blame_candidates` ✔ (two matched, one unmatched, counts) | FAIL | **FAIL** | M21/N6 killed. N10 survived: fixture Ana=6/Bea=3 sorts identically by name (`tests/ask.rs:1130-1153`); unit `candidates_are_matched_ranked_and_capped` (Ana 4, Bea 4, Cat 1, Dan 1) has the same flaw. Ordering claim unproven; give the alphabetically later contact the larger count |

Non-AC gap (reviewer 3 / planner #33): 200 without `X-Owl-Signature` (`src/client.rs:137-142`) untested; fake `(200, body)` induces it.

**Verdict: FAIL** — AC2, AC6, AC7 (round cap reached).

### Round 3 @763be86 (2026-09-02, fresh run, round 1 of 3 on the re-opened task)

Branch verified as-is at 763be86 (base `main` moved to d565878 after OWL-012; not rebased). Diff
`0672ff3...763be86` touches only `tests/ask.rs` and the unit fixture in `src/cli/ask.rs`; no production
code. Gate green in a detached verify worktree: `r3-build.txt`, `r3-test-workspace.txt`, `r3-clippy.txt`,
`r3-fmt.txt` all rc=0 (20/20 `tests/ask.rs`). Mutant sweep `r3-mutants.md` (raw `r3-mutants-log.txt`,
run per binary, classified on rc / `error[E` only): 13 mutants, 11 killed — all three round-2
survivors (N10, N4, N2) plus N10b, N4b, R429, SIG, ASC, CAP, RAMSG, DENOM — 2 survived (QUIET:
`--quiet` also suppressing errors in `src/main.rs`; TIE: tie-break by fingerprint instead of name),
both outside the AC claims and unspecified by §9. test-planner `r3-test-planner.md`: 38 rows, 10
uncovered, none of them the round-2 named gaps. test-reviewer `r3-test-reviewer.md`: GAPS (2), both
nits — warn-once proof is timing-dependent (fake counts no 500s); unit tie fixture cannot separate
name from fingerprint/email order. Hygiene `r3-hygiene.txt`: suite green twice under full
parallelism, `pgrep -x owl` empty; `notify` fixture N/A (the field is only read in `src/config.rs`
on this branch, which predates OWL-012).

AC7 fixture key check (read `tests/ask.rs::blame_candidates`, `fixture_repo`, `prepare_home`,
`src/contacts/mod.rs:94-103`): expected `[Zed, Ana]` differs from name ascending (Ana < Zed), commit
order (Ana's commit is first, so blame emits Ana's lines first), contact-book order (files are
`<fingerprint>.json`, `load_dir` sorts paths, in-test `fp(ana) < fp(zed)` asserted), insertion order
of `prepare_home` (Ana, Zed) and `by_email` key order (`ana@` < `zed@`). It cannot separate the
descending twins of those keys (a two-element list never can); the unit fixture `[Bea, Zed, Ana]` with
tail `[Cat, Dan]` does (name-desc `[Zed, Dan, Cat]`, reverse-contact `[Zed, Dan, Ana]`, lines-asc
`[Cat, Dan, Ana]`), and mutants N10, N10b and ASC all die in both `--bin owl` and `--test ask`.

| AC | Test (exists, drives `owl`, asserts criterion) | R2 | R3 | Evidence / gap |
|---|---|---|---|---|
| AC1 accepted → `accepted <id>`, exit 0, `asks/<id>.json` | `ask_accepted_writes_ask_record` ✔ | PASS | **PASS** | carried from 0672ff3; named test passes in `r3-test-workspace.txt` |
| AC2 cache hit skips network | `cache_hit_skips_network` ✔ + `unreadable_cache_entries_fall_through` (dir / non-record bytes → `ignoring unreadable cache entry`, exit 2 offline, `--quiet` twin) | FAIL | **PASS** | N4 and N4b killed; the spool `Err` arm is reached in-test (`cache_get().is_err()` guard) |
| AC3 offline → exit 2, `offline`, no record | `offline_exits_2_and_queues_nothing` ✔ | PASS | **PASS** | carried; passes in this run |
| AC4 endpoints tried in order | `endpoints_are_tried_in_order` ✔ | PASS | **PASS** | carried; passes in this run |
| AC5 `--wait` answer → stdout, exit 0, inbox, cache, B `done/` | `wait_returns_answer` ✔ | PASS | **PASS** | carried; N2 now killed (3.5 s outage). Nit: proof is load-dependent, see reviewer gap 1 |
| AC6 403 → exit 2, 429 → exit 3 | `unavailable_and_rate_limited_exit_codes` ✔ + `rate_limited_429_with_and_without_retry_after` (no header / 42 / 0 / HTTP-date, exact stderr, spool empty) | FAIL (gap) | **PASS** | R429 and RAMSG killed; `RateLimited { None }` arm exercised via scripted `accept_headers` |
| AC7 blame candidates ordered by line share | `blame_candidates` ✔ (Ana 3 first / Zed 6 / unmatched 11 → `[Zed, Ana]`, lines, shares, fingerprints, per-line stderr numbering, `len == 2`) | FAIL | **PASS** | N10, N10b, ASC, CAP, DENOM killed; fixture differs from every ascending key (above) |

Non-AC nits (non-blocking, for the PR or a follow-up): `--quiet` error-line behaviour unpinned
(QUIET survivor; add `err.contains("offline")` to the `--quiet` rows); tie-break fixture (TIE
survivor); warn-once count not asserted in the fake; planner #21 (unreadable cache directory + live
answer → `cache_put` onto a directory) is a real production edge the spec is silent on.

**Verdict: PASS** — AC1–AC7 all PASS at 763be86. FAILING: none.

## Escalation — round cap reached (2026-09-02, rounds=2)
Round 2 (0672ff3, tests only, no production change) killed all six round-1 survivors and AC1/AC5
now PASS. Still FAILING, all test gaps (no code defect found in either round):
- AC7: `blame_candidates` fixture (Ana 6 / Bea 3) sorts identically by name and by line share, so
  dropping the share sort key (mutant N10) survives; the unit fixture has the same flaw.
- AC2: `cache_lookup` Err arm (`src/cli/ask.rs:204-209`, unreadable cache record → warn + miss)
  has no test (mutant N4 survives).
- AC6: `RateLimited{retry_after_secs: None}` arm (`ask.rs:177`) untested; the fake peer's 429
  never omits Retry-After.
- Nits: N2 warn-once needs `outbox_fails_until >= 3.5 s`; 200 without `X-Owl-Signature` untested
  (`src/client.rs:137-142`).
Fix shape for the next attempt: fixture where share order differs from name order (e.g. Zed 6 /
Ana 3); one scripted-fake row per client response-shape arm; a corrupt (unreadable) cache file
test. Status set to `blocked`; `orch-state reset-stale` returns it to `todo`.

PR: https://github.com/Krab00/owlpost/pull/11 (draft, branch merged with main at 19b5371, 212 tests green)
