---
id: OWL-028
title: "Plugin: one-pick 'Draft & send' in the inbox flow"
status: done
deps: [OWL-023]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
User request 2026-09-06: answering a question today takes two pickers — Draft on the
`pending` record, then Send on the `drafted` one. The user wants to decide once.

Add a **Draft & send** pick; keep Draft (two-step) for when the human wants to read first.

- **`commands/inbox.md` step 3** picker becomes **Draft / Draft & send / Reject / Skip**.
  `Draft & send`: run `owl draft <id>`, print the draft verbatim in a code block (as step 4
  does), then run `owl send <id>` at once, without a second picker. The pick is the human's
  explicit approval to send whatever the harness produced; say so in the option description
  (`sends the draft as-is; pick Draft to read it first`). On a non-zero `owl draft` exit show
  the error and stop (nothing is sent); on a non-zero `owl send` exit show the error, the
  record stays `drafted`.
- **`commands/draft.md`**: after showing the draft, the offer includes `/owlpost:send <id>`
  as today; additionally accept `--send` in its arguments (`/owlpost:draft <id> --send`) which
  does draft, print, send in one go, same rules. `--send` is stripped before `owl draft`
  is run (owl has no such flag).
- **Ground-rule wording.** SKILL.md "The answer loop" step 6 and the sentence `Never chain
  \`owl draft\` and \`owl send\` in one step` (skill, inbox.md, draft.md) become: `Never
  chain owl draft and owl send unless the human picked "Draft & send" (or passed --send);
  the draft is still printed in full before owl send runs.` The `consent` picker is
  unchanged (allow ≠ answer).
- Tests: `tests/plugin.rs` pins the new picker option name in inbox.md, the `--send`
  argument in draft.md, and the amended ground-rule sentence in all three files (stated
  three times → pinned three times); asserts the old absolute sentence is gone.

Out of scope: an "Allow & draft & send" pick on consent records; auto policy (that is
`owl allow --always`).

## Acceptance criteria
- [ ] AC1: `commands/inbox.md` step 3 lists the four picks `Draft / Draft & send / Reject / Skip`, the `Draft & send` description contains `sends the draft as-is; pick Draft to read it first`, and the step says the draft is printed verbatim before `owl send`; pinned in `tests/plugin.rs` (modality: test)
- [ ] AC2: `commands/draft.md` documents `--send` (`/owlpost:draft <id> --send`), says it is stripped before `owl draft`, and that the draft is printed before `owl send`; pinned in `tests/plugin.rs` (modality: test)
- [ ] AC3: the sentence `Never chain owl draft and owl send unless the human picked "Draft & send"` (backticks allowed around commands) is present in SKILL.md, inbox.md and draft.md, and the old `Never chain draft and send in one step` / `Never chain \`owl draft\` and \`owl send\` in one step` is absent from all three; pinned in `tests/plugin.rs` (modality: test)
- [ ] AC4: README answer-loop paragraph mentions `Draft & send`; pinned in `tests/docs.rs` (modality: test)
- [ ] AC5: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-028-draft-and-send

Verify worktree at dba98ff (base 2929785), `CARGO_TARGET_DIR` shared. Evidence in `OWL-028.evidence/` (fmt.log, build.log, clippy.log, test.log, gate-exit.log, mutants.log).

| AC | Verdict | Evidence |
|---|---|---|
| AC1 inbox.md step 3: four picks, description literal, draft printed before `owl send` | PASS | inbox.md:56 `Draft / Draft & send / Reject / Skip`; :60-65 option with `sends the draft as-is; pick Draft to read it first`, draft → print → send order, both error branches. Pinned by `inbox_offers_draft_and_send_in_one_pick` + `inbox_command_walks_states_with_pickers`; mutants M1–M3, M13 RED |
| AC2 draft.md: `--send`, stripped before `owl draft`, printed before `owl send` | PASS | draft.md:3 argument-hint `[--send]`, :4 allowed-tools `Bash(owl draft:*), Bash(owl send:*)` (exact sorted equality), :16-20 body. Pinned by `draft_command_accepts_send_flag`; mutants M4–M6, M15 RED |
| AC3 amended never-chain sentence in SKILL.md, inbox.md, draft.md; old forms absent | PASS | Whole-line equality in all three (SKILL.md:158, inbox.md:87, draft.md:22); both old forms asserted absent in the three files + technical-design.md. Pinned by `never_chain_rule_is_amended_everywhere`; mutants M7–M10, M12, M14, M16 RED. Design doc / architecture.md / concept.md checked: no "human reads before send" sentence contradicts the one-pick path (architecture.md:130 "printed for preview; the human runs `owl send`" and :162 "preview before send in manual mode" stay true — the draft is printed before `owl send`). See PM-REVIEW note below. |
| AC4 README intro names `Draft & send` | PASS | README.md:9; pinned by `readme_answer_paragraph_names_draft_and_send` (pre-Quickstart slice, one-line literal); mutant M11 RED |
| AC5 build / test / clippy / fmt, no new crate | PASS | gate-exit.log: fmt 0, build 0, clippy 0, test 0 (all binaries green incl. notify, 22/22 plugin, 5/5 docs); Cargo.toml/Cargo.lock untouched |

PM-REVIEW (not an AC failure): inbox.md:76-77 step-4 `Send` option still says "This is the only pick that runs `owl send`". True inside the step-4 Send/Edit/Reject picker, but it is a second, unamended copy of the "send only on Send" rule now that step 3 `Draft & send` also runs `owl send`. Suggest "the only pick in this picker that runs `owl send`" or drop the sentence.

FAILING: none.

test-reviewer: OK. test-planner: 15 rows, 13 covered by named tests, 2 uncovered (plugin README draft-row command cell `[--send]`, draft.md plain-draft offer retained) — both confirmed present by diff review.

Mutants (own sweep, each recompiled from the verify worktree; the shared target dir had been overwritten by a sibling worktree's `plugin` binary between runs, first pass discarded):

| Mutant | Kill |
|---|---|
| M1 inbox picker reverted to `Draft / Reject / Skip` | RED `inbox_offers_draft_and_send_in_one_pick`, `inbox_command_walks_states_with_pickers` |
| M2 description literal `read it first` → `review it first` | RED `inbox_offers_draft_and_send_in_one_pick` |
| M3 "print the draft verbatim" removed from Draft & send option | RED `inbox_offers_draft_and_send_in_one_pick` |
| M4 draft.md allowed-tools without `Bash(owl send:*)` | RED `draft_command_accepts_send_flag` |
| M5 draft.md argument-hint without `[--send]` | RED `draft_command_accepts_send_flag` |
| M6 draft.md strip-before-`owl draft` sentence removed | RED `draft_command_accepts_send_flag` |
| M7 SKILL.md old `in one step` sentence restored | RED `never_chain_rule_is_amended_everywhere` |
| M8 inbox.md amended sentence wrapped onto two lines | RED `never_chain_rule_is_amended_everywhere` |
| M9 draft.md amended sentence without `(or passed --send)` | RED `never_chain_rule_is_amended_everywhere` |
| M10 SKILL.md step 6 without `/owlpost:draft <id> --send` | RED `never_chain_rule_is_amended_everywhere` |
| M11 README intro reverted | RED `readme_answer_paragraph_names_draft_and_send` |
| M12 plugin README inbox row reverted | RED `never_chain_rule_is_amended_everywhere` |
| M13 inbox.md preamble back to `"Send" pick` only | RED `inbox_command_walks_states_with_pickers` |
| M14 SKILL.md "Never send a draft the human has not seen in full." removed | RED `never_chain_rule_is_amended_everywhere` |
| M15 draft.md `(nothing is sent)` removed | RED `draft_command_accepts_send_flag` |
| M16 old sentence `Never chain draft and send in one step.` added to inbox.md | RED `never_chain_rule_is_amended_everywhere` |

Environmental: `hook_script_forwards_session_start_and_reads_plugin_json` failed during the mutant runs only because the shared `target/debug/owl` had been rebuilt by the OWL-026 worktree (new arm sentence); it passed in the gate run with this branch's binary.

Draft PR: https://github.com/Krab00/owlpost/pull/32 (round 2 at 3cf95a6: step-4 sentence scoped to its picker, M10 RED)
