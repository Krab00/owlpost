---
id: OWL-013
title: Claude Code plugin — hooks, skill, slash commands, memory-write guidance
status: done
deps: [OWL-010]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Create `plugins/claude-code/` per `docs/concept.md` (Harness adapters) and
`docs/architecture.md §3.3, §3.6`. `.claude-plugin/plugin.json` (name `owlpost`, version from
Cargo). `hooks/hooks.json`: `SessionStart`, `UserPromptSubmit` and `PostToolUse` each run
`owl inbox --count --format claude` (timeout 5 s; the hook must be a no-op when `owl` is not on
PATH — wrap in a tiny shell script that exits 0 silently). `skills/owlpost/SKILL.md`: when to
ask a peer (the user names a colleague, or asks about code whose blame points at a contact),
how to run `owl ask` (prefer `--file` candidates, always show the human the chosen peer before
sending), how to react to the injected counter (offer to show the inbox, never dump content
unasked), the answer loop (`owl inbox` → `owl show` → `owl draft` → show the draft verbatim →
`owl send`/`owl edit`/`owl reject` only on explicit human approval), and the memory rule: when
the human accepts an answer, save it to memory with provenance `owlpost:<peer>:<question id>`
and the date. Slash commands `commands/inbox.md` (`/owlpost:inbox` → list and offer actions),
`commands/ask.md` (`/owlpost:ask <peer> <path> <question>`), `commands/history.md`. A README in
the plugin dir with install steps (`/plugin marketplace add` + `/plugin install`) and the
requirement that `owl daemon` is installed.

## Acceptance criteria
- [ ] AC1: `plugins/claude-code/.claude-plugin/plugin.json` and `hooks/hooks.json` parse as JSON; hooks.json registers SessionStart, UserPromptSubmit and PostToolUse, each invoking `hooks/owl-count.sh` (modality: data)
- [ ] AC2: integration test `tests/plugin.rs::hook_script_emits_context_or_nothing` runs `hooks/owl-count.sh` with `PATH` including a built `owl` and a home with 2 unseen questions and asserts stdout is the exact claude injection JSON; with 0 unseen asserts empty stdout and exit 0; with `owl` removed from PATH asserts empty stdout and exit 0 (modality: test)
- [ ] AC3: `skills/owlpost/SKILL.md` has frontmatter `name: owlpost` and a `description` and contains the strings `owl ask`, `owl inbox`, `owl draft`, `owl send`, `owlpost:<peer>` (memory provenance) and the sentence that sending requires explicit human approval (modality: data)
- [ ] AC4: `commands/inbox.md`, `commands/ask.md`, `commands/history.md` exist with frontmatter `description` (modality: data)
- [ ] AC5: a manual smoke run documented in `plugins/claude-code/README.md`: install the plugin from the local path in Claude Code, start a session with an unseen question, observe the counter line — recorded as PM-REVIEW with the observed output pasted into the task file (modality: ui)

## Verification — task/OWL-013-claude-plugin

Round 1, 2026-09-03, commit `ea0c979` (base `main` c27126b), worktree `.worktrees/OWL-013`.
Evidence dir: `.orch/tasks/OWL-013.evidence/` (`gate.txt`, `hook-states.txt`, `hook-states-2.txt`, `inline-checks.txt`, `mutants.txt`).

Gate: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` — all rc=0 (`gate.txt`).
`cargo test --test plugin`: 7 passed, 0 failed, 1 ignored (smoke seed helper). Also passes with a stray `OWLPOST_HOME` and empty `HOME` exported; neither directory touched (`inline-checks.txt`).

| AC | Verdict | Evidence |
|---|---|---|
| AC1 plugin.json + hooks.json parse; 3 events → `hooks/owl-count.sh` | PASS | `jq`/python parse all three JSON files; `jq` shows SessionStart, UserPromptSubmit, PostToolUse each → `${CLAUDE_PLUGIN_ROOT}/hooks/owl-count.sh`, timeout 5 (`inline-checks.txt`, `hook-states.txt`). Script mode 100755 in the index, `sh -n` + `dash -n` clean. plugin.json version 0.1.0 == Cargo.toml. Test `manifests_parse_and_register_hooks` + `plugin_version_matches_cargo`; mutants M6–M13 killed. |
| AC2 `hook_script_emits_context_or_nothing`: exact JSON / silent / no owl | PASS | Test passes. Verifier re-ran the script by hand (`hook-states*.txt`): 2 unseen → stdout byte-exact to §9 line (`BYTE-EXACT match: yes`), rc 0, empty stderr; second call identical (never marks seen); 1 seen + 1 unseen → singular "1 new question (Maciek 1)"; 0 unseen → 0 bytes rc 0; owl absent from PATH → silent rc 0; owl failing (home is a file, rc 1 with stderr; fake owl rc 2) → silent rc 0. Also: direct exec via shebang, hook JSON on stdin and an open stdin pipe (no hang), and `$HOME/.config/owlpost` fallback with no `OWLPOST_HOME` all produce the same line. Mutants M1, M2, M4, M5 killed. |
| AC3 SKILL.md frontmatter + literals + approval sentence | PASS | `name: owlpost`, `description:` present; grep counts: `owl ask` 8, `owl inbox` 5, `owl draft` 3, `owl send` 5, `owlpost:<peer>` 1, approval sentence 1 (`inline-checks.txt`). Every flag the skill and commands mention exists in `owl <cmd> --help` (11 flags, 15 subcommands, `contact show`, `install --dry-run`); the "exit 4 on wait timeout" claim matches `src/cli/ask.rs:37`. Mutants M15–M17 killed. |
| AC4 three command files with `description` frontmatter | PASS | inbox/ask/history each have a non-empty `description:` line; mutants M18–M20 killed. |
| AC5 manual smoke run in Claude Code | PM-REVIEW | `README.md` documents the smoke steps (install from local path via `/plugin marketplace add` + `/plugin install owlpost@owlpost-local`, seed helper, `owl daemon` requirement) and has an "Observed output" paste block. Not runnable here (no `web`/`api` capability; human session required). Observed output to be pasted below by the PM. |

test-reviewer: **OK** (no gaps; notes M3 equivalence independently).
test-planner matrix: 27 rows; automatable NONE rows (2, 9, 10, 11, 12, 13, 17, 20–25) executed by the verifier inline as listed above; rows 14, 26, 27 are the AC5 human session.

Mutant sweep (20 mutants, `mutants.txt`): 18 killed, 2 survived.
- M3 drop `command -v owl` guard — **equivalent mutant**: with the guard gone, `owl … 2>/dev/null || exit 0` still yields empty stdout/stderr and rc 0 for a missing `owl` under sh, dash and bash (verified by hand). Not a test gap; the guard is defence in depth.
- M14 marketplace.json `name` `owlpost-local` → other — survives; no AC pins the marketplace name, but the README install line `owlpost@owlpost-local` depends on it. Advisory only (non-blocking): a one-line assertion in `manifests_parse_and_register_hooks` would pin it.

FAILING: none

### AC5 observed output (PM to paste)

```
<paste hook line / transcript excerpt here, with date and Claude Code version>
```

PR: https://github.com/Krab00/owlpost/pull/13 (draft)
