---
id: OWL-031
title: "Plugin: event-driven inbox watch — SessionStart watchPaths + FileChanged asyncRewake hook, no Monitor"
status: done
deps: [OWL-029, OWL-030]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
Finding from 2026-09-06 (fifth recurrence of "the watch is not running", after OWL-023/026/029/030):
the user opened a fresh session and saw no watch. Root cause is structural, not a bug: the watch is a
`Monitor` the **model** arms, and Claude Code never runs a model turn on `SessionStart` — the hook can only
inject text, so nothing happens until the first prompt. Two more costs of that design: the `--follow` loop
polls every 5 s, and its pid markers under `$OWLPOST_HOME/watch/` go stale when a session ends or `TaskStop`
kills the follow (three dead markers on the dev machine today; the hook only sweeps the current session id).

Replacement, proven on claude 2.1.263 in both `claude -p --input-format stream-json` and a real interactive
REPL driven through a pty (fixtures: session scratchpad `fcw/{settings.json,session-start.sh,file-changed.sh,pty_test.py}`):

1. A `SessionStart` hook's JSON may carry `hookSpecificOutput.watchPaths: ["<absolute path>"]` (in the
   binary: "Absolute paths to watch for FileChanged hooks"; not in the docs). Claude Code watches those
   paths with chokidar (`ignoreInitial: true`, `awaitWriteFinish` 500 ms); directories work and yield
   `add` / `change` / `unlink`.
2. A `FileChanged` command hook with `"asyncRewake": true` receives
   `{session_id, transcript_path, cwd, hook_event_name: "FileChanged", file_path, event}` on stdin. When it
   exits **2**, Claude Code wakes the idle session with a new model turn and shows the hook's stderr (stdout
   if stderr is empty) as a system reminder; any other exit code is silent. The interactive session woke in
   the same second the file was added. `async`/`asyncRewake` are ignored on `SessionStart`, `Stop` and
   `UserPromptSubmit` (documented), so the chain must go through `FileChanged`.
3. The wake runs the `UserPromptSubmit` hooks too, and the TUI labels it "Stop hook feedback" (the binary
   has `@internal` `rewakeMessage`/`rewakeSummary`; not part of this task).

So the plugin stops asking the model to arm anything. The hook registers the spool inbox directory on
session start and a second hook wakes the session when a record is added. The design doc §9, §11 and §12
are updated in the same PR; where this file and the design doc disagree, the design doc wins and this file
is corrected.

### A. `owl inbox --count --format claude` — SessionStart registers the watch
- `--hook-event SessionStart` **always** prints one JSON line (also at count 0) whose `hookSpecificOutput`
  carries `hookEventName: "SessionStart"`, `watchPaths: ["<home>/spool/inbox"]` (absolute; `<home>` is the
  resolved `$OWLPOST_HOME`, canonicalised when it exists) and `additionalContext` only when there is text
  (counter sentence + previews + `OPEN_SENTENCE` as today; the key is omitted when empty). When
  `plugin.json` says `{"watch": false}` there is no `watchPaths` key, and at count 0 nothing is printed
  (today's behaviour).
- The arm sentence (`ARM_PREFIX`, `arm_sentence`, `WATCH_COMMAND`'s role as a template) is removed from the
  Claude injection on every event; `UserPromptSubmit` and `PostToolUse` keep today's shape (counter only,
  nothing at 0). The live-marker check in the hook path goes away with it.
- `--hook-event SessionStart` sweeps `$OWLPOST_HOME/watch/`: every marker whose pid is dead is removed
  (all of them, not only the current session's). The sweep never fails the hook.

### B. `--hook-event FileChanged` — the wake
- Reads the hook stdin JSON (same reader as today; a terminal, empty or unparseable stdin, or a missing
  `event` → exit 0, print nothing). Never marks anything seen.
- `event == "add"` and unseen count > 0 and the watch is enabled (`plugin.json` not `{"watch": false}`)
  → prints the counter sentence (`sentence()`, 🦉 icon, same text as `--follow`) to **stderr** and exits
  **2**. Everything else (`change`, `unlink`, count 0, watch off) → exit 0, nothing printed.
  Design note: keying on `add` is the de-duplication — marking seen and moving to `done/` are `change`/
  `unlink`, so they never wake; two records arriving in a burst wake twice, which is acceptable.
- `--format claude` is required (the other formats have no such hook; `--hook-event FileChanged` with
  another format is a usage error, exit 2 from clap — distinct from the wake's exit 2 by its stderr text).

### C. Plugin files
- `hooks.json`: add
  `"FileChanged": [{"hooks": [{"type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/owl-count.sh --hook-event FileChanged", "asyncRewake": true, "timeout": 5}]}]`
  (the `${CLAUDE_PLUGIN_ROOT}` placeholder stays literal in the file). `SessionStart`, `UserPromptSubmit`,
  `PostToolUse` entries unchanged. `owl-count.sh` unchanged (it already passes `"$@"` and stdin through and
  is a strict no-op on failure) — but note its `2>/dev/null`: the wake's sentence goes to stderr, so the
  script must let stderr through on `FileChanged` (or the sentence goes to stdout there and the hook
  falls back to stdout — implementer's choice; the test pins whichever is chosen through the real script).
- `commands/watch.md`: `on` writes `{"watch": true}` and says
  `owlpost watch: on; the inbox is watched from the next session start`; `off` writes `{"watch": false}`
  and says `owlpost watch: off; this session stops waking now, new sessions do not watch`; `status` reads
  `plugin.json` and says `owlpost watch: default on|off; event-driven (FileChanged), nothing to arm`. No
  `Monitor`, `TaskStop`, marker or "arms on your next prompt" anywhere in the file. `allowed-tools` drops
  `Monitor` and `TaskStop`.
- `skills/owlpost/SKILL.md` "Live watch": one paragraph — the hook wakes the session with the counter
  sentence when a record arrives; report it in one line and offer `/owlpost:inbox`; nothing to arm; no
  sentence starting `owlpost: before handling this prompt` exists any more. README row for
  `/owlpost:watch` rewritten accordingly.
- `docs/technical-design.md` §9 (hook injection bullets + the `owl inbox` row), §11 and §12: describe
  `watchPaths` + `FileChanged` + `asyncRewake` + exit 2, the `add`-only rule, the dead-marker sweep; the
  marker/per-turn re-arm description and `scripts/e2e-watch-arm.sh` rows are replaced.

### D. What stays
- `owl inbox --count --follow [--session <id>]` stays as is (a poll loop any harness can run; the marker
  it writes is now only informational) — one `// ponytail:` line names it as the fallback for hosts
  without `FileChanged`. Nothing in the Claude plugin references it any more.

### E. Proof on the real harness: `scripts/e2e-watch-wake.sh` (replaces `e2e-watch-arm.sh`)
1. temp `OWLPOST_HOME` (`owl init`, no daemon), `owl` from `PATH` (led by the branch build by the
   verifier), `E2E_PLUGIN_DIR` → `--plugin-dir` (default: installed plugin);
2. `claude -p --input-format stream-json --output-format stream-json --verbose --include-hook-events
   --permission-mode bypassPermissions` in the background with stdin held open: sends one user message
   (`Reply with exactly the word: ready`), waits for the `result` event, then drops one unseen question
   record into `$OWLPOST_HOME/spool/inbox/` (a fixture built the way `tests/inbox.rs` builds records);
3. within 60 s asserts a second `assistant` event appears with **no** second user message sent, and that a
   `system` `hook_response` for `FileChanged` with `exit_code: 2` and the 🦉 sentence precedes it;
4. prints `PASS`/`FAIL <reason>`, keeps the stream under `E2E_OUT`, exits non-zero on FAIL, bounded by
   `timeout 180`. Cost: one `claude -p` call.
The interactive (pty) proof stays manual (AC8, orchestrator, post-merge).

Out of scope: `rewakeMessage`/`rewakeSummary`; Codex/Kimi; OS notifications; watching anything but
`spool/inbox`.

## Acceptance criteria
- [x] AC1: `owl inbox --count --format claude --hook-event SessionStart` with hook stdin `{"session_id":"S"}` prints one valid-JSON line at count 0 and at count N>0 whose `hookSpecificOutput.watchPaths` equals `["<home>/spool/inbox"]` (absolute, the test's temp home), `hookEventName` is `SessionStart`, `additionalContext` is absent at 0 and holds the counter sentence + previews + `owlpost: run /owlpost:inbox now.` at N>0; with `plugin.json` `{"watch": false}` the `watchPaths` key is absent and count 0 prints nothing; `UserPromptSubmit` and `PostToolUse` never carry `watchPaths`; no output on any event contains `owlpost: before handling this prompt` (modality: test)
- [x] AC2: `owl inbox --count --format claude --hook-event FileChanged` with stdin `{"session_id":"S","file_path":"…","event":"add"}` and N>0 unseen records exits 2, prints the 🦉 counter sentence (byte-identical to `sentence()`) on stderr and nothing on stdout, and marks nothing seen (the count is unchanged afterwards); the same with `event` `change` or `unlink`, or with count 0, or with `plugin.json` `{"watch": false}`, exits 0 and prints nothing; empty, unparseable or terminal stdin, or stdin without `event`, exits 0 and prints nothing; `--hook-event FileChanged` with `--format plain|codex|kimi` is a clap usage error (modality: test)
- [x] AC3: on `--hook-event SessionStart` every marker in `$OWLPOST_HOME/watch/` whose pid is dead is removed and a marker whose pid is alive (a spawned `sleep`) is kept, regardless of session id; an unreadable/absent `watch/` dir does not fail the hook (modality: test)
- [x] AC4: `tests/plugin.rs` runs the real `hooks/owl-count.sh` through `env -i /bin/sh` with Claude Code's stdin JSON on all four events and parses stdout with serde_json: `SessionStart` carries `watchPaths`; `FileChanged` with an `add` and N>0 exits 2 with the sentence on the stream the implementer chose (stderr or stdout, pinned), and exits 0 silently for `change`; `hooks.json` parses as JSON and its `FileChanged` entry has `asyncRewake: true`, command ending in `owl-count.sh --hook-event FileChanged`, timeout 5, and the other three entries are unchanged (modality: test)
- [x] AC5: no file under `plugins/claude-code/`, `docs/`, `README.md` contains `owlpost: before handling this prompt`, `arm the inbox watch`, `it arms on your next prompt`, `Monitor` (case-sensitive, in the plugin dir) or `e2e-watch-arm`; `commands/watch.md` contains the three exact status lines of part C and its `allowed-tools` has no `Monitor`/`TaskStop`; SKILL.md "Live watch" and the README `/owlpost:watch` row contain `nothing to arm`; design doc §9 contains `watchPaths`, `FileChanged`, `asyncRewake` and `exit 2`, §12 names `scripts/e2e-watch-wake.sh` — each pinned by a single-line `contains()` test (modality: test)
- [x] AC6: `scripts/e2e-watch-wake.sh` is executable, uses `--input-format stream-json`, drops the record only after the first `result` event, asserts the `FileChanged` `hook_response` with `exit_code` 2 and a second `assistant` event, prints `PASS`/`FAIL` (pinned in `tests/docs.rs` like the old script); `scripts/e2e-watch-arm.sh` is deleted (modality: test)
- [x] AC7: `scripts/e2e-watch-wake.sh` run by the verifier with `E2E_PLUGIN_DIR=plugins/claude-code` and `PATH` led by the branch's `target/release` prints `PASS`; the stream file and the script output are attached as evidence (modality: manual)
- [ ] AC8: after the merge, on this machine: `owl update --source .` prints `installed /home/kris/.local/bin/owl`; `owl inbox --count --format claude --hook-event SessionStart <<< '{"session_id":"x"}'` prints a line with `watchPaths`; the installed plugin's `hooks.json` has the `FileChanged` entry; `scripts/e2e-watch-wake.sh` with no overrides prints `PASS`; a fresh interactive `claude` session in this repo wakes by itself when a question arrives (checked by the user, not by a script) — run by the orchestrator/user, output kept in this file (modality: manual)
- [x] AC9: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-031-watch-wake

Round 1, 2026-09-06. Verified head `4b714d8` (src/plugin/docs/script) and `d47cd2f` (tests/docs.rs only, +44/-9; `git diff 4b714d8 d47cd2f --stat` = 1 file). Own detached worktree `.worktrees/verify-OWL-031` with private `CARGO_TARGET_DIR`, removed afterwards. Evidence: `.orch/tasks/OWL-031.evidence/` (paths below are relative to it). `cargo test -- --list` named every new test before any run (`gates.log`: 31 watch/sweep/file_changed/hook_script/manifests tests listed).

| AC | Verdict | Evidence |
|---|---|---|
| AC1 SessionStart `watchPaths` / additionalContext / watch false / UPS+PTU never / no arm text | PASS | tests `session_start_registers_the_watch_path_unless_plugin_json_says_off`, `hook_emits_watch_paths_and_sweeps_markers_per_event_marker_plugin_json_and_stdin`, `watch_path_follows_the_home_and_is_canonical`, unit `watch_path_is_claude_only_and_stands_alone_at_zero`; own matrix `matrix.log` rows A.* (event × plugin.json {absent,true,false,garbage,empty} × count {0,2}, stdin ignored, symlinked/relative home canonical, other formats) all PASS (one row `A.SS.false.0.noWatchPaths` was a quoting bug in my checker, corrected in `matrix-rerun.log`: rc 0, empty); mutants M01–M05 KILLED (`mutants.log`) |
| AC2 FileChanged wake: add+N>0+watch on → stderr sentence, exit 2, stdout empty, nothing seen; change/unlink/0/off/bad stdin → exit 0 silent; clap guard | PASS | tests `file_changed_wakes_on_add_with_unseen_records_only` (63-cell grid), `file_changed_requires_the_claude_format`, unit `file_changed_event_reads_the_event_string_only`; matrix rows C.FC.* (12 stdin shapes × 5 plugin.json × 2 counts) + C32 all-seen, C37 real tty via `script(1)` (rc 0 silent), C38 unparseable plugin.json = on, C39 answer record (`1 new answer`, byte-identical to `--format plain`, `matrix-rerun.log`), C42–C43 clap usage error for plain/codex/kimi/none/no --format/no --count (exit 2, `Usage:`, no 🦉, nothing seen); mutants M06–M12, M17–M19 KILLED |
| AC3 dead-marker sweep on SessionStart, any id; live kept; absent/unreadable dir OK | PASS | tests `session_start_sweeps_every_dead_marker_and_keeps_live_ones`, `a_killed_follow_leaves_a_stale_marker_the_session_start_sweep_removes`, unit `sweep_markers_removes_dead_markers_of_any_session_and_keeps_live_ones`; matrix B.* (reaped pid, garbage, empty, `sleep 60` live, own pid, dir entry kept, `watch` as file, chmod 000 dir, no sweep on UPS/PTU/FileChanged, sweep before format dispatch, real killed `--follow` marker) all PASS; mutants M13–M16 KILLED |
| AC4 real `owl-count.sh` via `env -i /bin/sh` on 4 events, serde_json parse, stream pinned; hooks.json entry | PASS | tests `hook_script_via_env_i_registers_the_watch_and_wakes_on_add`, `hook_script_forwards_session_start_and_reads_plugin_json`, `manifests_parse_and_register_hooks`; **stream chosen: stderr** (script captures `2>&1 >/dev/null`, re-emits on stderr, exit 2 only for `FileChanged`); matrix D.* (script on all events × watch × count, add/change/unlink, bad stdin, owl missing, owl failing → silent) and E.* (jq: FileChanged entry exact, asyncRewake true, timeout 5, command suffix, literal `${CLAUDE_PLUGIN_ROOT}`, other three entries byte-equal to `main`, no removed lines) all PASS; mutants H01–H06, S01–S06 KILLED |
| AC5 retired phrases absent; three watch.md lines; `nothing to arm` ×2; §9 four literals; §12 script name; allowed-tools | PASS | tests `watch_docs_describe_the_event_driven_wake_and_drop_the_arm_sentence`, `watch_command_is_a_plugin_json_toggle`; matrix F.* greps PASS; mutants D01–D18 KILLED. D19 (`Monitor` appended to `docs/concept.md`) SURVIVED as expected: AC5 scopes the `Monitor` check to the plugin dir; `docs/plan.md:92` carries a historical `Monitor` mention (out of scope, informational) |
| AC6 `e2e-watch-wake.sh` executable, stream-json, drop after first result, asserts FileChanged exit 2 + second assistant, PASS/FAIL; old script deleted; pinned in tests/docs.rs | **FAIL (pin only, one line)** | Script itself: executable, `#!/bin/sh`, correct flow, `e2e-watch-arm.sh` deleted (test `e2e_watch_wake_script_is_executable_and_pins_the_flow`, matrix F63). Pins on `4b714d8`: mutants E3 (drop before result wait), E4 (`"exit_code":0`), E5 (assistant assertion replaced) SURVIVED — the header comment (script lines 18–21) contains the needles. On `d47cd2f` (`mutants-d47cd2f.log`): E3, E4, I1 (`--input-format text`), I2 (exit_code grep dropped), I3 (missing-result FAIL removed) KILLED; **E5 still SURVIVES**: `s.contains("\"type\":\"assistant\"")` matches the comment, so the code's `grep -c -F '"type":"assistant"'` is unpinned. Fix: assert `code.contains("grep -c -F '\"type\":\"assistant\"'")` on the comment-stripped `code` string. test-reviewer GAP → OK-after-d47cd2f for the reviewer's named gap (E3) |
| AC7 `scripts/e2e-watch-wake.sh` with `E2E_PLUGIN_DIR` + release `owl` prints PASS | PASS | `ac7/output.txt`: `PASS (woke 4s after the record was added; one user message sent)`, owl 0.1.0 (release build, `release-build.log`), claude 2.1.263; `ac7/stream.jsonl` line 31 `hook_response` `hook_event: FileChanged`, `exit_code: 2`, stderr = 🦉 sentence; lines 47–49 second `assistant` turn; 0 `"type":"user"` events in the output stream; `claude.stderr` empty. Run with the nested-session env vars unset |
| AC8 post-merge on this machine | PENDING | orchestrator/user, post-merge |
| AC9 build / test / clippy / fmt; no new crate | PASS | `gates.log` (4b714d8): build 0, test 0 (26 binaries, 0 failed), clippy 0, fmt 0. `gates-d47cd2f.log`: `cargo test --workspace --no-fail-fast` exit 0 (26 × `test result: ok`), clippy 0, fmt 0. `git diff main...d47cd2f -- Cargo.toml Cargo.lock` empty |

Sub-agents: test-planner (`test-planner.md`, 64 rows) — every row run (tests or `matrix.sh`), 63 PASS, row 37 (tty) PASS via `script(1)`; test-reviewer (`test-reviewer.md`) — GAPS(1), closed by `d47cd2f`. Own mutant sweep (`mutants.sh`, per-mutant logs `mutant-logs/`): 58 mutants on `4b714d8` → 54 KILLED, 4 SURVIVED (D19 out of scope; E3/E4/E5 comment-masked pins); rerun on `d47cd2f` (`mutants-d47cd2f.sh`, 7 mutants) → E3, E4, I1, I2, I3 KILLED; E5 SURVIVED; D19 SURVIVED (informational).

FAILING: AC6 (test pin for the second-assistant assertion only; no product change needed)

PM-REVIEW:
1. Corrupt file in `spool/inbox/` (matrix C41): `owl inbox --count` exits 1 on any unparseable record (pre-existing `Spool::list` strictness), `owl-count.sh` swallows it → no `watchPaths` on SessionStart and no wake while the file exists. Not reachable by the ACs; `list_lenient` exists.
2. `owl-count.sh` propagates any owl exit 2 on `FileChanged` as a wake (D54: a clap usage error would wake the session with clap's text) and does not match the `--hook-event=FileChanged` form (D55: silent). Both unreachable from the shipped `hooks.json`.
3. AC5's `Monitor` check is plugin-dir only (as written); `docs/plan.md:92` mentions `Monitor` historically — no action.
4. Task file part C says `owl-count.sh` "unchanged" then allows the stderr change; the script did change (FileChanged case). Design doc §9 describes the shipped behaviour; no doc/task conflict found.
5. AC2 "terminal stdin" is verified only by the shell row (pty via `script(1)`), not under `cargo test` (cannot be induced there).

## Verification — round 2 (AC6 only, head `e398a8d`)

2026-09-06. `git diff d47cd2f e398a8d --stat` = `tests/docs.rs | 7 +` (one pin added: the assistant check on its code line). Fresh detached worktree `.worktrees/verify-OWL-031` at `e398a8d`, private `CARGO_TARGET_DIR`, removed afterwards. Evidence: `.orch/tasks/OWL-031.evidence/round2/`. `cargo test -- --list` names `e2e_watch_wake_script_is_executable_and_pins_the_flow` (`gates-e398a8d.log`).

| AC | Verdict | Evidence |
|---|---|---|
| AC1–AC5, AC7, AC9 | PASS (carried from round 1; no src/plugin/docs/script change since `4b714d8`) | round-1 table above |
| AC6 `e2e-watch-wake.sh` + tests/docs.rs pins | **PASS** | `mutants-e398a8d.log`: E3 (drop before result wait), E4 (`"exit_code":0`), **E5 (assistant grep replaced by `"type":"system"`) KILLED** — fails at tests/docs.rs:456 `the assistant check counts "type":"assistant" after the hook_response line` (`round2/mutant-logs/E5.log`); I1 (`--input-format text`), I2 (exit_code grep dropped), I3 (missing-result FAIL removed) KILLED. D19 (`Monitor` in docs/concept.md) survives, out of AC5 scope, informational. Script unchanged since round 1 (executable, correct flow, old script deleted) |
| AC8 post-merge | PENDING | orchestrator/user |
| AC9 gates on `e398a8d` | PASS | `gates-e398a8d.log`: `cargo test --workspace --no-fail-fast` exit 0, 26 binaries `test result: ok`, 499 passed total; clippy `-D warnings` 0; `cargo fmt --check` 0 |

FAILING: none

Draft PR: https://github.com/Krab00/owlpost/pull/41 (status `done` 2026-09-06; AC8 runs after the human merges it).
