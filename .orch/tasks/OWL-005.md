---
id: OWL-005
title: mTLS spike — self-signed cert from ed25519 key, pinned verifiers
status: todo
deps: [OWL-002]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `tls.rs` per `docs/architecture.md §4` and `docs/technical-design.md §7`. From the
ed25519 identity build a self-signed X.509 certificate with rcgen (Ed25519 signature
algorithm). Provide `server_config(identity, book, allow_unpinned_for_card: bool) ->
rustls::ServerConfig` with a custom `ClientCertVerifier` that accepts a client certificate iff
its SubjectPublicKeyInfo public key equals a pubkey in the ContactBook (and records the
resulting fingerprint for handlers), or, when configured, accepts *no* client certificate for
card-only requests; and `client_config(identity, expected_pubkey: Option<PubKey>) ->
rustls::ClientConfig` with a `ServerCertVerifier` that pins the server's public key (None =
unpinned, used only by `owl add`). Expose `peer_fingerprint_from_cert(&CertificateDer)`.
This is the riskiest piece of the project: do it first, keep it under ~200 lines, document any
rustls `danger` API used.

## Acceptance criteria
- [ ] AC1: integration test `tests/tls.rs::pinned_handshake_succeeds` starts a rustls TLS listener on 127.0.0.1:0 with identity B and a book containing A; a client with identity A pinned to B's pubkey completes the handshake and the server observes A's fingerprint (modality: test)
- [ ] AC2: integration test `tests/tls.rs::unknown_client_is_refused` connects with identity C (not in B's book) and asserts the handshake fails (modality: test)
- [ ] AC3: integration test `tests/tls.rs::wrong_server_key_is_refused` pins the client to a different pubkey than the server presents and asserts the handshake fails (modality: test)
- [ ] AC4: integration test `tests/tls.rs::unpinned_client_allowed_when_enabled` with `allow_unpinned_for_card = true` completes a handshake without a client certificate, and the same server with the flag false refuses it (modality: test)
- [ ] AC5: unit test `tls::tests::cert_carries_identity_key` asserts the generated certificate's SPKI public key bytes equal the identity's public key (modality: test)
- [ ] AC6: `cargo clippy --all-targets -- -D warnings` passes with no `#[allow]` on the verifier implementations (modality: test)
