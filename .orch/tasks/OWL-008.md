---
id: OWL-008
title: Pull loop — answer ingestion, ack, liveness probe cache
status: done
deps: [OWL-007]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement the pull half of `daemon.rs` per `docs/architecture.md §3.5`. Every
`config.pull_interval_secs` the daemon groups `asks/` records by responder fingerprint, and for
each responder with at least one open ask calls `GET /v1/outbox` on the first reachable
endpoint. For each returned envelope: verify the signature against the responder's pinned key,
check `in_reply_to` matches an open ask, store the answer in `inbox/` with state `pending`,
update the asker cache, move the ask to `done/`, ack, and emit the daemon's `AnswerIngested`
event, which `notify.rs` (OWL-012) turns into the desktop notification. Responders that fail to
connect are recorded in an in-memory liveness cache (`last_probe`, `ok`) and skipped for 60 s,
capped at one pull interval minus one second (so the re-probe never depends on tick jitter; a
1 s interval re-probes an offline peer on every loop, as AC4 requires). Outbox
entries older than `outbox_ttl_days` are expired to `done/` with state `expired` by the
responder's daemon on each loop. `owl doctor` reports the daemon's last pull time from
`$OWLPOST_HOME/daemon.status` (JSON written each loop).

## Acceptance criteria
- [ ] AC1: integration test `tests/pull.rs::answer_is_pulled_and_acked` with `pull_interval_secs = 1`: A has an open ask to B; B's outbox holds a signed answer for it; within 5 s A's inbox has the answer (state pending, seen false), A's cache has it, A's `asks/` is empty, and B's outbox record is in `done/` (modality: test)
- [ ] AC2: integration test `tests/pull.rs::forged_answer_is_dropped` places an answer in B's outbox signed by a key that is not B's and asserts A never ingests it and logs a warning (modality: test)
- [ ] AC3: integration test `tests/pull.rs::unrelated_answer_is_ignored` places an answer whose `in_reply_to` matches no open ask and asserts it is not ingested and not acked (modality: test)
- [ ] AC4: integration test `tests/pull.rs::offline_responder_is_skipped_then_retried` points B at a closed port for the first 3 s and then starts B; A ingests the answer within 10 s (modality: test)
- [ ] AC5: integration test `tests/pull.rs::outbox_ttl_expires` with `outbox_ttl_days = 0` and a record dated yesterday asserts it moves to `done/` with state `expired` on the first loop (modality: test)
- [ ] AC6: `daemon.status` JSON contains `last_pull_at`, `open_asks`, `peers_probed` after one loop (modality: data)

## Verification — task/OWL-008-pull

Round 1, 2026-09-03, branch `task/OWL-008-pull` @ `507f9d2`, worktree `.worktrees/OWL-008`. Evidence: `.orch/tasks/OWL-008.evidence/`.

**Gate** (`gate.log`): build OK, `cargo test --workspace` OK (all bins green), clippy `-D warnings` OK, fmt OK.
**Flakiness** (`pull-3x.log`): `cargo test --test pull` 3/3 green (4.1–5.1 s each).
**Criteria**: branch copy of AC1–AC6 byte-identical to this file; the implementer's Description edits (AnswerIngested event → notify; liveness skip = min(60 s, one pull interval)) are consistent with the code and were copied here.

| AC | Verdict | Evidence |
|---|---|---|
| AC1 answer pulled, stored pending/unseen, cached, ask → done, B outbox → done(acked) | PASS | `tests/pull.rs::answer_is_pulled_and_acked` 3/3; mutants M1/M5/M6/M14 killed (`mutants.log`); grouping probe: two asks to one responder ingested in one `pull_once` (`probes.log` GROUPING open_asks=0 peers_probed=1 events=2) |
| AC2 forged answer dropped + WARN, not acked | PASS | `forged_answer_is_dropped` asserts `WARN … signature does not verify` and B outbox still `unacked`; M1 (skip verify) and M2 (ack on Forged) killed; probe `probe_forged_shape`: right key + question payload / non-JSON raw / non-payload JSON all → Forged |
| AC3 unrelated answer not ingested, not acked | **FAIL** | AC-named test passes and M3/M4 killed, but probe `probe_cross_peer_answer` (`verify_probe.rs`, `probes.log` ROW6): a B-signed answer whose `in_reply_to` is A's open ask **to C** is ingested, cached under C's question hash, closes the ask to C and is acked (`ask_to_c_still_open=false hijack_in_inbox=true hijack_acked=true`). `ingest_envelope` (`src/pull.rs:212`) matches `open` without checking `ask.peer == contact.fingerprint`, so any pinned contact can answer questions addressed to another contact. Fix: filter the open ask by the responder's fingerprint (one line) + a test with two contacts. `ask --wait` is unaffected (single-entry map with the right peer). |
| AC4 offline responder skipped then retried, ingested < 10 s | **FAIL (tests)** | Behaviour proven by the verifier: real late start of B on the same fixed `listen` port after ≥2 failed probes → ingested at 4.07 s (`probes.log` AC4-LATE); re-probe counter: 4 failed probes / 1 skipped loop in 4.5 s at interval 1 (`probes-2.log` ROW16); M9/M10/M13/M15/M16 killed. But the shipped `offline_responder_is_skipped_then_retried` rewrites A's contact from a closed port to an already-running B (proves book reload, not retry of the same endpoint) and asserts "retried" only via eventual ingestion, no probe counter (test-reviewer gaps 1–2). Required: start B late on `Config.listen = closed_port()` via `common::respawn`, and wait for ≥2 `outbox fetch failed` lines naming the dead address before B comes up. |
| AC5 TTL 0 + record dated yesterday → done(expired) on first loop | PASS | `outbox_ttl_expires` (boundary twin dated now is kept); unit `expire_outbox_boundary` (exact/older/future/unparseable, TTL 0/1/14), `expire_outbox_failed_move_leaves_the_record_unacked`; M7 (`<=`→`<`) and M8 (expired→acked) killed; probe `probe_ttl_max` (u64::MAX no panic) |
| AC6 `daemon.status` has `last_pull_at`, `open_asks`, `peers_probed` after one loop | PASS | Built binary `owl daemon --foreground` against a temp home: `jq` keys `last_pull_at,open_asks,peers_probed`, timestamp advances each second, `owl doctor` → `ok pull: last pull 0s ago, 0 open ask(s), 0 peer(s) probed`; blocking `daemon.status` with a dir → WARN each loop, loop keeps running, no `.tmp` left (`ac6-daemon.stderr`); `status_file_is_written_each_loop`; M11 (missing key) killed; probes `probe_status_write_blocked`, `probe_read_status_wrong_shape` (`[]`, null, missing/wrong-typed fields → `parsing` error; `owl doctor` exit 1 `fail pull`, `--json` status `fail`) |

**test-planner** (`test-planner.md`): 48 rows; 15 GAP rows attempted, all executed via `verify_probe.rs` — 14 behave as designed, row 6 is the AC3 defect. Extra loop-robustness probes green: status write blocked (ROW25), asks dir removed → `pull failed` and `last_pull_at` frozen until restored (ROW26), outbox dir chmod 000 → `outbox expiry failed`, pull + status still advance (ROW21), done/ blocked → inbox written first, ask stays waiting, loop continues, ack after unblock (ROW8), ack refused by B → stored + `answered`, warned once, not re-fetched because no open ask remains (ROW7), duplicate answers in one batch → one ingested/acked, second left unacked (ROW5), `owl ask --wait` skips a forged entry and takes the genuine one, forged never acked (ROW40), `--wait` I/O error exits non-0/non-4 with ask still waiting and answer unacked (ROW42).
**test-reviewer** (`test-reviewer.md`): GAPS — AC4 fixture (late start + counter), grouping test, `--wait` Forged arm. Doctor rewrite lost no coverage.
**Mutants** (`mutants.log`): 16/16 killed under separate `--lib pull` / `--test pull` / `--test doctor` / `--test ask` runs; 0 survivors, 0 NOCOMPILE.

**Advisory (not blocking):** at `pull_interval_secs = 1` the skip window equals the tick period, so whether a loop re-probes an offline peer depends on sub-millisecond tick jitter (observed 1 skipped loop in 5). A window strictly shorter than the interval (or counting loops) would make it deterministic.

FAILING: AC3 (cross-peer answer ingested — code fix + test), AC4 (test does not run the named scenario; add late-start fixture + probe counter). Also add the `--wait` Forged-arm test and a two-asks-one-responder test.

### Round 2 (2026-09-03) — `task/OWL-008-pull` @ `564b61b`, AC3 + AC4 only

**Gate** (`gate-r2.log`): build OK, `cargo test --workspace` OK, clippy `-D warnings` OK, fmt OK. **Flakiness**: `--test pull` 3/3 (8 tests), `--test ask` 3/3 (21 tests). Criteria byte-identical to the branch copy; the Description edit (skip window = min(60 s, interval) − 1 s, jitter-free re-probe) matches `Liveness::skip_for` and was ported here.

| AC | Verdict | Evidence |
|---|---|---|
| AC3 | PASS | `ingest_envelope` now filters the matched open ask by `ask.peer == contact.fingerprint` (`src/pull.rs:222`). Verifier probe `probe_cross_peer_answer` re-run against the fix: `ask_to_c_still_open=true hijack_in_inbox=false hijack_acked=false` (`probes-r2.log`). New tests `answer_for_another_peers_ask_is_ignored` (integration, two contacts, asserts inbox/cache/ask/ack + debug line) and the unit case in `ingest_envelope_verdicts`. Mutant R1 (filter dropped) killed by the unit test every run and by the integration test in 3 of 4 runs (`mutants-r2.log` recorded one PASS of `--test pull` under R1 that two manual full-suite reruns and a script rerun could not reproduce, `mutant-r1-recheck.log`, `mutant-r1-script-rerun.log`; most likely a build-timestamp artefact — noted, not blocking, since the unit kill is deterministic). |
| AC4 | PASS | `offline_responder_is_skipped_then_retried` rewritten: B's home prepared on a fixed closed `Config.listen` port, ≥2 `outbox fetch failed` lines naming that port asserted by counter and `fetched() == 0` while down, then `common::respawn` on the same port, ingestion < 10 s, ≥1 `outbox fetched` line for B's fingerprint. Verifier probes: late start ingested at 3.08 s; re-probe counter 5 failed probes / 0 skipped loops in 4.5 s at interval 1 (round 1 had 1 skipped loop — the −1 s cap removed the jitter). Mutants R2 (skip ignored), R3 (constant 60 s window), R4 (old min(60, interval) without −1 s) killed; R3 dies in `offline_responder_is_skipped_then_retried` itself. Test-mutant R5 (both counter thresholds → 0) fails the AC4 test (`assert_eq!(fetched(), 0)` / timing still hold, so the weakened test is caught), and R5+R3 combined still fails. |

test-reviewer round-1 gaps closed: grouping (`asks_are_grouped_by_responder`, one probe for two asks, `open_asks == 0` in the same `pull_once`), `owl ask --wait` Forged arm (`tests/ask.rs::wait_skips_forged_entries_and_takes_the_genuine_answer`: forged skipped and never acked, genuine printed and acked), AC4 counter (above). Nothing committed by the verifier; implementer worktree clean.

FAILING: none

PR: https://github.com/Krab00/owlpost/pull/14 (draft)
