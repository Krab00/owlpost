---
id: OWL-032
title: "Message rendering moves into the CLI: `owl show --format claude` prints the framed block, `owl inbox --format claude` prints the answers table; the plugin pastes, never re-renders"
status: done
deps: [OWL-031]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
Observed 2026-09-06 23:08–23:15 UTC in an interactive session: the model walked `/owlpost:inbox`
and printed the consent question and the three answer records in its own layout (a plain bold
header, a table with a `🟢` marker and a `re:` column) instead of the SKILL.md "Framed message"
(16 × 🟧, `🦉 **<peer>** · HH:MM · <project> · <path>`) and "Showing messages" (`🟦 00:16 ·
<peer> | text`) formats. Root cause is structural: `commands/inbox.md` is what the model has in
context, and in four places it says "see the skill, 'Framed message'" / "see the owlpost skill,
'Showing messages'". SKILL.md is not loaded when the command runs, so the reference points at
text the model has never seen and it improvises. Any model, any harness, will do the same.

Fix: render in code, not in prose. The CLI prints the finished Markdown; the command says
"paste the output verbatim". One rule instead of a template to copy, deterministic, the same
for Claude, Codex and Kimi, and testable.

- `owl show <id> --format claude` prints the framed block for a `question` record (any
  state) and for an `answer` record: line 1 and the last line are exactly 16 × `🟧`; line 2 is
  `🦉 **<peer name>** · HH:MM · <project or `-`> · <path or `whole repository`>`; on a
  `consent` record the header carries the fingerprint after the name (`**<name>** (owl:…)`);
  the body is the message text verbatim inside a ```` ```text ```` fence (a text containing
  three backticks is fenced with four). HH:MM is the local time of `received_at`. A `drafted`
  record prints the frame for the question, then `draft:` and the draft in a plain
  ```` ```text ```` block (drafts are ours, never framed), then `harness: <name>`. When the
  draft's language differs from the question's (detected by a small stopword heuristic,
  Polish/English only; ponytail), one line `note: the draft is in a different language than the
  question — pick Edit` follows. Without `--format` the plain output is unchanged.
- `owl inbox --format claude` (listing mode, no `--count`) prints the received-messages table
  for the `answer` records: header-less two-column Markdown table, newest last, only records
  received in the last 24 h, at most the 10 newest, one line under it `<N> older answers not
  shown — owl history` when any were cut; the left cell is `<marker> HH:MM · <peer name>`, the
  right cell the answer text verbatim with `|` escaped as `\|` and newlines as `<br>`; above the
  table one line per answer `↳ <question id short> "<first line of the question, ≤60 chars>"`
  keyed by the same marker, so each answer names the question it replies to. Markers come from
  the fixed list 🟦 🟩 🟨 🟪 🟧 🟥 (then repeat) and are assigned per peer fingerprint in order
  of first appearance, persisted in `$OWLPOST_HOME/markers.json` (append-only map fingerprint →
  index), so the same peer keeps the same marker across calls and sessions. Question records in
  the listing are printed as framed blocks (same renderer as `owl show`), consent ones first.
  Listing still marks the listed records seen. `--format codex|kimi` in listing mode print the
  same Markdown (the harness-specific difference is only in `--count` injection lines).
- `plugins/claude-code/commands/inbox.md`: every "print the framed message block (see the
  skill …)" becomes `run owl show <id> --format claude and paste its output verbatim`; the
  answers step becomes `run owl inbox --format claude and paste its output verbatim`; no
  sentence in the file refers the reader to another file for a format. A "Never" list at the
  top: never a Markdown table for a single question; never your own icons, markers or columns;
  never paraphrase; the orange frame always means "from a peer". SKILL.md "Showing messages" and
  "Framed message" keep the examples but state that the CLI renders them and the model pastes.
- Design doc §9 (CLI contract): the `--format claude` output of `show` and `inbox` listing,
  the frame literals, the 24 h / 10 rows rule, `markers.json`.

Out of scope: colours in the terminal (plain output unchanged), the history command's rendering
(`owl history --format claude` can reuse the table renderer later), any change to `--count`
injection lines (OWL-031 contract stays byte-identical).

## Acceptance criteria
- [ ] AC1: `owl show <id> --format claude` on a `pending` question prints exactly: 16 × 🟧, the header line with peer name, `HH:MM` of `received_at` in local time, project and `whole repository` for a null path (the path itself otherwise), a ```` ```text ```` fence with the question byte-identical, 16 × 🟧; on a `consent` record the header contains ` (owl:<fingerprint>)` right after the bold name; on a `drafted` record the frame is followed by `draft:`, the draft in a plain ```` ```text ```` block and `harness: <name>`; a question containing three backticks is fenced with four; `--format plain` and no `--format` print today's output unchanged (modality: test)
- [ ] AC2: `owl inbox --format claude` with 12 answer records from two peers (11 within 24 h, 1 older, spread over 26 h) prints a two-column table with exactly 10 rows, newest last, the oldest in-window row dropped, the line `2 older answers not shown — owl history`, the same marker for the same fingerprint on every row, `🟦` for the first-appearing peer and `🟩` for the second, `|` in a cell escaped and newlines rendered as `<br>`, one `↳` line per shown answer naming its question; a second run (new process) assigns the same markers (`markers.json` persisted); listing marks the records seen (modality: test)
- [ ] AC3: `owl inbox --format claude` with a `consent` question, a `pending` question and one answer prints the two framed blocks (consent first) and then the table, in that order, with no other text in between (modality: test)
- [ ] AC4: a drafted record whose question is Polish and draft English (and the reverse) prints the `note: the draft is in a different language than the question — pick Edit` line; same-language pairs print no note (modality: test)
- [ ] AC5: `commands/inbox.md` contains `owl show <id> --format claude` and `owl inbox --format claude`, contains `paste its output verbatim`, and contains neither `see the skill` nor `see the owlpost skill`; its first 25 lines contain `never your own icons, markers or columns`; SKILL.md "Framed message" and "Showing messages" contain `the CLI renders`; design doc §9 contains `--format claude` under `show` and `markers.json` — each pinned by a single-line `contains()` in `tests/docs.rs` (modality: test)
- [ ] AC6: `tests/plugin.rs` (or `tests/inbox.rs`) runs the real binary end to end: spool one consent question from a contact, run `owl show <id> --format claude`, assert the first and last lines are `🟧` × 16 and the header starts with `🦉 **` (modality: test)
- [ ] AC7: on this machine, in a fresh interactive session, `/owlpost:inbox` with one held question and one answer in the spool shows the orange frame and the marker table exactly as the CLI printed them (checked by the user; the session transcript is the evidence) (modality: manual)
- [ ] AC8: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-032-format-claude

Branch `task/OWL-032-format-claude` @ `6770c62` (base `main` @ `953850a`), worktree `.worktrees/OWL-032` (private `target/`), verified 2026-09-07. Evidence under `.orch/tasks/OWL-032.evidence/` (`e1…e9-*` = stdout of the real binary captured by a throwaway test appended to `tests/inbox.rs`, reverted + `touch`ed afterwards; `gates.txt`; `lists-and-lock.txt`; `ac5-greps.txt`; `md-fence-parity.txt`; `mutants.md` + `mutants.log`).

| AC | modality | verdict | evidence |
|---|---|---|---|
| AC1 | test | PASS | `e1-show-claude-utc.md` (16 × 🟧 first/last, `🦉 **Maciek** · 23:08 · <project> · src/auth/session.rs`, ```text fence, question verbatim), `e1-show-claude-gmt-9.md` (`TZ=Etc/GMT-9` → `08:08`, local not UTC), `e2-show-nullpath.md` (`whole repository`), `e3-show-consent.md` (`**Ana** (owl:wyxim75c6m5p4ywv) ·` right after the bold name), `e4-show-drafted.md` (frame, `draft:`, plain ```text block, `harness: fake`), `e5-show-backticks.md` (four-backtick fence both ends), `e1-show-plain.txt` == `e1-show-noformat.txt` (`cmp` SAME) and both equal main's pinned shape (`show_prints_content_and_marks_seen`, unchanged from main, green). Tests: `show_format_claude_prints_the_framed_block`, `…_frames_an_answer`, `…_prints_the_draft_and_the_language_note` (`tests/inbox.rs`) |
| AC2 | test | FAIL (behaviour PASS; test gaps) | `e8-fixture.txt` + `e8-inbox-claude-run1.md`: 12 answers, Maciek/Ana alternating, 26 h span; 10 rows, `answer 2`…`answer 11` ascending (newest last), `answer 0` (26 h) and `answer 1` (oldest in window) absent, last line `2 older answers not shown — owl history`, 🟦 on every Maciek row (first-appearing), 🟩 on every Ana row, row 7 `a \| b<br>second line`, ten `↳ <last 8 of question id> "question N?"` lines. `e8-run1-eq-run2.txt` = `true` (second process, same markers), `e8-markers.json` = `{maciek: 0, ana: 1}`, `e8-inbox-json-after.json` 12/12 `seen: true`. Test: `inbox_format_claude_prints_the_answers_table` |
| AC3 | test | PASS | `e9-inbox-ac3.md` / `e9-inbox-ac3.debug.txt`: whole stdout = consent block (Ana, with fingerprint), blank line, pending block (Maciek), blank line, `↳` line + 1-row table, nothing else (no consent prompt, no notes). Test: `inbox_format_claude_prints_blocks_then_table_and_nothing_else` |
| AC4 | test | PASS | `e4-show-drafted.md`: PL question/EN draft → `note: … — pick Edit`; EN/PL → note; EN/EN and PL/PL → no note; plain output of the same records has no note and no frame. Test: `show_format_claude_prints_the_draft_and_the_language_note`; unit `language_note_only_when_both_known_and_different` (unknown/tie → none) |
| AC5 | test | FAIL (docs PASS; one pin ineffective) | `ac5-greps.txt`: inbox.md `owl show <id> --format claude` ×5, `owl inbox --format claude` ×1, `paste its output verbatim` ×5, `see the skill` 0, `see the owlpost skill` 0, `never your own icons, markers or columns` in lines 1–25 (line 20); SKILL.md `the CLI renders` at line 169 (Showing messages) and 193 (Framed message); design §9 `owl show` row contains `--format claude`, §9 contains `markers.json`. Pinned by `format_claude_docs_pin_the_cli_renders_the_model_pastes` (`tests/docs.rs`), single-line `contains()` per literal; mutants D1–D14 below |
| AC6 | test | PASS | `tests/plugin.rs::show_format_claude_frames_a_consent_question_end_to_end` runs the real `owl` binary on a spooled consent question from a contact (`home_with(1,…)`), asserts first/last line == 🟧×16 and `lines[1].starts_with("🦉 **Maciek** (owl:")`; listed in `cargo test -- --list` (`lists-and-lock.txt`), green in `gates.txt` |
| AC7 | manual | PM-REVIEW | Not attempted (interactive session, user-checked). Steps for the human: (1) install this branch on the machine — `owl update --source task/OWL-032-format-claude` AND make sure the marketplace's plugin source (the main checkout, see OWL-031 AC8 lesson) is on this branch or reinstall the plugin from `.worktrees/OWL-032/plugins/claude-code`; (2) spool one held question (a contact with policy `never`/no policy asking) and one answer (ask a peer with `owl ask … --wait 0`, let the answer arrive); (3) in a fresh `claude` session run `/owlpost:inbox`; (4) compare the transcript with `owl show <id> --format claude` and `owl inbox --format claude` run in a terminal: the orange frame (16 × 🟧, `🦉 **…**` header, ```text body) and the 🟦 marker table must appear byte-for-byte as the CLI printed them, no model-made table or icons |
| AC8 | data | PASS | `gates.txt`: `cargo build` rc=0, `cargo fmt --check` rc=0, `cargo clippy --all-targets -- -D warnings` rc=0, `cargo test --workspace --no-fail-fast` rc=0 (all 26 binaries `ok`, 0 failed; 136 lib + 32 inbox + 26 plugin + 11 docs …). No new crate: `lists-and-lock.txt` — `git diff main...HEAD -- Cargo.lock` adds 0 `[[package]]` entries, only `+ "chrono",` under the `owlpost` dependency list; `chrono 0.4.45` already in main's lock; design §1 line 18 carries the one-line justification |

### Mutant sweep (verifier's own list, built from the diff's consumers; per-mutant `timeout 300`; `mutants.md`, `mutants.log`)

66 mutants applied (63 by script + 3 by hand): **57 RED, 1 EQUIVALENT, 8 SURVIVED**. Baseline re-run on the restored tree green (`post-sweep-green.txt`), tree clean at 6770c62.

Survivors and what they mean:

| survivor | consequence | ruling |
|---|---|---|
| M10b `in_window.sort_by` deleted | every fixture (unit `(0..12)` and `tests/inbox.rs` `build`) inserts rows in ascending `received_at`, and `spool.list` returns put order, so "newest last" is never distinguished from "list order" | **AC2 test gap** — fix: spool the 12 answers out of chronological order (e.g. `(0..12).rev()` or row 5 first) in both the unit test and `inbox_format_claude_prints_the_answers_table`; keep the newest-last assertions |
| M26 `.take(60)` → `.take(600)` | the AC2 literal "first line of the question, ≤60 chars" has no fixture longer than ~12 chars | **AC2 test gap** — fix: one row (unit or e2e) with a 61+-char first line containing a multi-byte char around position 60; assert the `↳` line ends with exactly the first 60 chars + `"` |
| D6 SKILL.md line 169 `the CLI renders` removed (Showing messages only) | `md_section(&skill, "## Showing messages")` runs to `## Memory rule` and therefore *includes* `### Framed message` (line 178–199); `showing.contains("the CLI renders")` stays green on line 193's copy. The Showing-messages pin guards nothing (rule stated twice, one copy unguarded — OWL-024 lesson) | **AC5 test gap** — one-line fix in `tests/docs.rs`: replace `assert!(showing.contains("the CLI renders"));` with `assert!(showing.contains("the CLI renders it — `owl inbox --format claude` prints the table"));` (that phrase sits on one line, line 169, and does not occur in the Framed-message subsection), or cut `showing` at `### Framed message` |
| M46 inbox listing `--format claude` checked before `--json` | no test runs `owl inbox --json --format claude` (§9: "`--json` is unchanged") | not an AC literal; recommend one assert (JSON array, no Markdown, no `markers.json` written) |
| M24 `find_question` = `[Dir::Done]` only | §9 claims `done/`, `asks/` or `inbox/`; only `done/` is ever exercised | not an AC; design-doc claim unguarded — either test the `asks/`/`inbox/` arms or narrow the doc |
| M25 `in_reply_to` from `rec.meta` fallback removed | `Payload::answer` always sets `in_reply_to` and `pull.rs` refuses answers without a matching ask, so the arm is unreachable | not an AC; CLAUDE.md §2 says delete it (or give it a fixture) |
| M41 listing passes `None` as the draft | a `drafted` question in `owl inbox --format claude` loses `draft:`/`harness:`/`note:`; the description says "same renderer as `owl show`", no AC names it, inbox.md step 4 uses `owl show` for drafts | not an AC; recommend adding one `set_draft` record to the AC3 test |
| M17 `!json &&` dropped from `framed` | equivalent: `if json {…} else if framed {…}` in the loop already gives `--json` precedence (`e1-show-json-wins.json`) | no gap |

Full table:

| mutant | file | verdict | killed by |
|---|---|---|---|
| M1 FRAME 16→15 | src/render.rs | RED | render::: render::tests::frame_is_sixteen_icons_header_and_fence |
| M2 consent header: fingerprint dropped | src/cli/show.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else, show_format_claude_prints_the_framed_block |
| M2b fingerprint on pending instead of consent | src/cli/show.rs | RED | format_claude: show_format_claude_frames_an_answer, show_format_claude_prints_the_framed_block, inbox_format_claude_prints_blocks_then_table_and_nothing_else |
| M3 UTC instead of local time | src/render.rs | RED | format_claude: show_format_claude_prints_the_framed_block |
| M4 fence always three backticks | src/render.rs | RED | render::: render::tests::fence_grows_past_backticks_in_the_text |
| M5 pipe not escaped | src/render.rs | RED | render::: render::tests::cell_escapes_pipes_and_breaks |
| M6 newline not <br> | src/render.rs | RED | render::: render::tests::cell_escapes_pipes_and_breaks |
| M7 row cap 10→9 | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M8 window 24h→25h | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M8b window 24h→23h | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M9 markers.json never written | src/render.rs | RED | render::: render::tests::markers_follow_first_appearance_and_persist |
| M10 sort reversed (newest first) | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M10b sort deleted | src/render.rs | SURVIVED |  |
| M11 language note one direction only | src/render.rs | RED | render::: render::tests::language_note_only_when_both_known_and_different |
| M12 language note also when same language | src/render.rs | RED | render::: render::tests::language_note_only_when_both_known_and_different |
| M13 consent not first | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else |
| M14 older line only when cut > 1 | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M14b older line text | src/render.rs | RED | format_claude: inbox_format_claude_prints_the_answers_table |
| M15 whole repository literal | src/render.rs | RED | render::: render::tests::frame_is_sixteen_icons_header_and_fence |
| M16 empty project not dashed | src/render.rs | RED | render::: render::tests::frame_is_sixteen_icons_header_and_fence |
| M17 --format beats --json in show | src/cli/show.rs | SURVIVED |  |
| M18 draft: literal | src/render.rs | RED | render::: render::tests::fence_grows_past_backticks_in_the_text |
| M19 harness: literal | src/cli/show.rs | RED | format_claude: show_format_claude_prints_the_draft_and_the_language_note |
| M20 marker list order swapped | src/render.rs | RED | render::: render::tests::table_names_missing_questions_with_an_empty_string, render::tests::markers_follow_first_appearance_and_persist, render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M21 marker index off by one | src/render.rs | RED | render::: render::tests::table_names_missing_questions_with_an_empty_string, render::tests::markers_follow_first_appearance_and_persist, render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M22 inbox listing: codex/kimi not rendered | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_the_answers_table |
| M23 answer frame ignores the question's project/path | src/cli/show.rs | RED | format_claude: show_format_claude_frames_an_answer |
| M24 find_question only done/ | src/render.rs | SURVIVED |  |
| M25 in_reply_to meta fallback removed | src/cli/inbox.rs | SURVIVED |  |
| M26 first line not cut at 60 | src/render.rs | SURVIVED |  |
| M27 rendered listing does not mark seen | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else, inbox_format_claude_prints_the_answers_table |
| M28 sections joined by one newline | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else |
| M29 empty table still a section | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else |
| M30 short id = first 8 chars | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last, render::tests::table_names_missing_questions_with_an_empty_string |
| M31 text_block keeps trailing newlines | src/render.rs | RED | render::: render::tests::fence_grows_past_backticks_in_the_text |
| M32 cell keeps trailing newlines | src/render.rs | RED | render::: render::tests::cell_escapes_pipes_and_breaks |
| M33 ↳ marker replaced | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last, render::tests::table_names_missing_questions_with_an_empty_string |
| M34 table delimiter | src/render.rs | RED | render::: render::tests::table_keeps_24h_window_and_10_newest_rows_newest_last |
| M35 markers do not wrap after six | src/render.rs | RED | render::: render::tests::markers_follow_first_appearance_and_persist |
| M36 language tie = English | src/render.rs | RED | render::: render::tests::language_note_only_when_both_known_and_different |
| M38 language note never (show) | src/cli/show.rs | RED | format_claude: show_format_claude_prints_the_draft_and_the_language_note |
| M39 show ignores the draft | src/cli/show.rs | RED | format_claude: show_format_claude_prints_the_draft_and_the_language_note |
| M40 show: codex/kimi not framed | src/cli/show.rs | RED | format_claude: show_format_claude_prints_the_framed_block |
| M41 listing ignores drafts | src/cli/inbox.rs | SURVIVED |  |
| M42 markers not saved after listing | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_the_answers_table |
| M43 ↳ line loses the question text | src/cli/inbox.rs | RED | format_claude: inbox_format_claude_prints_blocks_then_table_and_nothing_else, inbox_format_claude_prints_the_answers_table |
| M44 blocks: header line dropped | src/render.rs | RED | render::: render::tests::frame_is_sixteen_icons_header_and_fence |
| M45 language note literal | src/render.rs | RED | format_claude: show_format_claude_prints_the_draft_and_the_language_note |
| D1 inbox.md: `owl show <id> --format claude` → `owl show <id>` | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D2 inbox.md: `owl inbox --format claude` → `owl inbox` | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D3 inbox.md: paste its output verbatim → print its output | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D4 inbox.md: 'see the skill' sentence added | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D4b inbox.md: 'see the owlpost skill' sentence added | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D5 inbox.md: 'never your own icons, markers or columns' removed | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D5b inbox.md: Never list pushed past line 25 | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D6 SKILL.md Showing messages: 'the CLI renders' removed (line 169 only) | plugins/claude-code/skills/owlpost/SKILL.md | SURVIVED |  |
| D7 SKILL.md Framed message: 'the CLI renders' removed (line 193 only) | plugins/claude-code/skills/owlpost/SKILL.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D8 design §9 show row without --format claude | docs/technical-design.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D9 design §9: markers.json renamed | docs/technical-design.md | NOAPPLY | 1 |
| D10 design §1: chrono justification removed | docs/technical-design.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D11 inbox.md step 5: paste → show | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D12 inbox.md: 'never paraphrase' bullet removed | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D13 inbox.md: 'never a Markdown table for a single question' removed | plugins/claude-code/commands/inbox.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D14 SKILL.md: markers.json mention removed | plugins/claude-code/skills/owlpost/SKILL.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes |
| D9 (by hand, `mutants-by-hand.txt`) design §9 line 384 `markers.json` → `marker-map.json` | docs/technical-design.md | RED | docs: format_claude_docs_pin_the_cli_renders_the_model_pastes (tests/docs.rs:594) |
| M46 (by hand) inbox listing: `--format claude` checked before `--json` | src/cli/inbox.rs | SURVIVED | (no test runs `owl inbox --json --format claude`) |
| M17 re-examined | src/cli/show.rs | EQUIVALENT | the `if json {…} else if framed` order inside the loop (show.rs:86/91) already gives `--json` precedence; the `!json &&` in `framed` is redundant |


### test-reviewer verdict

**GAPS** (independent read-only audit, no cargo). Confirmed by the sweep: (1) table sort untested — fixture order equals sort order → M10b survives; (2) `≤60 chars` truncation untested → M26 survives; (3) `rec.meta["in_reply_to"]` fallback dead in tests and unreachable in production → M25 survives; (5) `find_question` only proves `done/` → M24 survives; (6) drafted question in the listing → M41 survives; (7) `inbox --json --format claude` precedence → M46 survives; (8) SKILL.md "Showing messages" pin satisfied by the nested "Framed message" subsection → D6 survives. Not confirmed as survivors but flagged: (4) orphan answer inside the TABLE through the binary (`↳ <short> ""` / `↳ - ""`) is unit-only; (9) three pins violate the OWL-022 single-line rule — `"`<marker> ↳ <question id short> \"<first line of the"` and `"`chrono` (`clock`, no default"` are wrap-point fragments in `tests/docs.rs`, and `drafts.contains("`draft:` with the draft in a\n   plain code block")` in `tests/plugin.rs` contains a newline (false RED on reflow). The reviewer judged the lowercased `contains` loop in `tests/plugin.rs` redundant, not weaker (each step also has an exact-case pin). Everything else was judged solid: exact-equality pins through the real binary for AC1–AC4/AC6, negative twins for plain/`--json`/unknown format, the marker-order swap, the window-only case, `older_line` counts, `\r\n` cell, four-backtick fence, marker wrap, unparseable `markers.json`.

### Rulings on the implementer's judgment calls

1. **Short question id = last 8 chars of the UUIDv7** — PASS. `e8-fixture.txt`: all twelve question ids share the first 8 chars (`01a07d58`, the ms timestamp); the last 8 are distinct. The first 8 would be useless as a short id; §9 documents the choice.
2. **Trailing line breaks dropped inside the fence / cells** — PASS (with a note). `e6-trailing-newlines.txt`: `"ends with one newline\n"` renders `…\nends with one newline\n```` — the text's own terminating `\n` is exactly the newline before the closing fence, so the fenced content IS byte-identical; `"ends with two\n\n"` loses one trailing empty line (a blank line before the closing fence would render as trailing whitespace the reader cannot tell from the fence); interior blank lines are kept (`inner\n\nblank kept`). Content lines are byte-identical in every case; only trailing whitespace is normalised; §9 states it ("trailing line breaks dropped"). Not a FAIL: the rule protects the words, and no fix would make a trailing `\n\n` visible without producing a stray blank line.
3. **Fence = longest backtick run + 1, min 3** — PASS. Superset of the AC ("three → four", `e5-show-backticks.md` shows four both ends); the unit test also pins four ticks → five, so a text quoting a four-backtick fence still renders.
4. **inbox.md step 5 keeps the words "Showing messages"** — PASS. `ac5-greps.txt`: the phrase occurs once, as the *name* of the table (`The output is the "Showing messages" table of the answers …`), not as a pointer to another file; `see the skill` = 0, `see the owlpost skill` = 0. The only remaining reference to the skill (line 103) is for the memory rule, not a format.
5. **`--json` wins over `--format` in `owl show`** — PASS. `e1-show-json-wins.json` is the JSON record; consistent with the pre-existing `owl inbox` precedence (`opts.json` checked first) and documented in §9. Note: `--format nope --json` still exits 1 (`unknown --format nope`) because the value is validated before the branch — a usage error, acceptable.

### Notes (not ACs)
- test-planner matrix: 70 rows (44 bin / 14 unit / 8 docs / 1 manual / 2 can't-induce); GAP rows beyond the ones above that are worth a look but block nothing: `show all --format claude` on an empty spool and on an unknown id (regression twins), `auto_error` note suppressed in the rendered listing, pre-seeded `markers.json` winning over appearance order, 2-backtick fence, exact-10 / 11-in-window cap-only cases, same-second tiebreak (dead: equal unix ⇒ equal string).
- PM-REVIEW-shaped observations from the planner, none an AC: an unparseable `received_at` on an answer is silently counted as "older"; when every answer is older than 24 h the listing prints nothing at all (no `older` line) — arguably fine (empty listing) but worth a conscious decision; `show --json --format nope` exits 1 although `--json` "wins".
- Plain output unchanged vs `main`: `git diff --numstat main...HEAD` shows `tests/inbox.rs` 402/0 (only appended), and the pre-existing pins of main's plain `show`/`inbox`/hook output (`show_prints_content_and_marks_seen`, `listing_marks_seen`, `count_and_formats`) pass unchanged; `e1-show-plain.txt` == `e1-show-noformat.txt`, `e7-inbox-plain.txt` == `e7-inbox-noformat.txt`. The plain branches in `show.rs`/`inbox.rs` are untouched by the diff (only an `else if` inserted).
- Markdown sanity (`md-fence-parity.txt`): inbox.md, SKILL.md and the design doc are fence-balanced; the SKILL.md example holding a ```text block sits in a four-backtick outer fence (lines 184–191); the new §9 prose has no fenced blocks.
- `Etc/GMT-9` exists on this machine (`/usr/share/zoneinfo/Etc/GMT-9`); the AC1 local-time test depends on tzdata being installed wherever the suite runs.

### Round 2 — 2c35a5a

Scope: AC2, AC5, the nine round-1 test-reviewer GAPS, AC8. Head `2c35a5a` (6b1f446 fixes + 2c35a5a drops the redundant `!json` guard), worktree clean before and after. `git diff 6770c62..2c35a5a -- src/` (`r2-src-diff.patch`): exactly two app-code hunks — the `rec.meta["in_reply_to"]` fallback deleted in `print_rendered` (`grep -n in_reply_to src/cli/inbox.rs` → one line, `payload.in_reply_to.clone()`) and `framed` in `show.rs` no longer `!json &&` (the `if json {…} else if framed` order carries the precedence, now pinned by R5). `src/render.rs` changed in its `#[cfg(test)]` module only. AC1/AC3/AC4 paths untouched; their tests re-ran green in the gates.

| AC | modality | verdict | evidence |
|---|---|---|---|
| AC2 | test | FAIL (behaviour PASS; one narrow test gap left) | Round-1 survivors closed: R1 sort deleted → RED (unit rotated rows; e2e spooled `[5,0,11,3,8,1,10,2,7,4,9,6]`), R2 `take(60)`→600 → RED (70- and 60-char first lines, unit + e2e). New survivor R2b: `.chars().take(60)` → `bytes().take(60)` + lossy survives because every ≥60-char fixture is ASCII; the AC literal is "≤60 **chars**" and the product's input is Polish. Fix: use `"ł".repeat(70)` / pin `"ł".repeat(60)` in `reference_line_keeps_the_first_60_chars_of_the_first_line` and in the `refs3` e2e block |
| AC5 | test | PASS | `md_section` now stops at the next heading of any level (guarded by `!showing.contains("### Framed message")`); R3a (line 169 copy removed) → RED and R3b (line 193 copy removed) → RED, each on its own distinct one-line pin. Reflowed pins single-line and effective: R9a §9 `↳ … ≤60 chars` literal (line 381) → RED, R9b §1 chrono line (line 19) → RED, R9c inbox.md step-4 sentence (line 81) → RED (`tests/plugin.rs`). All round-1 AC5 greps unchanged |
| AC8 | data | PASS | `r2-gates.txt` on 2c35a5a: build rc=0, fmt rc=0, clippy `-D warnings` rc=0, `cargo test --workspace --no-fail-fast` rc=0 (26 binaries ok, 0 failed; new `reference_line_keeps_the_first_60_chars_of_the_first_line` listed); Cargo.lock still adds 0 `[[package]]` |

Round-2 mutants (`r2-mutants.md`, `r2-mutants.log`; per-mutant `timeout 300`, restore + `touch`, green re-run afterwards): **15 applied, 13 RED, 2 SURVIVED.**

| # | mutant | verdict |
|---|---|---|
| R1 | sort deleted | RED (unit) |
| R2 | `take(60)`→600 | RED (unit `reference_line_keeps_the_first_60_chars…`) |
| R2b | bytes-based cut instead of chars | **SURVIVED** (ASCII-only fixtures) |
| R3a / R3b | SKILL.md `the CLI renders` removed from Showing / from Framed, independently | RED / RED |
| R4 | inbox listing `--format claude` before `--json` | RED (`inbox_format_claude_prints_blocks_then_table_and_nothing_else`) |
| R5 | show.rs `if json {` → `if json && !framed {` | RED (`show_format_claude_prints_the_framed_block`) |
| R6a / R6b | `find_question` without `asks/` / without `inbox/` | RED / RED (`show_format_claude_frames_an_answer`) |
| R7 | meta fallback | gone (grep) |
| R8 | listing passes `None` as the draft | RED |
| R9a / R9b / R9c | §9 `↳` literal, §1 chrono line, inbox.md step-4 sentence mutated | RED / RED / RED |
| R10 | orphan answer in the listing gets `"?"` instead of `""` | **SURVIVED** (no listing e2e with an orphan; only the unit test sees it) |
| R11 | rendered listing does not mark seen (re-check after the `--json` run was added before the rendered run) | RED (AC2 + AC3 tests) |

test-reviewer round 2: **GAPS** (2) — (A) `show_format_claude_frames_an_answer` spools the orphan after its only listing run, so no e2e asserts `↳ <short> ""` / `| Orphan. |` in the table (= R10; fix: bring the orphan into the window, run `inbox --format claude`, assert both); (B) the ≤60 cut is proven with ASCII only (= R2b; fix above). Reviewer note without a survivor: in the AC3 test the `--json --format claude` run now precedes the rendered run and both branches mark seen, so the trailing `seen` assertion no longer proves the rendered listing marks seen — R11 is still RED via the AC2 test (12/12 seen), so no gap, but moving the json block after the `seen` assertion would restore the AC3 proof. Eight of the nine round-1 gaps confirmed closed; gap (4) (orphan in the table through the binary) is the one that remains.

Round-1 rows carried forward: AC1, AC3, AC4, AC6 PASS (r1); AC7 PM-REVIEW (r1 steps).

### Round 3 — 8742fcc

Scope: AC2, AC8, test-reviewer on the round-3 diff. Head `8742fcc` (one commit on 2c35a5a), worktree clean before and after. `git diff 2c35a5a..8742fcc -- src/` touches `src/render.rs` only inside `mod tests` (hunk `@@ -494 … mod tests {`: the `ł` fixtures); `tests/inbox.rs` adds the Polish 70/60-char rows, the orphan answer in the AC2 `h3` listing, the `!seen` pre-assert in the AC3 test and moves the `--json --format claude` check to the `e` home.

| AC | modality | verdict | evidence |
|---|---|---|---|
| AC2 | test | PASS | `r3-mutants.md`: R3-1 `bytes().take(60)`+lossy → RED, R3-2 `&s[..s.len().min(60)]` → RED (unit `reference_line_keeps_the_first_60_chars_of_the_first_line`; the e2e `refs3` block pins `"ł"×60` + a byte-length check too), R3-3 orphan `""`→`"?"` in the listing → RED (`inbox_format_claude_prints_the_answers_table`, `🟩 ↳ <short> ""` + `| … Maciek | lost |`), R3-4 rendered listing skips `mark_seen` → RED (AC3 test pre-asserts unseen; AC2 too), R3-5 `--format claude` before `--json` → RED. Round-1/2 behaviour evidence (`e8-*`) unchanged (src untouched) |
| AC8 | data | PASS (one pre-existing timing flake noted) | `r3-gates.txt` on 8742fcc: build rc=0, fmt rc=0, clippy `-D warnings` rc=0; `cargo test --workspace --no-fail-fast` ×3 (`r3-suite-1/2/3.log`): run 1 515/0, run 2 514/1, run 3 515/0. The run-2 failure is `session_start_registers_the_watch_path_unless_plugin_json_says_off` (`tests/inbox.rs:506`): it asserts two consecutive plain listings equal and the `AGE` column ticked `0s`→`1s` between them (log lines 416–421). Pre-existing: the assertion is from OWL-029 (`239a83c`, 2026-09-06, on `main`), the test is byte-identical on this branch (`git diff main...HEAD` has no hunk in it) and the plain listing path is untouched by OWL-032 — a `main` timing flake, not a FAIL of this task; worth a follow-up (compare with `AGE` masked or freeze `received_at`). Cargo.lock still +0 packages |

Round-3 mutants: **6 applied, 6 RED** (`r3-mutants.md`, `r3-mutants.log`; per-mutant `timeout 300`, restore + `touch`, green re-run afterwards). R3-6 — the round-3 reviewer's predicted survivor (markers assigned/written above the `if opts.json` branch, so `markers.json` is written even under `--json`) — is RED, but on marker *order* (`r3-mutant-R3-6-detail.txt`: spool order gives Maciek 🟩 in `inbox_format_claude_prints_the_answers_table`), not on the `--json` side effect itself.

test-reviewer round 3: **GAPS (1)** — round-2 gaps (A) orphan-in-listing and (B) non-ASCII cut confirmed closed (both mutant variants RED in unit and e2e); the one remaining item: the `!markers.json.exists()` clause of the relocated `--json --format claude` check lives in home `e`, which holds one question and no answer, so `Markers` is never dirty and the clause holds on every code path (the JSON-array and no-`FRAME` clauses stay discriminating and kill R3-5). Verifier ruling: **note, not AC-blocking** — no AC names `--json`, the precedence itself is proven, and the only mutant that makes the clause matter (a `--json`-only side effect that preserves shown-order markers) would have to duplicate `answers_table`. One-line fix if wanted: put one `put_answer` into `e` before that check (or run it in `h3`).

Round-1/2 rows carried forward: AC1, AC3, AC4, AC6 PASS (r1); AC5 PASS (r2); AC7 PM-REVIEW (r1 steps).

### Resolution
- Status: done (rounds 1–3; FAILING: none; PM-REVIEW: AC7 manual). Draft PR: https://github.com/Krab00/owlpost/pull/43 (never merged or marked ready by the loop).
