---
id: OWL-006
title: Daemon HTTP API — agent card, questions, outbox, ack, rate limit, replay
status: done
deps: [OWL-003, OWL-004, OWL-005]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `server.rs` and the listener half of `daemon.rs` per `docs/technical-design.md §7`
and `docs/architecture.md §3.2`. axum router served by axum-server with the OWL-005 rustls
config; a connection-level extension carries the peer fingerprint (or none for unpinned card
requests). Routes: `GET /.well-known/agent-card.json` and `/.well-known/agent.json` (A2A-shaped
card per §7, `owlpost.responds` derived from `config.responder.enabled`); `POST /v1/questions`
(verify body signature against the pinned peer key, schema check, replay window, per-peer token
bucket, policy lookup → 403 `unavailable` for `never` or responder disabled, responder cache hit
→ 200 with the cached answer envelope, else store inbox record with state `consent` (no policy)
or `pending` and return 202); `GET /v1/outbox` (answers whose `to` equals the caller);
`POST /v1/outbox/{id}/ack` (move to done; 404 if not the caller's). Every malformed input is a
4xx with an `error` field naming the problem. `owl daemon --foreground` runs the server on
`config.listen` (port 0 allowed for tests; the bound address is logged and written to
`$OWLPOST_HOME/daemon.addr`). Auto-accept scheduling and the pull loop are later tasks; leave
clearly named hooks for them.

## Acceptance criteria
- [ ] AC1: integration test `tests/server.rs::card_is_served_unpinned_and_pinned` fetches both well-known paths without a client cert and with a pinned client, and asserts `owlpost.fingerprint`, `owlpost.protocol == 1`, `capabilities.streaming == false` (modality: test)
- [ ] AC2: integration test `tests/server.rs::question_is_accepted_and_spooled` posts a signed question from peer A (in B's book, policy manual) and asserts 202 with the id, and an inbox record with state `pending` exists in B's spool (modality: test)
- [ ] AC3: integration test `tests/server.rs::no_policy_means_consent` posts from a peer without a policy overlay and asserts the record state is `consent` (modality: test)
- [ ] AC4: integration test `tests/server.rs::never_and_disabled_return_unavailable` asserts 403 with `{"error":"unavailable"}` for a peer with mode never and for any peer when `responder.enabled == false`, and that no inbox record is created (modality: test)
- [ ] AC5: integration test `tests/server.rs::bad_inputs_are_4xx_never_500` covers: wrong signature (400), body signed by A but `from` claims C (400), stale ts (400), duplicate id (409), non-JSON body (400), missing `body.question` (400) — each response names the problem in `error` (modality: api)
- [ ] AC6: integration test `tests/server.rs::rate_limit_trips` with a contact `rate_limit_per_hour = 3` asserts the 4th question within a minute returns 429 with a `Retry-After` header (modality: test)
- [ ] AC7: integration test `tests/server.rs::cache_hit_returns_answer` pre-populates B's responder cache for the question hash and asserts the POST returns 200 with a verifiable answer envelope from B (modality: test)
- [ ] AC8: integration test `tests/server.rs::outbox_is_per_caller_and_ack_moves_to_done` puts answers addressed to A and to C in B's outbox; A's GET lists only its own; ack of A's id returns 204 and the record is in `done/`; ack of C's id by A returns 404 (modality: test)
- [ ] AC9: `owl --home <tmp> daemon --foreground` with `listen = 127.0.0.1:0` writes `daemon.addr` within 2 s and serves the card at that address (modality: test)

## Verification — task/OWL-006-server
Round 1 · head `67668a5` · verify worktree `.worktrees/verify-OWL-006` (detached, removed after) · evidence `.orch/tasks/OWL-006.evidence/`

| Criterion | Verdict | Evidence |
|---|---|---|
| Standing gates (build, test ×2, clippy -D warnings, fmt) | PASS | `build.txt`, `test-run1.txt`, `test-run2.txt` (113 tests, both runs green, stable), `clippy.txt`, `fmt.txt` (exit 0) |
| §1 crates / crypto provider | PASS | Cargo.toml adds tokio, axum 0.8, axum-server 0.8 (`tls-rustls-no-provider`), reqwest 0.13 (`rustls-no-provider`, json), tracing(+subscriber); `tower` justified inline (Layer trait). Cargo.lock: `ring` present, zero `aws-lc` entries, no native-tls/openssl |
| §12 test rules (offline, port 0, own home) | PASS | grep: no hard-coded ports outside pure-fs unit tests, no `~/.config`/`dirs::`/`HOME`; every integration test uses `tempfile` + `listen = 127.0.0.1:0` |
| AC1 card served unpinned + pinned, fields per §7 | FAIL | `card_is_served_unpinned_and_pinned` passes; all §7 fields present. Mutant M17 (`owlpost.harness` hard-coded to `"claude"`) SURVIVED — harness value never pinned to config. Zero-contact unpinned card probed OK (200) but untested (`mutant-sweep.md`) |
| AC2 signed question → 202, inbox `pending` | PASS | `question_is_accepted_and_spooled`; M08/M29/M30 killed (state, per-POST contact re-read, byte-identical raw). Advisory: M16 (`Spooled.auto` flag for the OWL-008 hook) survived — add a unit test on the channel event |
| AC3 no policy → `consent` | PASS | `no_policy_means_consent`; M08 killed |
| AC4 403 `unavailable` for never / responder disabled, no record | PASS | `never_and_disabled_return_unavailable`; M04, M05 killed |
| AC5 bad inputs 4xx never 500, each names the problem | FAIL | `bad_inputs_are_4xx_never_500` covers all 6 named cases plus 17 schema cases; M01/M02/M03/M11/M19/M20/M21/M28 killed. Mutant M14 (`SeenIds::load` → empty set; §6 requires ids persisted in `seen-ids.txt`) SURVIVED — duplicate id after daemon restart untested. Duplicate id across peers probed OK (409) but untested |
| AC6 rate limit 429 + `Retry-After` | FAIL | `rate_limit_trips` passes; M09/M10 killed. SURVIVED: M15 (global `rate_limit_per_peer_per_hour` default ignored — every limited peer has an explicit policy rate), M18 (`Retry-After` `floor` vs `ceil`; test only asserts `1..=1200`, should be `== 1200`), M23 (policy checked before rate limit, §3.2 step order inverted — a `never` peer never trips 429) |
| AC7 cache hit → 200 signed answer + header | FAIL | `cache_hit_returns_answer` matches §7 (raw payload body, `X-Owl-Signature` header, verifies with B's key); M24 killed. Mutant M22 (cache lookup gated on a policy being present) SURVIVED — cache × policy matrix has only the `manual` row. See PM-REVIEW below for the `none` row semantics |
| AC8 outbox per caller, ack → done, foreign → 404 | FAIL | `outbox_is_per_caller_and_ack_moves_to_done`; M06/M07/M25 killed. Mutant M27 (kind filter removed: non-answer records addressed to the caller get listed) SURVIVED — the stray record in the test is addressed to B, so it never discriminates |
| AC9 `owl --home <tmp> daemon --foreground` writes `daemon.addr` ≤ 2 s and serves the card | PASS | `daemon_foreground_writes_addr_and_serves_card` spawns the real binary via `CARGO_BIN_EXE_owl`, polls ≤ 2 s, asserts loopback, port ≠ 0, no `.tmp`, card fingerprint/protocol, pinned `/v1/outbox` 200, exit 1 without identity; `Child` Drop kills + waits; `pgrep` after the suite → no orphan. M13 killed by AC9 |
| Check-order claims (nothing written before checks; rate limit before policy; contacts re-read per POST; TLS key set startup snapshot with ponytail; `owl daemon` without flag runs attached with ponytail) | PASS (code) | `src/server.rs:306-424` order matches; `remember`/`spool.put` only after all checks; ponytail comments at `src/server.rs:106`, `src/main.rs` `daemon_cmd`. Rate-limit-before-policy is code-true but not test-pinned (M23) |
| PM-REVIEW 1 | PM-REVIEW | Cache hit returns the answer to a peer with **no policy** (not yet consented). Task file orders cache before consent; `docs/architecture.md §3.2` lists the policy step (no policy → `consent`) before the cache step. Decide whether an unconsented peer may receive cached answers; the AC7 matrix test must pin the decision |
| PM-REVIEW 2 | PM-REVIEW | axum defaults: unknown route 404, wrong method 405, body > 2 MiB 413 return empty bodies with no `error` field. §7 says every 4xx names the problem; decide whether these count as "malformed input" (tests assert only the 404 status) |
| PM-REVIEW 3 | PM-REVIEW | Storage edge states: ack sets state `acked` then moves; a failed move leaves an `acked` record in outbox (still listed). One corrupt JSON file in `outbox/` makes `GET /v1/outbox` 500 for everyone. Low priority |

FAILING: AC1, AC5, AC6, AC7, AC8 (all test gaps; no behaviour bug found — every mutant survivor was verified correct by probe or code reading).

## Verification — task/OWL-006-server (round 2)
Head `0e9fe33` · fresh detached verify worktree (removed after) · evidence `r2-*` files in `.orch/tasks/OWL-006.evidence/`. Diff since `67668a5` touches only src/server.rs, tests/common/mod.rs, tests/server.rs and the task Description; unchanged rows from round 1 stand.

| Criterion | Verdict | Evidence |
|---|---|---|
| Standing gates | PASS | `r2-build.txt`; `cargo test --lib` 38 passed; `cargo test --test server` 9 passed, twice (`r2-test-server-run1/2.txt`); `cargo test --workspace` all green (`r2-test-workspace.txt`); clippy -D warnings and fmt clean; `r2-orphans.txt` empty |
| Task file edit | PASS | Only the Description sentence changed (`git diff 67668a5..0e9fe33 -- .orch/tasks/OWL-006.md`): no policy → `consent` + 202, never a cached answer; `never` → 403; manual/auto → cache hit 200 else `pending` 202; "policy before cache, per architecture §3.2". Frontmatter and AC1..AC9 untouched |
| AC1 card | PASS | harness/name/endpoints echo test (`codex`, "Cody"); zero-contact daemon serves unpinned card and refuses a pinned cert; R1-M17 KILLED |
| AC5 4xx never 500 | PASS | cross-peer duplicate → 409; restart on same home via `respawn` → 409, new id 202; 404/405/413 are JSON `{"error"}`; R1-M14, R2-N1..N4 KILLED |
| AC6 rate limit | PASS | `Retry-After == 1200` exact; `never` peer with rate 3 → 3×403 then 429 (rate limit before policy); global `rate_limit_per_peer_per_hour = 2` → 429 with `Retry-After 1800` for a policy-less and a manual peer; unit `take(3,0.5)==Err(1200)`; R1-M15, M18, M23 KILLED |
| AC7 cache hit | PASS | four-row matrix on one cached hash: none → 202 `consent`, never → 403, manual/auto → 200 with B's signature; unit `may_read_cache`; R1-M22, M22b KILLED |
| AC8 outbox / ack | PASS | stray is a signed *question* addressed to A: not listed, ack → 404; corrupt `outbox/corrupt.json` skipped in listing, ack → 404; blocked `done/` target → 500 and record stays `unacked`; R1-M27, R2-N5..N7, N9 KILLED |
| PM-REVIEW 1 (policy before cache) | PASS | implemented via `may_read_cache`, tested (AC7 row) |
| PM-REVIEW 2 (JSON 404/405/413) | PASS | `.fallback`, `.method_not_allowed_fallback`, `DefaultBodyLimit` 64 KiB + `body_bytes` mapping; tested with pinned and unpinned clients |
| PM-REVIEW 3 (ack order, corrupt outbox) | PASS | move-then-set_state; lenient lister with warn; both tested |
| Advisory | — | R2-N8 (`.json` extension filter in `list_lenient`) survives; harmless, non-json files fail the parse step and are skipped anyway. R1-M16 (`Spooled.auto`) now killed by the `record_state` unit test |

FAILING: (none)

Draft PR: https://github.com/Krab00/owlpost/pull/8
