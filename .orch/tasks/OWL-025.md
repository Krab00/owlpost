---
id: OWL-025
title: Plugin: register owl mcp at user scope (no plugin .mcp.json), @owl: typing hint, doctor check
status: done
deps: [OWL-024]
source: adhoc
source_updated_at: 2026-09-05
criteria_source: derived
---
## Description
Follow-up to OWL-024, from the manual smoke on 2026-09-05 (AC9 of OWL-024). Findings:

- A server bundled through the plugin's `.mcp.json` is named `plugin:owlpost:owl` by Claude
  Code, so the mention reads `@plugin:owlpost:owl:to://…` and the 16-character prefix sinks
  the resource in the `@` typeahead ranking: `@krz` never showed it, `@owl` ranked it third.
- The same server registered at user scope (`claude mcp add --scope user owl -- owl mcp`)
  is named `owl`: `@owl:krz` ranks the contact near the top, `@owl:` alone lists the book.
  That is the accepted UX: type `@owl:` and the start of the name (or e-mail/domain), pick
  with the arrows, type the question. `@krz` alone will never beat files/connectors — that
  ranking is Claude Code's.

Changes (all on the OWL-024 branch `task/OWL-024-mcp-contacts`, so PR #30 updates):

- **Remove `plugins/claude-code/.mcp.json`.** The plugin no longer bundles the server.
- **`owl setup`** (after the plugin steps, only when `claude` is on PATH): if
  `claude mcp get owl` fails, run `claude mcp add --scope user owl -- owl mcp`; if it
  succeeds print `mcp server owl already registered`. `--dry-run` prints
  `would run: claude mcp add --scope user owl -- owl mcp` (or the already-registered line).
- **`owl update`**: the same idempotent registration step, after its plugin steps, same
  dry-run wording.
- **`owl doctor`**: a new check `mcp` after the harness checks: `ok` when
  `claude mcp get owl` exits 0 (`owl registered in Claude Code (user scope)`); `warn` when
  `claude` is on PATH but the server is missing, detail ending with the fix
  `claude mcp add --scope user owl -- owl mcp`; `warn` `claude not on PATH` otherwise. Never
  `fail` (owlpost works without Claude Code).
- **Docs**: `commands/contacts.md` hint becomes the single line
  `Type @owl: and the start of the name, pick the contact, then type the question.`
  (keep `@owl:to://` as the shape of the inserted mention); README "Mention a contact"
  rewritten: `@owl:krz` example, registration by `owl setup`/`owl update` (or the
  `claude mcp add` line by hand), `owl doctor` check, no `.mcp.json`; the README components
  table row "MCP server" points at `owl mcp` + `owl setup`; SKILL.md "Mentioned contact"
  gains the `@owl:` typing hint and drops the `.mcp.json` note; design doc §9 `owl mcp` row
  and §11 (service install / setup) mention the user-scope registration and the doctor check.
- Tests use a fake `claude` script on PATH that logs argv (as the plugin steps are tested);
  `claude mcp get owl` exit status is scripted per case.

Out of scope: changing the URI shape, re-ranking inside Claude Code, Codex/Kimi.

## Acceptance criteria
- [ ] AC1: `plugins/claude-code/.mcp.json` does not exist; `tests/plugin.rs` asserts its absence and that README, SKILL.md and `docs/technical-design.md` no longer say `.mcp.json` for the plugin (modality: test)
- [ ] AC2: `owl setup` with a fake `claude` on PATH runs `mcp add --scope user owl -- owl mcp` after the plugin steps when `mcp get owl` fails, and only prints `mcp server owl already registered` when it succeeds; `--dry-run` prints `would run: claude mcp add --scope user owl -- owl mcp` (or the already-registered line) and runs nothing — argv log asserted (modality: test)
- [ ] AC3: `owl update` does the same registration step idempotently, same dry-run wording — argv log asserted (modality: test)
- [ ] AC4: `owl doctor` prints an `mcp` check: `ok   mcp: owl registered in Claude Code (user scope)` when `claude mcp get owl` exits 0; `warn` with the detail ending `claude mcp add --scope user owl -- owl mcp` when it exits non-zero; `warn mcp: claude not on PATH` without `claude`; never `fail`; JSON output carries the same check — `tests/doctor.rs` rows for all three (modality: test)
- [ ] AC5: `commands/contacts.md` contains the single-line hint `Type @owl: and the start of the name` and still `@owl:to://`; README "Mention a contact" contains `@owl:krz`, `owl setup`, `claude mcp add --scope user owl -- owl mcp` and not `.mcp.json`; SKILL.md "Mentioned contact" contains `@owl:` typing hint text `type @owl: and the start of the name`; design doc §9/§11 contain `claude mcp add --scope user owl -- owl mcp`; `tests/plugin.rs`/`tests/docs.rs` pin each literal on one line (modality: test)
- [ ] AC6: manual (done 2026-09-05 by the user, record only): with the user-scope server, `@owl:krz` ranks the contact near the top and `@owl:` lists the book; the plugin-bundled server ranked it third or lower (modality: manual, PM-REVIEW)
- [ ] AC7: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-024-mcp-contacts (OWL-025)

Round 1, 2026-09-06. Verify worktree `.worktrees/verify-OWL-025` at `505a023` (detached), removed afterwards. Evidence: `.orch/tasks/OWL-025.evidence/`.

| AC | Verdict | Evidence |
|---|---|---|
| AC1 `.mcp.json` gone, docs say nothing about it | PASS | `git ls-tree 505a023 plugins/claude-code` has no `.mcp.json`; `tests/plugin.rs::plugin_bundles_no_mcp_json` green; mutants D06/D08/D12/D13 (implementer) RED. Note: a server re-bundled through `.claude-plugin/plugin.json` `"mcpServers"` is NOT caught (verifier mutant V06 SURVIVED) — AC1 as written holds, hardening suggested. |
| AC2 `owl setup` registration, argv log, dry-run wording | PASS | `tests/setup.rs` 3 setup rows green; manual `setup-dry-{reg,unreg}.out` (calls.log = `claude --version`, `claude mcp get owl` only); mutants V01/V02 + C01/C02/C05–C10 RED. |
| AC3 `owl update` same step, idempotent | PASS | `tests/setup.rs` 3 update rows green; manual `update-dry-{reg,unreg}.out`; mutants V03 (update-only scope drop), C03/C04/C19 RED. |
| AC4 `owl doctor` `mcp` check, three states, never fail, JSON | FAIL (test gap; behaviour correct) | Behaviour verified by hand: `doctor-{reg,unreg,empty}.out` and `.json` show `ok   mcp: owl registered in Claude Code (user scope)`, `warn mcp: owl not registered in Claude Code; run claude mcp add --scope user owl -- owl mcp`, `warn mcp: claude not on PATH`; mcp warn not counted in `N check(s) failed`. Test-reviewer gap: `tests/doctor.rs` asserts `--json` only for the registered (status) and unregistered (status+detail) states; the no-`claude` state has no `--json` row, AC4 asks for "rows for all three". |
| AC5 doc literals pinned on one line | FAIL (minor) | All literals present and pinned (`tests/plugin.rs`; mutants V05/V08/D02/D11 RED on wrapped or dropped lines). Test-reviewer gap: `mcp server owl already registered` is stated three times (README Mention, §9 `owl mcp` row, §11) but pinned only in README and §11 — the §9 row needle list at `tests/plugin.rs` `design_doc_lists_owl_mcp` lacks it (rule: stated twice → pinned twice, or state once). |
| AC6 typeahead ranking (manual) | PM-REVIEW | Done 2026-09-05 by the user per the task file: `@owl:krz` near the top and `@owl:` lists the book with the user-scope server; plugin-bundled server ranked third or lower. Record only. |
| AC7 build / test / clippy / fmt, no new crate | PASS | `ac7-toolchain.txt`; `suite-final.log`: 25 suites, 454 passed, 0 failed on the clean tree; `Cargo.toml`/`Cargo.lock` untouched. |

FAILING: AC4 (missing `--json` row for the no-`claude` state in `tests/doctor.rs`), AC5 (`mcp server owl already registered` unpinned in the §9 `owl mcp` row needles).

**test-reviewer**: GAPS. (1) AC4 — `tests/doctor.rs:206-217` checks only the text line for the empty-PATH run; add a `--json doctor` run asserting `{"check":"mcp","status":"warn","detail":"claude not on PATH"}` (and optionally pin the ok-state JSON detail). (2) AC5 — `docs/technical-design.md:300` §9 row says `mcp server owl already registered`; `tests/plugin.rs` §9 needles (`design_doc_lists_owl_mcp`) do not pin it, README and §11 do. Non-blocking notes: dry-run tests fold registered+unregistered into one `#[test]` each; `no_claude_on_path_skips_the_registration` doc-comment says "both modes" but runs `--dry-run` only; `mcp_check` `Err(other)` arm untested; `commands/setup.md`/`update.md` sentences unpinned (not AC-required). Fake `claude` logs full argv and tests compare the whole ordered log with `assert_eq!`, so argv order/spelling cannot be masked.

**test-planner**: 38 rows (19 happy / 19 edge), E2E 0, 2 can't-induce (AC6, real-`claude` smoke). Uncovered rows run by hand by the verifier, all consistent (`uncovered-rows.out`): `mcp add` exits 1 → setup/update bail `claude mcp add --scope user owl -- owl mcp failed (exit status: 1)`, exit 1 (rows 13/14); `claude` mode 0644 → setup treats as not on PATH, doctor `warn mcp: claude mcp get owl: Permission denied (os error 13)` (16/27); `mcp get` exit 2 → treated as not registered in doctor and setup (15/28); real-mode no-`claude` for setup/update skips the step, exit 0 (6/12); failed non-required `marketplace update` still reaches `mcp get`/`mcp add` (18); noisy `mcp get` output never leaks (29); doctor `--json` no-`claude` (24) in `doctor-empty.json`; mcp warn + real fail → count excludes mcp (26).

**Mutants (verifier, 10; implementer's 37 not re-run except the two that were SURVIVED in his log before `505a023`)** — `mutants-verifier.log`:
- RED V01 `mcp get` argv order swapped; V02 setup registers twice; V03 update-only `--scope user` dropped; V04 doctor check name `mcp`→`mcps` (text+JSON); V05 contacts.md hint wrapped onto two lines; V07 doctor mcp check before harness; V08 SKILL.md hint wrapped; D02 (re-run) contacts drops `@owl:to://`; D11 (re-run) §11 drops the add line.
- SURVIVED V06 `.claude-plugin/plugin.json` gains inline `"mcpServers": {"owl": …}` — the plugin would again bundle a `plugin:owlpost:owl` server and no test notices. Suggest asserting `plugin.json` has no `mcpServers` key in `plugin_bundles_no_mcp_json`.

## Verification round 2 — task/OWL-024-mcp-contacts (OWL-025), commit 6f9c0bb

2026-09-06. Test-only change on top of `505a023` (`tests/doctor.rs` +18, `tests/plugin.rs` +11). Fresh verify worktree at `6f9c0bb`, removed afterwards. Only AC4, AC5 and the V06 survivor re-checked.

| AC | Verdict | Evidence |
|---|---|---|
| AC4 doctor `mcp` check, JSON rows for all three states | PASS | `tests/doctor.rs` now runs `--json doctor` on the empty PATH and asserts `check: mcp`, `status: warn`, `detail == "claude not on PATH"` (exact equality). `cargo test --test doctor` green. Mutant R2-1 (detail `claude not on PATH` → `claude missing`) RED. |
| AC5 doc literals pinned, stated-twice rule | PASS | `design_doc_lists_owl_mcp` §9 `owl mcp` row needles now include `` `mcp server owl already registered` ``; the literal is pinned in README, §9 and §11. `cargo test --test plugin` green. Mutant R2-2 (§9 row drops the literal) RED. |
| AC1 hardening (V06 survivor) | PASS | `plugin_bundles_no_mcp_json` asserts `plugin.json` has no `mcpServers` at top level nor under `experimental`. Mutants R2-3a (top-level) and R2-3b (`experimental.mcpServers`) both RED. |
| AC7 (re-run) | PASS | `suite-r2.log`: 25 suites, 454 passed, 0 failed on the clean tree; clippy `-D warnings` and `fmt --check` clean. |

FAILING: none.

Mutants round 2 (`mutants-verifier-r2.log`): R2-1 RED, R2-2 RED, R2-3a RED, R2-3b RED. Note on R2-1: text and JSON share one `Check`, so no mutant can flip the JSON detail alone; the new assertion was read and is an exact-equality check on the JSON detail, not a `contains`.

Draft PR: https://github.com/Krab00/owlpost/pull/30 (shared with OWL-024; round 2 at 6f9c0bb: all PASS, AC6 PM-REVIEW recorded; 2636ec1 adds the user's message-table rule to the skill)
