---
id: OWL-012
title: OS notifications, `owl install/uninstall`, `owl watch`, `owl doctor`
status: done
deps: [OWL-006]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `notify.rs`, `cli/install.rs`, `cli/watch.rs`, `cli/doctor.rs` per
`docs/technical-design.md §9, §11`. `notify(kind, peer_name, path)` runs `osascript` on macOS
or `notify-send` on Linux when `config.notify` is true and the binary exists; the text never
contains the question or answer body; `OWLPOST_NOTIFY_CMD` overrides the command (tests point it
at a script that appends to a file). The daemon calls it on question arrival (OWL-006 hook) and
answer ingestion (OWL-008 hook). `owl install` writes `~/Library/LaunchAgents/dev.owlpost.owl.plist`
(macOS) or `~/.config/systemd/user/owlpost.service` (Linux) running `owl daemon` with the
current `--home`, loads/enables it, and prints status; `--dry-run` prints the unit to stdout
and writes nothing; `owl uninstall` reverses it. `owl watch [--id <id>] [--timeout <secs>]`
polls the spool every 500 ms and exits 0 printing the record id when a matching unseen inbox
record appears, exit 4 on timeout. `owl doctor` checks: key present, config parses, each
endpoint's host resolves, configured harness binaries are on PATH, daemon reachable at
`daemon.addr` (card fetch), last pull age; prints one line per check with ok/warn/fail and exits
1 if any check fails.

## Acceptance criteria
- [ ] AC1: integration test `tests/notify.rs::daemon_notifies_on_question_and_answer` sets `OWLPOST_NOTIFY_CMD` to a logging script and asserts one line per event containing the peer name and path but not the question text (modality: test)
- [ ] AC2: unit test `notify::tests::disabled_or_missing_binary_is_silent` asserts no error when `notify == false` or the command is absent (modality: test)
- [ ] AC3: `owl --home <tmp> install --dry-run` prints a plist on macOS / a systemd unit on Linux containing the absolute `owl` path, `daemon`, and `--home <tmp>`, and creates no file (modality: test)
- [ ] AC4: integration test `tests/watch.rs::watch_returns_on_arrival` starts `owl watch --timeout 10`, writes an inbox record after 1 s, and asserts exit 0 with the id on stdout within 3 s; `--id <other>` keeps waiting and exits 4 at the deadline (modality: test)
- [ ] AC5: integration test `tests/doctor.rs::doctor_reports_each_check` with a running daemon asserts exit 0 and lines for key, config, endpoints, harness, daemon, pull; with the daemon stopped asserts exit 1 and a `fail` on the daemon line (modality: test)
- [ ] AC6: `owl install` and `owl uninstall` are covered by a test that runs them with `HOME` pointed at a temp dir on the current OS and asserts the unit file is created and removed (loading may be skipped when `OWLPOST_INSTALL_NO_LOAD=1`) (modality: test)

## Verification — task/OWL-012-notify-install

Round 1, verified 2026-09-02 against `f0b1f97` (base `main` e2a9008) in a detached worktree.
Evidence: `.orch/tasks/OWL-012.evidence/` (gate-*.log, diff.patch, watch-3x.log, mutants.log,
mutants.py, notifier-calls-whole-suite.txt, home-before/after.txt).

| Check | Verdict | Evidence |
|---|---|---|
| build / fmt / clippy -D warnings / `cargo test --workspace` | PASS | gate-*.log, all exit 0; 176 tests, 0 failed |
| AC1 notify on question + answer, no body in text | FAIL (test gaps) | `tests/notify.rs::daemon_notifies_on_question_and_answer` passes and kills body-leak (M01), kind-swap (M04/M26), dropped answer hook (M27). Survivors: M19 `question_path` fallback `"?"` → `""`, M20 `peer_name` empty-name filter removed — both untested branches in `src/daemon.rs` |
| AC2 disabled / missing binary silent | PASS | `notify::tests::disabled_or_missing_binary_is_silent`; kills M02 (`config.notify` check removed), M23 (which-check removed), M03 (override ignored) |
| AC3 `install --dry-run` prints unit, writes nothing | PASS | `tests/install.rs::install_dry_run_prints_unit_and_writes_nothing`; kills M05 (`--home` dropped), M06 (dry-run writes), M21 (relative home not absolute), M29 (non-absolute owl path) |
| AC4 `watch` returns on arrival / `--id other` exits 4 | FAIL (test gap) | `tests/watch.rs::watch_returns_on_arrival` passes 3/3 loops and under full parallel suite (2 runs); kills M09 (marks seen), M10 (`--id` dropped), M11 (exit 4→1), M12 (`--timeout 0` never polls), M28 (main downcast), M30 (poll 5 s). Survivor: M18 — no `--timeout` branch (`None => sleep`) turned into immediate exit 4 is caught by nothing |
| AC5 `doctor` lines + exit codes | FAIL (test gap) | `tests/doctor.rs::doctor_reports_each_check`; kills M13 (daemon always ok), M14 (exit ignores fails), M15 (selected harness skipped), M17 (`>`→`>=`), M22, M24. Survivor: M16 — pull warn threshold `2 × pull_interval_secs` replaced by constant 120; `pull_check_ages` only tests the default interval |
| AC6 `install` / `uninstall` create + remove unit, NO_LOAD honoured | PASS | `tests/install.rs::install_then_uninstall_creates_and_removes_unit_file`; kills M07 (file left), M08 (NO_LOAD ignored — fake `systemctl` on PATH logged `daemon-reload` + `enable --now`, test failed on `load skipped`), M25 (wrong dir) |
| plist branch unit-tested on Linux | PASS | `cli::install::tests::plist_runs_owl_daemon_with_home` via pure `unit_text` |
| Real HOME untouched, no stray processes | PASS | full suite with `HOME=<scratch>`: `~/.config/systemd/user` listing identical before/after; 0 `owl watch`/`owl daemon` processes after; scratch HOME holds only cargo/rustup dirs |
| Test hygiene: no host side effects | FAIL | `tests/common::spawn_daemon_with` keeps `notify = true` with no `OWLPOST_NOTIFY_CMD`; running the suite with a logging notifier recorded 19 notifier calls outside `tests/notify.rs` (12 "Ana asks about…", 6 "Cat…", 1 "None…") — on this host that is 19 real `notify-send` desktop popups per `cargo test` (notifier-calls-whole-suite.txt) |
| `src/main.rs` diff scope | PASS | `mod cli;`, four match arms, `Err(e)` downcast only |
| Sibling branches vs `AppState::new` type change | PASS | OWL-007 and OWL-010 touch neither `src/server.rs` nor `src/daemon.rs`; no other `AppState::new`/`on_spooled` caller outside those files on main. (Both siblings do touch `src/cli/mod.rs` and `src/main.rs` → textual merge conflicts expected, not type breaks) |
| `tests/notify.rs` single test / env order | PASS | only reader of `OWLPOST_NOTIFY_CMD`; other binaries pass env per child `Command` |
| test-planner matrix | 95 rows, 27 NONE | largest uncovered block: `load`/`unload` (I22–I26, fake systemctl), W9/W10, Dr17, D4/D5/D7/D8 |
| test-reviewer | GAPS (4) | notify hygiene in common fixture; pull_check at non-default interval; watch without `--timeout`; `question_path` "?" fallback |

Mutant sweep: 30 mutants, 26 killed, 4 survived (M16, M18, M19, M20). Run as `cargo test --lib`,
`--bin owl`, and `--test <bin>` separately (mutants.log).

FAILING: AC1 (M19, M20 untested fallbacks), AC4 (M18 no-timeout branch), AC5 (M16 pull threshold at
non-default interval), standing hygiene (real `notify-send` popups from `tests/server.rs`).
Code behaviour: no defects found; all failures are test coverage / fixture hygiene.

Round 1 verdict: **FAIL**.

## Verification — task/OWL-012-notify-install (round 2)

Re-verified 2026-09-02 against `ee0cfba` (delta from `f0b1f97`: tests/fixtures only — the 47-line
`src/cli/doctor.rs` change is entirely inside `#[cfg(test)] mod tests`; no Cargo.toml/lock edit;
`tests/common/mod.rs` gains only `notify: false` + doc comment). Scope: AC1, AC4, AC5, notify
hygiene; other round-1 rows stand. Evidence: `.orch/tasks/OWL-012.evidence/round2/`.

| Check | Verdict | Evidence |
|---|---|---|
| build / fmt / clippy -D warnings / `cargo test --workspace` | PASS | gate-*.log, all exit 0; 181 tests, 0 failed |
| Per-binary runs (`--lib`, `--bin owl`, `--test notify/install/watch/doctor/cli/server`) | PASS | per-binary.log, all green |
| Notify hygiene | PASS | full suite with logging `OWLPOST_NOTIFY_CMD` + isolated HOME: no log file created → 0 notifier calls outside tests/notify.rs (was 19). `tests/server.rs::fixture_daemon_has_notifications_off` kills M35 (fixture default flipped back to true) |
| Real HOME untouched / stray processes | PASS | `~/.config/systemd/user` listing identical before/after; scratch HOME holds only .cargo/.rustup; `pgrep -x owl` = 0 after suite and after 3× watch loop |
| AC1 notify (round-1 survivors) | PASS | M19 (missing record → ""), M19b (unparsable raw → ""), M20 (empty contact name) all killed by `daemon_notifies_on_question_and_answer` (now 6 asserted lines + silent twin) |
| AC4 watch (round-1 survivor) | PASS | M18 (no-timeout arm → exit 4) and M18b (no-timeout → single poll) killed by `watch_without_timeout_waits_indefinitely`; 3/3 loop runs green in 3.07 s each; negative twin kills its child with `child.kill()` + `wait_with_output()` |
| AC5 doctor (round-1 survivor) | PASS | M16 (constant 120) killed by `cli::doctor::tests::pull_warn_threshold_follows_pull_interval`; M17 (`>`→`>=` at the 10 s boundary, interval 5) killed by the same test plus `pull_check_ages`; binary-level twin in tests/doctor.rs asserts `warn pull: last pull 11s ago (interval 5s)` with exit 0 |
| Install load/unload path (new) | PASS | M31 (drop `enable --now`), M32 (ignore load failure), M33 (unload no-op), M34 (ignore unload failure) killed by `install_and_uninstall_drive_the_service_manager` / `service_manager_failure_is_reported` with exact argv assertions against fake `systemctl`/`launchctl`/`id` on a temp PATH |

Round-2 sweep: 12 mutants, 12 killed, 0 survived (mutants.log, mutants.py).
Minor note (no action required): in `watch_without_timeout_waits_indefinitely` the negative-twin
`assert!(child.try_wait()…is_none())` precedes `child.kill()`, so a failing assertion would leave one
`owl watch` running; it cannot fail without a code regression.

FAILING: none.

Round 2 verdict: **PASS** (AC1–AC6 all PASS; standing gates PASS; hygiene PASS).

Draft PR: https://github.com/Krab00/owlpost/pull/10
