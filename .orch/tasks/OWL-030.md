---
id: OWL-030
title: "owl update: the daemon unit must never read the replaced binary's path as `<path> (deleted)`"
status: in_progress
deps: [OWL-029]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
Found by OWL-029 AC8 on the dev machine (2026-09-06 19:15 UTC): the first real `owl update --source .`
with the OWL-029 binary printed `installed /home/kris/.local/bin/owl` and then rewrote the systemd
unit as `ExecStart="/home/kris/.local/bin/owl (deleted)"`; the daemon went into a 203/EXEC restart
loop (12 restarts) until the unit was repaired by hand. `owl doctor` showed
`warn binary: daemon unit runs /home/kris/.local/bin/owl (deleted)` and `fail daemon: … Connection refused`.

Root cause: `run_update` (`src/cli/update.rs`) computes `active = install::owl_path()` once, replaces
the file by rename, and then calls `install::uninstall()` + `install::install(home, false)`;
`install::install` (`src/cli/install.rs:229`) calls `owl_path()` **again**. After the rename the
running process's `/proc/self/exe` reads `<path> (deleted)`, `canonicalize` fails (the path no
longer exists as an inode) and the raw string is written into the unit. The OWL-029 end-to-end
tests never hit this because `OWLPOST_UPDATE_ACTIVE` redirects the replaced file away from the
running test binary.

Fix, smallest that closes it:
- `install::install` takes the owl path from its caller: `pub fn install(home, dry_run, owl: &Path)`
  (or an `install_with(owl)` used by update; the CLI `owl install` keeps resolving `owl_path()`
  itself). `run_update` passes the `active` it computed **before** the replacement, so the unit
  is written from a value read while the file still existed.
- Defence in depth: `install::owl_path()` strips a trailing ` (deleted)` from `current_exe()`
  before canonicalising (Linux `/proc/self/exe` artefact), with a `// ponytail:` naming the
  artefact, so `owl doctor`, `owl install` and anything else that resolves the running binary
  after an in-place replacement report the real path.
- The doctor `binary` check reuses `owl_path()` (it already does) and therefore stops printing
  `(deleted)`.
- Design doc §11: one sentence — the unit is rewritten from the path captured before the
  replacement; a running process's own path may read `(deleted)` after a rename on Linux.
- README/quickstart: one line for installs older than OWL-029: the first `owl update` still runs
  the old updater (installs to `~/.cargo/bin`); bootstrap once with the two-command copy+rename
  into `~/.local/bin` (or re-run the release installer), then `owl update` works in place.

Out of scope: macOS launchd equivalent (no `(deleted)` artefact there; the caller-passed path fixes
it anyway); the release installer.

## Acceptance criteria
- [ ] AC1: an end-to-end test in `tests/setup.rs` copies the test `owl` binary to `<tmp>/bin/owl`, runs **that copy** as `owl update --source <dir>` with the fake `cargo` and a temp `HOME` and **without** `OWLPOST_UPDATE_ACTIVE` (so the running process's own file is rename-replaced), and asserts: exit 0, `installed <tmp>/bin/owl` printed, the unit written under the temp `HOME` names exactly `<tmp>/bin/owl` (no `(deleted)`, no other path), and the file at `<tmp>/bin/owl` holds the built bytes; the same test asserts the unit text does not contain the string `(deleted)` (modality: test)
- [ ] AC2: a unit test drives the `owl_path()` normalisation with `/x/owl (deleted)` → `/x/owl` and leaves `/x/owl` and a path that merely ends in `deleted` untouched (modality: test)
- [ ] AC3: `owl doctor` run from a binary whose file was replaced under it prints `ok binary: <path>` (drive it in the AC1 test: after the update, run `<tmp>/bin/owl doctor` with the temp HOME and PATH led by `<tmp>/bin`, assert the `binary` row is `ok` and no row contains `(deleted)`) (modality: test)
- [ ] AC4: design doc §11 and README carry the sentences named above, pinned single-line in `tests/docs.rs` (modality: test)
- [ ] AC5: on the dev machine after the merge: `owl update --source .` → `installed /home/kris/.local/bin/owl`, `~/.config/systemd/user/owlpost.service` has `ExecStart="/home/kris/.local/bin/owl"`, `systemctl --user is-active owlpost.service` → `active`, `owl doctor` → `ok binary: /home/kris/.local/bin/owl` and `ok daemon: …` (run by the orchestrator, recorded here) (modality: manual)
- [ ] AC6: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-030-update-unit-path

Round 1, 2026-09-06, branch at d0a4f24 (base e1b0278), fresh detached worktree `.worktrees/verify-OWL-030` with a private `target/` (removed afterwards). `cargo test -- --list` named the three new tests before any run was trusted. Gates: build, `fmt --check`, `clippy -D warnings` rc 0; `cargo test --workspace` 495 passed, 0 failed; `Cargo.*` unchanged. Flake: the e2e test 10/10 alone, the whole `tests/setup.rs` binary 5/5 with default threads. test-reviewer: **GAPS** (the captured-path plumbing has no observing test; the new `owl setup` caller is unpinned). Evidence in `.orch/tasks/OWL-030.evidence/` (gitignored, on disk).

Verifier sweep (13 mutants, `timeout 300`, each survivor's log checked for a rebuild and all 6 test binaries green):

| mutant | what | verdict | killer |
|---|---|---|---|
| M1 | `run_update` passes `&install::owl_path()?` after the rename (strip kept) | **SURVIVED** | — |
| M2 | `owl_path()` without `strip_deleted` (plumbing kept) | **SURVIVED** | — (the implementer's report said "M2 alone RED"; it is not) |
| M3 | M1 + M2, the incident as shipped | RED | `update_of_the_running_binary_writes_the_unit_from_the_captured_path` |
| M4 | strip matches `(deleted)` without the space | RED | `strip_deleted_drops_only_the_proc_suffix` |
| M5 | `install` ignores its `owl` parameter and calls `owl_path()` | **SURVIVED** | — (report said RED; it is not — `tests/install.rs` pins the CLI arm, which passes the same path) |
| M6 | canonicalise failure after the strip keeps a bogus path instead of the raw one | SURVIVED | fallback arm no test can reach (a fresh `owl doctor` always finds its file); minor |
| M7 | `owl setup` passes `/wrong/owl` to `install` | **SURVIVED** | — |
| M8 | `owl install` (CLI) passes `/wrong/owl` | RED | `install_dry_run_prints_unit_and_writes_nothing`, `install_then_uninstall_creates_and_removes_unit_file` |
| M9 | `strip_deleted` is the identity | RED | `strip_deleted_drops_only_the_proc_suffix` |
| M10 | strip also trims a bare `deleted` | RED | same |
| M11 | design §11 sentence drifts | RED | `update_docs_name_the_captured_path_and_the_bootstrap` |
| M12 | README bootstrap command drifts | RED | same |
| M13 | (equivalent: path round-trip) | SURVIVED | discard |

Judgement on the reported survivor M1: **not acceptable as-is** (a test-coverage call, not a design call, so no PM-REVIEW). Two independent fixes for one failure are fine only when each has its own kill switch; here neither does — M1, M2 and M5 each survive alone and only the pair (M3) is red. The task names the caller-passed path as the fix and the strip as defence in depth, yet no test observes the plumbing at all: the e2e test can only see the outcome, and `strip_deleted` repairs any wrong re-read on the way. The pin is cheap and needs no `/proc` artefact: in `update_replaces_a_running_active_binary_end_to_end` (the `OWLPOST_UPDATE_ACTIVE` route, where `active-owl` differs from the test binary) assert the unit's `ExecStart` line equals `ExecStart="<user_home>/active-owl" daemon --home "<home>"` — M1 and M5 then write the test binary's path and go red regardless of the strip. Same shape as OWL-029 round 1 (a wiring arm proven only on the pure function).

| AC | verdict | evidence |
|---|---|---|
| AC1 | **FAIL** (test modality; behaviour correct) | `update_of_the_running_binary_writes_the_unit_from_the_captured_path` runs a copy of the test binary as the replaced process without `OWLPOST_UPDATE_ACTIVE`, asserts exit 0, `installed <tmp>/bin/owl`, the exact `ExecStart` line, no `(deleted)` in the unit or stdout, the built bytes — M3 RED reproduces the incident. But the plumbing this AC exists for is unobserved: **M1, M5 SURVIVED**, and M2 shows the strip is likewise unpinned in situ; **M7** (`owl setup` passes a wrong path) SURVIVED. |
| AC2 | PASS | `strip_deleted_drops_only_the_proc_suffix`: `/x/owl (deleted)` → `/x/owl`; `/x/owl`, `/x/owldeleted`, `/x/owl deleted`, `/x/owl(deleted)`, mid-path suffix untouched; M4, M9, M10 RED. Drives `strip_deleted` rather than `owl_path()` itself, which cannot be induced in-process; acceptable. |
| AC3 | PASS | same e2e test: `<tmp>/bin/owl doctor` with the temp HOME and PATH led by `<tmp>/bin` after the update → exactly one `ok   binary: <tmp>/bin/owl` row, no `(deleted)` anywhere. |
| AC4 | PASS | four single-line pins in `tests/docs.rs`; M11, M12 RED. Note: the second half of the §11 sentence (`install::owl_path()` strips that suffix before canonicalising …) is an unpinned restatement (OWL-024 rule), optional. |
| AC5 | PENDING (post-merge) | orchestrator step on the dev machine. |
| AC6 | PASS | gates green, 495 tests, no new crate. |

FAILING: AC1. Fix shape for round 2: (1) one `ExecStart` equality assert on the unit written by `update_replaces_a_running_active_binary_end_to_end` (kills M1, M5); (2) one non-dry-run `SETUP` assert that the unit's `ExecStart` names canonical `CARGO_BIN_EXE_owl` (kills M7); optionally pin or drop the second §11 statement of the strip rule.
