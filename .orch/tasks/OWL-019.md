---
id: OWL-019
title: "Contacts: global (user) and local (repo) scopes, `owl add <peer-file>` implemented"
status: done
deps: [OWL-017]
source: adhoc
source_updated_at: 2026-09-05
criteria_source: derived
---
## Description
Today the contact book merges two providers, `.agents/peers/*.json` in the git root (`repo`)
and `$OWLPOST_HOME/contacts/*.json` (`local`), but the user cannot tell them apart in the
CLI beyond a `SOURCE` column, cannot list one scope alone, and `owl add` prints
`not implemented yet` — the only way to add a colleague is to copy the JSON by hand into the
home directory (observed 2026-09-05).

Name the scopes the way users think about them: **global** = the user's own book in
`$OWLPOST_HOME/contacts/` (every repo on this machine), **local** = the repository's
`.agents/peers/` (shared with the team via PR). Implement:

- `owl add <peer-file.json | '<inline json>' | -> [--local]` — accepts a file path, the JSON pasted
  as one argument, or `-` for stdin (so `/owlpost:add` can pass what the user pasted); validates it (name, pubkey, optional emails
  and endpoints), refuses a pubkey already present in either scope, writes it as
  `<slug>.json` into the global book, or into `.agents/peers/` of the current repo with
  `--local`, and prints `added <name> <fingerprint> (global|local)`. TOFU rule from
  `docs/architecture.md` §4 unchanged: adding never sets a policy; `owl allow` does.
- `owl contact list [--global|--local]` — the `SOURCE` column reads `global` / `local`
  (rename of `local` / `repo`; policy overlays still print the scope they live in);
  without a flag both scopes are listed, merged as before.
- `owl contact remove <peer> [--local]` — deletes the file from the named scope (default
  global); refuses to touch `.agents/peers/` without `--local`.
- `/owlpost:me` output tells the colleague to run `owl add <file>` (already does) — no
  change needed once `owl add` exists.

Out of scope: editing contacts in place, fetching a peer file over the network
(`owl card <peer>` already fetches a card), an interactive picker (OWL-020).

## Acceptance criteria
- [ ] AC1: `owl add <file>` writes `$OWLPOST_HOME/contacts/<slug>.json` and `owl contact list` shows the contact with source `global`; `--local` writes `<git root>/.agents/peers/<slug>.json` with source `local` — integration tests in `tests/contacts.rs` (modality: test)
- [ ] AC2: `owl add` on a file whose pubkey exists in either scope exits 1 with `already a contact: <name> (<scope>)` and writes nothing; a file without `name` or `pubkey`, or with an unparsable pubkey, exits 1 naming the field (modality: test)
- [ ] AC3: `owl contact list --global` / `--local` list exactly one scope; `--global --local` together is a usage error (modality: test)
- [ ] AC4: `owl contact remove <peer>` removes the global file; `owl contact remove <peer> --local` removes the repo file; removing a peer that lives only in the other scope exits 1 with a hint naming the flag (modality: test)
- [ ] AC5: `source` values `global`/`local` replace `local`/`repo` everywhere: `contacts::Contact::source` doc, `owl contact show` JSON, `docs/technical-design.md` contact-book section, `plugins/claude-code/skills/owlpost/SKILL.md`; `tests/docs.rs` and `tests/plugin.rs` pass (modality: data)
- [ ] AC6: `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass (modality: data)

## Verification — task/OWL-019-contact-scopes
Head 48cfbf7 (base 45b8309). Verify env: worktree `.worktrees/OWL-019`; deterministic binary built from `git archive 48cfbf7` (the worktree's `target/debug/owl` was a stale mutant build, see notes). Evidence: `OWL-019.evidence/` (`gates-final.txt`, `ac-walk.txt`, `defects.txt`, `mutants.txt`, `mutants-pass2.txt`).

| Criterion | Verdict | Evidence |
|---|---|---|
| AC1 `owl add <file>` → `$OWLPOST_HOME/contacts/<slug>.json`, source `global`; `--local` → `<git root>/.agents/peers/<slug>.json`, source `local`; inline JSON and `-` stdin | PASS | `ac-walk.txt`: `added Ola Nowak owl:223h… (global)`, file `ola-nowak.json` holds only name/emails/endpoints/pubkey; `--local` from nested cwd writes `.agents/peers/marek-nowak.json` (dir created when missing); `contact list` rows `local`/`global`; inline + stdin arms exit 0. Tests `add_file_writes_global_slug_and_lists_as_global`, `add_local_writes_into_repo_peers_and_lists_as_local`, `add_reads_inline_json_and_stdin` pass; mutants A1–A8, A10–A13 killed |
| AC2 dup pubkey in either scope → exit 1 `already a contact: <name> (<scope>)`, nothing written; missing/invalid `name`/`pubkey` exit 1 naming the field | PASS (1 survivor) | `ac-walk.txt`: `already a contact: Ola Nowak (global)`, `… Marek Nowak (local)` for both flag values, dirs unchanged; `lacks the \`name\` field`, `lacks the \`pubkey\` field`, `field \`pubkey\` is invalid: … got 3`, `field \`pubkey\` must be a string`, `field \`name\` is empty`, empty stdin → `not valid JSON`; same-slug/different-key refused in both scopes; directory at destination → exit 1, no `.tmp`. Survivor **A9** (`string_list` `all`→`any`): a mixed array `"emails":["a@x",1]` is accepted — no test with a partially-string array (negative twin missing) |
| AC3 `contact list --global` / `--local` list one scope; both flags = usage error | PASS | `ac-walk.txt`: `--global` = Ola+Pawel only, `--local` = Marek only, `--json` sources match; `--global --local` (both orders) → clap error, exit 2. Mutants M1, N1, N2, N9 killed |
| AC4 `contact remove <peer>` deletes the global file; `--local` deletes the repo file; peer only in the other scope → exit 1 with hint naming the flag | **FAIL** | Plain case passes (`removed Pawel Nowak (global)`, `removed Marek Nowak (local)`, hints `Marek Nowak is a local contact; use --local` / `Ola Nowak is a global contact; drop --local`, outside a repo → `… is not inside a git repository`). **Defect (defects.txt):** after the documented flow `owl add` → `owl allow <peer>` (writes `<fp>.json` beside `<slug>.json`), `owl contact remove Pawel` exits 1 with the wrong hint `Pawel Nowak is a global contact; drop --local` (no `--local` was given) and deletes nothing; `scope_files("global")` yields two "Pawel" entries so `resolve` is ambiguous and falls into the hint arm. `remove <fp>` deletes only the overlay, contact stays listed and `owl add` still says `already a contact`. No test walks add→allow→remove. Survivor **N7** (`remove_file` error ignored): the chmod-555 failure path (`removing …: Permission denied`, exit 1, file kept) is correct by hand but unpinned |
| AC5 `source` `global`/`local` everywhere: Contact doc, `contact show` JSON, technical-design §5, SKILL.md; `tests/docs.rs`, `tests/plugin.rs` pass | PASS (test pin weak) | `grep -rn '"repo"' src tests docs plugins` → only path names / an unrelated `scope.repo` policy key. `contact show --json` → `"source": "global"` / `"local"`. `docs/technical-design.md` §5 + CLI table, `SKILL.md` `## Contacts`, `commands/add.md` (`Bash(owl add:*)`, `[--local]`) updated. `tests/docs.rs` (4) and `tests/plugin.rs` (7) pass but pin none of the new wording (would also pass with `local`/`repo`). Leftovers `docs/concept.md:80,108,124`, `docs/pilot.md:29` still say "repo/local provider" — PM-REVIEW whether in scope |
| AC6 `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` | PASS | `gates-final.txt`: build ok; 124 lib + 47 bin + all 22 integration binaries green (cli 26, contacts 32, policy 20 …); clippy clean; fmt ok |
| Policy lost after `owl allow` on an `owl add` contact whose slug sorts after `owl:` (e.g. `pawel-nowak.json` > `owl:fcg….json`) | **FAIL** (correctness, AC1/AC4 flow) | `defects.txt`: `owl allow Pawel --always --i-verified-the-fingerprint` prints `policy auto` and writes `owl:fcg….json`, but `contact list` shows POLICY `-` and `contact show Pawel` has no `policy`; Marek (`marek-nowak.json` < `owl:`) keeps `auto`. `ContactBook::load` merges `$home/contacts` in filename order and the later slug file overwrites `policy` with `None`. Existing test `set_policy_on_slug_named_local_contact_writes_fp_file` uses `ola` (sorts before `owl:`) and never reloads, so it cannot see it |
| Probe: leftover global overlay after `contact remove --local` blocks re-adding the same key with `already a contact:  (global)` (empty name) | PM-REVIEW | `defects.txt`/`ac-walk.txt`: after `add --local`, `allow`, `remove --local`, `contact list` shows a nameless `global manual` row and both `owl add` / `owl add --local` fail with an empty name in the AC2 message. Recoverable only via `owl contact remove <fp>`. Task text does not say whether `remove` should drop the overlay; the empty `<name>` in the AC2 literal is a usability defect either way |
| TOFU: `owl add` never writes a policy; `owl allow --always` on an added (global) contact needs `--i-verified-the-fingerprint` | PASS | `defects.txt`: added file has no `policy`; `allow Pawel --always` → exit 1 `was added by hand … --always --i-verified-the-fingerprint`, no overlay written; with the flag → `policy auto`. Mutants L1, L2 killed |
| Test quality (test-reviewer) | GAPS | No add→allow→remove flow test; policy-loss ordering untested; hint arm negative twin (ambiguity ≠ other scope) missing; AC5 wording unpinned; `--local` blocked-destination half does not assert stderr |
| Mutant sweep (30 mutants, `cargo test --workspace`; 8 rerun with `--no-fail-fast` because a `notify::` lib test flaked under load) | 28 killed / 2 survived | `mutants.txt`, `mutants-pass2.txt`: survivors A9 (`string_list` all→any), N7 (`remove_file` error ignored) |
| Commit hygiene | PASS | No new fixture files; no vendored paths; tree clean after every revert |

Notes: the worktree's `target/debug/owl` (mtime 01:49) failed `cli::allow::tests::always_guard_matrix` and let `allow --always` through without the flag — a stale binary from a same-second mutant revert (cargo fingerprint), not the source; `touch` + rebuild passes and the archive-built binary confirms the guard. Implementers running self-sweeps should `touch` the file after each revert.

**VERIFY OWL-019: FAIL (PASS 5 / FAIL 2 / PM-REVIEW 1)**

### Round 2 — 7870d29
Worktree reset to origin/task/OWL-019-contact-scopes (7870d29), sources touched, gates rebuilt; hand checks with a binary built from `git archive 7870d29`. Evidence: `gates-round2.txt`, `round2-walk.txt`, `mutants-round2.txt`.

| Criterion | Verdict | Evidence |
|---|---|---|
| Policy loss after `owl allow` (slug before / after `owl:`) | PASS | `round2-walk.txt`: Pawel (`pawel-nowak.json` > `owl:`) and Marek (`marek-nowak.json` < `owl:`) both show `global auto` in `contact list`; `contact show Pawel` JSON carries `policy.mode: auto`. New `merge()` is order-independent. Mutants R1 (policy dropped), R2 (later file overwrites, the old bug), R3 (bare overlay not backfilled) killed by `allow_writes_manual_and_always_writes_auto`, `allow_then_list_keeps_policy_for_name_after_owl_prefix`, `policy_survives_reload_whatever_the_filename_order` |
| AC4 `contact remove` after `allow`, both scopes | PASS | `round2-walk.txt`: global: `removed Pawel Nowak (global)`, `<slug>.json` and `owl:<fp>.json` both gone, `list --global` no longer lists Pawel, re-add of the same key → `added … (global)`; `--local`: overlay in `$home/contacts` and `.agents/peers/ola-nowak.json` both gone, list empty, re-add `--local` succeeds; without `--local` the repo file is untouched and the hint `Ola Nowak is a local contact; use --local` is printed; `drop --local` hint only with `--local`; unknown peer → `no contact matches "Nobody"` (no hint); ambiguous prefix → `ambiguous peer "Ola": matches Ola Nowak, Olaf`, nothing deleted; outside a repo → `… is not inside a git repository`. Mutants R4 (overlay kept), R5 (resolve not scoped), R7/R8 (hint guards without the flag check), R9 (`--local` deletes every peer file) killed |
| N7 unlink error propagates | PASS | chmod 555 on `$home/contacts` → `removing …/olaf.json: Permission denied (os error 13)`, exit 1, file kept; same for `.agents/peers` with `--local` (no scope hint). Mutant R6 killed by `contact_remove_propagates_a_failed_unlink`, `contact_remove_local_failed_unlink_is_not_masked_by_the_scope_hint` |
| A9 mixed-type lists | PASS | `["a@x",1]` → `peer file field \`emails\` must be an array of strings`; `["h:1",{}]` → `… \`endpoints\` …`, exit 1. Mutant R10 killed by `add_rejects_mixed_type_lists` |
| Round-1 PM-REVIEW (leftover overlay after `remove --local`) | PASS (resolved by the fix) | `remove --local` now also deletes the `$home/contacts/<fp>.json` overlay: no nameless row in `contact list`, re-add of the same key succeeds |
| AC6 gates on 7870d29 | PASS | `gates-round2.txt`: build ok, all 23 test binaries ok, clippy clean, fmt ok |
| Test reviewer (round 2) | OK for the round-2 rows; 2 minor round-1 gaps still open | AC5 wording still unpinned by `tests/plugin.rs` / `tests/docs.rs` (they pass with either wording); `add_blocked_by_directory_at_destination_leaves_no_tmp` `--local` half still lacks the stderr assertion. Neither changes an AC verdict; `docs/concept.md` / `docs/pilot.md` "repo/local provider" wording remains PM-REVIEW |
| Mutant sweep round 2 | 10 / 10 killed | `mutants-round2.txt` (`cargo test --workspace --no-fail-fast`; `install_sh` and `notify` tests flaked under load and were ignored for classification; every mutant also has a relevant killer). R3 re-confirmed on `--test contacts --test cli` alone |

**VERIFY OWL-019 (round 2): PASS (PASS 6 / FAIL 0 / PM-REVIEW 1)**

PR (single-PR run): https://github.com/Krab00/owlpost/pull/24
