---
id: OWL-009
title: Responder runner — harness templates, prompt, redaction, fake harness
status: done
deps: [OWL-003]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `runner.rs` per `docs/technical-design.md §10` and `§3` (`harnesses`, `projects`).
`Runner::draft(config, question_payload) -> Draft { text, harness, redactions, status }`: pick
the harness (`--harness` override, else `config.responder.harness`; refuse a template with
`enabled == false` naming `disabled_reason`), resolve the working directory from
`config.projects[project]` (error `unknown project` otherwise), build the §10 prompt (scope
sentence reflects `responder.scope`; the question is quoted inside triple quotes and never
interpolated into instructions), substitute `{prompt}` or write `{prompt_file}`, spawn with the
template `env`, `OWLPOST_RESPONDER=1`, capture stdout/stderr, enforce `timeout_secs`, extract
the answer via `answer_path` (`raw`, `result`, `last_message`, `last_text`), run the redaction
regexes (replace with `[redacted]`, count), and return. Ship
`tests/fixtures/fake-harness.sh`: prints a JSON-free canned answer containing the line
`api_key = sk-test-123456` and echoes argv + stdin to `$FAKE_HARNESS_LOG`; a `FAKE_SLEEP` env
makes it sleep for timeout tests. Ship `scripts/opencode-agent.json` with the `owl-readonly`
agent definition (tools write/edit/bash disabled).

## Acceptance criteria
- [ ] AC1: integration test `tests/runner.rs::fake_harness_produces_redacted_draft` runs the fake harness and asserts the draft contains `[redacted]`, does not contain `sk-test-123456`, and `redactions == 1` (modality: test)
- [ ] AC2: integration test `tests/runner.rs::prompt_contains_question_project_path_and_scope` reads `$FAKE_HARNESS_LOG` and asserts the prompt includes the project id, the path, the question inside `"""` fences, the sentence `Do not run commands, do not modify files`, and with `private_memory = false` does not mention any notes directory (modality: test)
- [ ] AC3: integration test `tests/runner.rs::cwd_is_project_checkout` asserts the fake harness ran with `PWD` equal to the mapped checkout, and that an unmapped project yields an error containing `unknown project` (modality: test)
- [ ] AC4: integration test `tests/runner.rs::disabled_harness_is_refused` asserts `--harness kimi` errors with the configured `disabled_reason` and spawns nothing (modality: test)
- [ ] AC5: integration test `tests/runner.rs::timeout_marks_draft` with `FAKE_SLEEP=5` and `timeout_secs = 1` asserts status `timeout` within 3 s and the child process is gone (modality: test)
- [ ] AC6: unit tests `runner::tests::extract_*` cover `raw`, `result` (Claude `--output-format json` sample), `last_message` (Codex `--json` JSONL sample), `last_text` (opencode/kimi JSONL sample) using fixture strings under `tests/fixtures/harness-output/` (modality: test)
- [ ] AC7: unit test `runner::tests::extract_failure_keeps_raw` asserts an unparsable output yields status `extract_failed` with the raw text preserved (modality: test)

## Verification — task/OWL-009-runner
Round 1 · head `ede91b1` · verify worktree `.worktrees/verify-OWL-009` (detached, removed) · evidence `OWL-009.evidence/`

| Criterion | Verdict | Evidence |
|---|---|---|
| Gates: build / fmt / clippy -D warnings | PASS | `gates.txt` — all exit 0 |
| Gates: `cargo test --workspace` ×2, offline, stable | PASS | `cargo-test-x2.txt` — 47+25+16+11+8+11 pass both runs; wall 3.4 s / 1.2 s |
| §1 crates: only `regex` added (already in the §1 list) | PASS | `hygiene.txt` Cargo.toml diff |
| `fake-harness.sh` committed 100755 | PASS | `hygiene.txt` `git ls-files -s` |
| §10 prompt text verbatim | PASS | `runner::tests::prompt_follows_section_10_exactly` pins the exact 12-line string; compared line by line with §10 — identical (scope sentence, "Do not run commands, do not modify files.", "Cite file paths…", blank line, Project/File/Question/fences/"Answer in at most 300 words.") |
| AC1 redacted draft (`fake_harness_produces_redacted_draft`) | PASS | test passes; mutants `redact-string`, `redact-count-*`, `redact-not-applied-to-extracted`, `responder-env-*` all killed (`mutants*.txt`) |
| AC2 prompt contents + scope (`prompt_contains_question_project_path_and_scope`) | PASS | asserts project, path, fenced question (2 fences, injected `"""`→`'''`), exact "Do not run commands…" sentence, no `notes` with private_memory=false, notes path with true; mutants `private-memory-*`, `fence-not-neutralised`, `prompt-do-not-run-typo` killed |
| AC3 cwd + unknown project (`cwd_is_project_checkout`) | PASS | pwd == canonical checkout; `unknown project github.com/acme/other`, no spawn; missing checkout → `not accessible`; mutants `cwd-removed`, `project-dir-not-canonical` killed |
| AC4 disabled harness refused (`disabled_harness_is_refused`) | PASS | reason string + `kimi` in error, log never created; disabled default, override wins, unknown harness; mutants `disabled-check-*`, `disabled-reason-dropped`, `override-*` killed |
| AC5 timeout (`timeout_marks_draft`) | PASS | FAKE_SLEEP=5, timeout_secs=1; 1 s ≤ took < 3 s (observed 1.05 s); status `timeout`; `kill -0 <pid>` fails for the child; mutants `timeout-x3`, `timeout-millis`, `timeout-no-kill`, `timeout-status-ok` killed. Orphaned `sleep 5` grandchild (declared ponytail) exits on its own within 5 s (`hygiene.txt`), never indefinite |
| AC6 `extract_*` unit tests with fixtures | PASS | `extract_raw_trims_whole_stdout`, `extract_result_from_claude_json`, `extract_last_message_from_codex_jsonl`, `extract_last_text_from_{opencode,kimi}_jsonl` + reject twins; fixtures under `tests/fixtures/harness-output/` via `include_str!`; 9 extraction mutants killed |
| AC7 `runner::tests::extract_failure_keeps_raw` | FAIL | Test exists and passes, but lines 608–621 re-implement the `draft` match arm (`Err(_) => (raw, ExtractFailed)`) inside the test and assert on the value the test built. Mutants `extract-failed-drops-raw` and `extract-failed-status-ok` survive this test and are killed ONLY by `tests/runner.rs::extract_failed_draft_keeps_raw_stdout_and_redacts`. The named unit test therefore does not prove "yields status extract_failed with raw preserved" against production code. Fix: have the named test drive `draft` (fake harness + `FAKE_OUTPUT_FILE=…/garbage.txt`, `answer_path=result`) or move the draft-level assertion there and keep only the `extract` contract |
| Test hygiene (temp dirs only, no real harness, no leftovers) | PASS | `hygiene.txt` — no `owlpost-prompt-*` left after a clean run; only temp homes/checkouts; no network |
| Mutant sweep (43 mutants) | PASS with 4 survivors | `mutants.txt` (first pass had a filter bug: `cargo test --lib runner --test runner` filtered out every integration test; `mutants-rerun.txt` re-ran the 18 apparent survivors correctly). Real survivors: `timeout-ge-to-gt` (equivalent, 20 ms poll), `stdin-inherit` (equivalent under cargo's stdin), `opencode-and-to-or` (no test with `type≠text` but `part.type=text`), `kimi-fallback-removed` (§12 `~/.kimi-code/bin/kimi` fallback untested) |

FAILING: AC7 (tautological named test; behaviour itself is correct and proven at draft level).

Non-blocking gaps to close in the same fix round (from planner/reviewer/sweep): timeout with partial stdout + redaction (needs a print-then-sleep knob in the fake harness); opencode `part.type` twin (`opencode-and-to-or` survivor); kimi `~/.kimi-code/bin` fallback both halves; stderr text asserted in the "no output" error; misleading comment at `tests/runner.rs:367`; pretty-printed multi-line `result` JSON; prompt-file cleanup when spawn fails.

## Verification — task/OWL-009-runner (round 2)
Head `53670e3` · re-verified AC7 + the round-1 non-blocking gaps · evidence `OWL-009.evidence/round2-gates.txt`, `round2-mutants.txt` · verify worktree removed. Diff since `ede91b1` touches only `src/runner.rs`, `tests/runner.rs`, `tests/fixtures/fake-harness.sh`; `build_prompt` untouched, so the §10 row stands. Fake harness still 100755.

| Criterion | Verdict | Evidence |
|---|---|---|
| Gates build / fmt / clippy -D warnings | PASS | `round2-gates.txt`, all exit 0 |
| `cargo test --workspace` ×2; `--lib runner::tests` (22) and `--test runner` (13) run separately, no positional filter | PASS | `round2-gates.txt`; 122 tests, stable across both runs |
| AC7 `runner::tests::extract_failure_keeps_raw` | PASS | Now calls the production `finish_extract` for result/last_message/last_text (+ Ok path), then drives `draft()` with the fake harness printing `garbage.txt` under `answer_path=result`. Mutants `extract-failed-drops-raw` and `extract-failed-status-ok` are killed by the named test ALONE (`round2-mutants.txt`, `(named)` rows) and again in the full run |
| Gap: timeout keeps + redacts partial stdout | PASS | `timeout_with_partial_output_keeps_and_redacts_captured_text` via `FAKE_PRINT_THEN_SLEEP`; mutants `timeout-partial-text-dropped` and `timeout-no-redact-on-partial` killed; child asserted dead |
| Gap: opencode `part.type` twin | PASS | guard is now `\|\|`; both twin lines in `extract_last_text_rejects_streams_without_text`; mutant `opencode-or-to-and` killed |
| Gap: kimi `~/.kimi-code/bin` fallback (§12) | PASS | `resolve_program_in(first, PATH, HOME)`; `kimi_falls_back_to_home_bin_when_not_on_path` covers neither / fallback / PATH-wins / kimi-only / PATH=None; mutants `kimi-fallback-removed` and `kimi-path-does-not-win` killed |
| Gap: stderr in the no-output error | PASS | `FAKE_STDERR`; stderr text and exit code asserted; mutant `stderr-dropped-from-error` killed |
| Gap: pretty-printed `result` JSON | PASS | `extract_result_from_pretty_printed_json` |
| Gap: prompt-file cleanup on spawn failure; misleading comment | PASS | `prompt_file_is_cleaned_up_when_spawn_fails` (non-executable program); comment at `tests/runner.rs` corrected |
| `<home>/tmp` move leaks nothing outside the temp home | PASS | prompt files written under `prompt_dir(home)`; test asserts the path starts with `<home>/tmp` and the dir is empty afterwards; no `owlpost-prompt-*` in `/tmp` after the full run or after the sweep; mutant `prompt-dir-global-tmp` killed |
| §10 / §12 still honoured | PASS | prompt template diff-free; `prompt_follows_section_10_exactly` unchanged and passing; kimi fallback retained per §12; tests offline, no real harness |
| Mutant sweep round 2 (12 mutants) | PASS | 12/12 killed, worktree clean after revert |

FAILING: (none)

Observation only: the orphaned `sleep 5` grandchildren (now up to 6 per full run, from three timeout-style tests) still exit on their own within 5 s; declared ponytail, unchanged.

Draft PR: https://github.com/Krab00/owlpost/pull/7
