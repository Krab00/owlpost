---
id: OWL-010
title: Inbox CLI — inbox, show, draft, edit, send, reject, history, hook formats
status: done
deps: [OWL-003, OWL-009]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement the inbox commands per `docs/technical-design.md §8–§9` and `docs/architecture.md
§3.3–§3.4`. `owl inbox` lists inbox records (id, from name, type, state, path, age) and marks
them seen; `--new` only unseen; `--all` includes seen; `--count` prints the unseen count;
`--format plain|claude|codex|kimi` with `--count` emits exactly the injection shapes from §9 and
**empty output with exit 0 when the count is 0**. `owl show <id|all>` prints the full question
or answer (and the draft if present) and marks seen. `owl draft <id>` runs the OWL-009 runner,
stores the draft on the record (state `drafted`), prints it with a redaction summary.
`owl edit <id>` opens the draft in `$EDITOR` and stores the result. `owl send <id>` builds the
answer payload (`in_reply_to`, `harness`, `redactions`, `cached=false`), signs it, writes it to
`outbox/`, moves the question to `done/` (state `answered`), updates the responder cache.
`owl reject <id>` moves to `done/` (state `rejected`). `owl history` lists `done/` with filters
`--peer`, `--path <glob>`, `--since <RFC3339|Nd>`. `--json` on all of them.

## Acceptance criteria
- [ ] AC1: integration test `tests/inbox.rs::count_and_formats` with two unseen questions asserts `owl inbox --count` prints `2`, `--count --format claude` prints a single line of JSON with `hookSpecificOutput.hookEventName == "UserPromptSubmit"` and `additionalContext` starting with `owlpost: 2 new questions`, and with zero unseen prints nothing and exits 0 (modality: test)
- [ ] AC2: integration test `tests/inbox.rs::listing_marks_seen` asserts `owl inbox` shows both, a second `owl inbox --count` prints `0`, and `owl inbox --new` prints nothing while `--all` still lists both (modality: test)
- [ ] AC3: integration test `tests/inbox.rs::draft_send_moves_records` runs `owl draft <id>` with the fake harness, asserts state `drafted` and the printed draft contains `[redacted]`; then `owl send <id>` creates an outbox envelope whose signature verifies against the responder's key, with `in_reply_to == <id>` and `redactions == 1`, the question is in `done/` with state `answered`, and the responder cache holds the answer under the question hash (modality: test)
- [ ] AC4: integration test `tests/inbox.rs::edit_replaces_draft` with `EDITOR` set to a script that appends `EDITED` asserts the stored draft ends with `EDITED` and `send` ships the edited text (modality: test)
- [ ] AC5: integration test `tests/inbox.rs::reject_and_history` rejects one record and asserts `owl history --json` lists it with state `rejected`, `--peer <name>` filters correctly, and `--since 1d` includes it (modality: test)
- [ ] AC6: `owl draft <id>` on a record in state `consent` exits 1 with a message containing `owl allow` (modality: test)
- [ ] AC7: `owl send <id>` without a draft exits 1 with a message containing `owl draft` (modality: test)

## Verification — task/OWL-010-inbox

Round 1 (5ca261b, 2026-09-02, table never written — run interrupted): gate green; 28 mutants, 3 survived (M12 extract-failed-ok, M23 history-own-branch, M26 exit-code-ignored); real panic — `owl reject`/`owl send` on a record with non-object `meta` hit `IndexMut` on a `Value` after the record was already in `done/` with stale state (`OWL-010.evidence/meta-index-panic.log`, `mutants.log`).

Round 2 (9bb08dd, 2026-09-02, verify slot 1, detached worktree `.worktrees/verify-OWL-010`, evidence files prefixed `r2-`):

| Check | Evidence | Result |
|---|---|---|
| Gate: `cargo build`, `cargo test --workspace` ×2, clippy `-D warnings`, `fmt --check` | `r2-build.log`, `r2-test-workspace-run{1,2}.log` (167 tests, 0 failed, both runs), `r2-clippy.log`, `r2-fmt.log` — all exit 0 | PASS |
| AC1 `tests/inbox.rs::count_and_formats` (tests/inbox.rs:230) | exact `2\n`; full claude line pinned + `hookEventName`/`additionalContext` prefix; one line; zero-unseen → `(0,"","")` for all 4 formats; drives `owl` binary. `r2-ac-named-tests.log` | PASS |
| AC2 `listing_marks_seen` (:337) | both listed; second `--count` exact `0\n`; `--new` empty exit 0; `--all` lists both; third unseen record isolates `--new` | PASS |
| AC3 `draft_send_moves_records` (:464) | `[redacted]` present and secret absent; state `drafted`; envelope verifies vs responder key and NOT vs asker; `in_reply_to==id`; `redactions==1`; `cached==false`; done/ `answered`; cache hit by hash. Criterion holds. **Gap**: the round-2 `cli::finish` reorder (write state, then rename) is unverified — mutant F1 (rename-before-write) SURVIVED all three test binaries (`r2-mutants.log`); independent probe (`r2-probe.log` PROBE C): with `done/<id>.json` blocked by a directory, `owl send` exits 1 leaving the record in `inbox/` in state `answered` with the answer already in `outbox/`, and a retry of `owl send` is refused ("state answered, not drafted") — the doc comment on `finish` ("a retry redoes") is false for send. | FAIL (gap + failure-path defect) |
| AC4 `edit_replaces_draft` (:662) | draft ends with `EDITED`, editor saw original, `send` ships edited text; negatives: no EDITOR, editor exit 3, emptied file → draft unchanged | PASS |
| AC5 `reject_and_history` (:755) | state `rejected`; `--peer` name/case/fp, one-letter twin excluded; `--since 1d` includes; inclusive boundary at exact second, +1s excluded; glob twins. Criterion holds. **Gap**: `history.rs` `Identity::load` `Err(_) => None` arm untested — mutant F8 SURVIVED (probe D shows the fallback works: peer=`from`). Minor: after a failed rename, retried `owl reject` overwrites `previous_state` with `rejected` (probe B). | FAIL (gap) |
| AC6 draft on `consent` → exit 1, message has `owl allow` | `draft_on_consent_points_to_allow` (:1001): exit 1 + `owl allow` + peer fp, record untouched, runner never ran; `denied`/answer/unknown-id/unknown-harness negatives | PASS |
| AC7 send without draft → exit 1, message has `owl draft` | `send_without_draft_points_to_draft` (:1143): exit 1 + `owl draft <id>`; inbox/done/outbox/cache untouched; consent/stale-draft/malformed-draft/answer/foreign-`to` negatives | PASS |
| Round-1 panic probe (meta `"oops"`, `[1]`, `null`, `7` → `owl reject`, `owl send`) | `r2-probe.log` PROBE A: all 8 runs exit 0, record in done/ with final state, `done_at` and `previous_state`/`answer_id`; nothing left in inbox/ | PASS (fixed) |
| Round-1 survivors re-run as mutants (`--lib`, `--bin owl`, `--test inbox` separately) | M12 killed by `draft_extract_failed_is_stored_but_exits_1`; M23 killed by `history_names_the_other_party_for_records_this_identity_sent`; M26 killed by `cli::tests::exit_code_comes_from_exit_error_else_1` (`r2-mutants.log`) | PASS |
| Fresh sweep (9 mutants) | F2 no `done_at`, F3 extra keys dropped, F4 state unset, F5 `meta_object` repairs null only, F6 `exit_code` default 0, F7 history always `to`, F9 hint says `owl draft` → all KILLED. **F1 finish rename-before-write SURVIVED; F8 history no-key arm errors SURVIVED.** 10/12 killed | FAIL (2 survivors) |
| test-planner (`git diff main...9bb08dd`) | 58-row matrix; VERDICT: GAPS. Binding: #9 finish rename failure after state write (the only test that proves the reorder); #8 put failure before rename; #54 history `own=None`. Also flagged (non-blocking): `send --json`/`edit --json` shapes, `show all` on empty inbox, `--count --json` with an answer record (`questions != count`), EDITOR whitespace / with-args, temp-file cleanup on editor failure, `payload_of` malformed `raw`, `cache_put` blocked mid-send | GAPS |
| test-reviewer | All 7 AC tests exist under the exact names, drive `target/debug/owl`, pin exact values, negative twins present; round-2 tests would each fail on the round-1 revert except that `reject_and_send_survive_non_object_meta` cannot distinguish the finish ordering. VERDICT: GAPS — (1) `finish` failure path (block `done/<id>.json`) for reject and send incl. retry outcome; (2) `history` with no key file | GAPS |
| Hygiene | Own `tempdir` per test, `--home`, `OWLPOST_HOME`/`EDITOR` scrubbed from child env, no `set_var`, only `fake-harness.sh`, daemon on `127.0.0.1:0`; full parallel suite green twice. `notify`: N/A on this branch — OWL-012 is not in `main` (base e2a9008) and `notify` is read only in `src/config.rs`, so no popup can fire; whoever merges OWL-012 must set `notify=false` in `tests/common` per `_lessons.md` | PASS |

**Verdict: FAIL** (round 2 of 2). Code: the round-1 panic is fixed and all three round-1 survivors are killed. Remaining: (a) `cli::finish` failure path — write-then-rename reorder unverified (F1 survives) and `owl send` cannot be retried after a failed rename, contrary to the function's comment; (b) `history` no-identity fallback arm untested (F8 survives).

FAILING: AC3 (finish failure path: mutant F1 survives; send unrecoverable after failed rename), AC5 (history `Identity::load` Err arm: mutant F8 survives)

Round 3 (a5ffe1a, 2026-09-02, fresh run round 1 of 3, verify slot 1, detached worktree `.worktrees/verify-OWL-010`, evidence files prefixed `r3-`; only AC3 and AC5 re-verified, the other five carried):

| Check | Evidence | Result |
|---|---|---|
| Gate: `cargo build`, `cargo test --workspace` ×2, clippy `-D warnings`, `fmt --check` | `r3-build.log`, `r3-test-workspace-run{1,2}.log` (173 tests, 0 failed, both runs, full parallelism), `r3-clippy.log`, `r3-fmt.log` — all exit 0 | PASS |
| AC1, AC2, AC4, AC6, AC7 (carried from 9bb08dd) | Named tests `count_and_formats`, `listing_marks_seen`, `edit_replaces_draft`, `draft_on_consent_points_to_allow`, `send_without_draft_points_to_draft` pass in both r3 runs; no source change touches them except `edit.rs` `edited_at` via `as_object_mut` (mutant M4 "edited_at never written" KILLED by `edit_replaces_draft`, `send_and_edit_json_shapes`) | PASS (carried) |
| AC3 `draft_send_moves_records` (:464) + `finish()` failure path | Named test passes. `r3-probe.log` PROBE B/C: `done/<id>.json` blocked by a directory → `send`/`reject` exit 1, one stderr line `finishing record <id>: renaming to …: Is a directory`, inbox file byte-identical (`drafted`/`consent`), no `.tmp` leftovers; `send` blocked twice → still exactly 1 outbox envelope with the same id; after unblock `send` exits 0, reports and reuses the first attempt's answer id, envelope byte-equal, done `answered`; retried `reject` records `previous_state=consent` (round-2 overwrite fixed). `draft` after a failed send: allowed (exit 0, re-draft) — §8 (`draft.rs` gate) accepts `pending|drafted`, and the record is still `drafted`, so this is per spec. PROBE E: **the post-put `remove_file` failure IS test-inducible without root** — `chmod 0o555 spool/inbox` → `send` exits 1 `removing …/inbox/<id>.json: Permission denied`, `done/<id>.json` present (`answered`, `answer_id`) beside the untouched inbox record; retry after `chmod 0o755` exits 0, same answer id, 1 envelope, done overwritten in place; same for `reject` with `previous_state=consent` kept. Behaviour matches the new doc comment. Mutants (`r3-mutants.log`, `--lib`/`--bin owl`/`--test inbox` separately): F1 (put into inbox then rename), F1b (remove before put), M6 (no "finishing record" context), F10 (never reuse), F11 (reuse any envelope), M7 (wrong recipient on reuse) all KILLED by the new tests. **SURVIVED: M5 `remove_file` error ignored (exit 0 with the record in both `inbox/` and `done/`)** — nothing exercises the remove arm; the implementer's "not test-inducible without root" note is wrong (PROBE E, and test-reviewer confirmed independently). Also survived, non-binding: M1 (`question_id` absent matches), M2 (non-string `question_id` matches), M3 (cache rewritten on reuse) — no other writer of `outbox/` exists in `src/`, so every outbox record carries a string `question_id`, and M3 rewrites identical content (PROBE G shows the real code leaves the cache untouched, PROBE F shows absent/number/array/other-id/non-object meta never match and duplicates pick the lowest id, PROBE H shows a matching record with malformed `raw` fails cleanly before `finish`, inbox intact) | FAIL (gap: `remove_file` failure arm untested, mutant M5 survives) |
| AC5 `reject_and_history` (:755) + `history` no-key arm | Named test passes; `history_without_identity_key_lists_done_with_from_as_peer` pins JSON and table output with `peer==from` for a record this identity sent. PROBE D: key removed → exit 0, empty stderr, sent record's peer is own fp. Mutant F8 (`Identity::load` Err arm returns the error) KILLED by the new test | PASS |
| Round-2 regression: meta non-object (`"oops"`, `[1]`, `null`, `7`) → `reject`, `send` | PROBE A: all 8 runs exit 0, record in `done/` with final state, `done_at` + `previous_state`/`answer_id`, inbox empty | PASS |
| test-planner (`git diff 9bb08dd...a5ffe1a`) | 24-row matrix; VERDICT: GAPS. Binding: rows 8/9 `remove_file` failure for send and reject (chmod recipe). Flagged non-binding: `existing_answer` filter robustness (13–15), malformed matching `raw` (16), unparsable `outbox/` file (17), cache-not-rewritten assertion cannot distinguish (18), `--json` on the reuse path (20), stale envelope after `edit` between failed send and retry (19, PM-REVIEW) | GAPS |
| test-reviewer | All four new tests assert real outcomes, drive the binary, no mocks, each fails on 9bb08dd. VERDICT: GAPS — the `remove_file` arm is inducible via `chmod 0o555` as non-root and is the only way to exercise the "retry overwrites done/ in place" claim (`send_reuses_only…` deletes `done/` before retrying). Nit: "byte-for-byte" is asserted as `Record` equality, not file bytes | GAPS |
| Hygiene | Suite green twice under full parallelism; no stray `owl` processes after the runs; own tempdir per test, `OWLPOST_HOME`/`EDITOR` scrubbed; `notify`: N/A on this branch (OWL-012 not in base `main`, see round 2) | PASS |

Observation (not an AC, needs a product call): `owl edit` after a failed `send` is accepted (record still `drafted`) and the retried `send` reuses the already-spooled envelope, so the edit is silently not shipped (PROBE I). Since the envelope may already have been served from `outbox/`, reuse is the safe choice, but `edit` (or `send`) should say so — refuse `edit` when an outbox envelope for the question exists, or warn on the retry.

**Verdict: FAIL** (round 3 of the task, round 1 of 3 in this run). Code behaves as documented on every path probed, including the `remove_file` failure; AC5 now passes. AC3 fails only on coverage: the `remove_file` arm of `finish()` has no test and mutant M5 (error ignored) survives — add one test per caller (`send`, `reject`) using `chmod 0o555` on `spool/inbox` (skip if euid 0), asserting exit 1 with `removing …`, `done/` present, inbox untouched, then retry after `chmod 0o755` with the same answer id / `previous_state`.

FAILING: AC3 (`finish()` `remove_file` failure arm untested; mutant M5 survives; implementer's "not inducible without root" note is false — `chmod 0o555` on the inbox directory induces it)

Round 3b (2944334, 2026-09-02, fresh run round 2 of 3, verify slot 1, detached worktree `.worktrees/verify-OWL-010`, evidence files prefixed `r3b-`; AC3 only, diff `a5ffe1a...2944334` touches only `src/cli/{edit,mod,send}.rs` and `tests/inbox.rs`, so no test-planner rerun):

| Check | Evidence | Result |
|---|---|---|
| Gate: `cargo build`, `cargo test --workspace` ×2, clippy `-D warnings`, `fmt --check` | `r3b-build.log`, `r3b-test-workspace-run{1,2}.log` (176 tests, 0 failed, both runs, full parallelism), `r3b-clippy.log`, `r3b-fmt.log` — all exit 0 | PASS |
| AC3 `draft_send_moves_records` + `finish()` `remove_file` arm | Named test passes. New `send_with_unremovable_inbox_exits_1_and_retries_with_same_answer` and `reject_with_unremovable_inbox_exits_1_and_retries` use a `ReadOnlyInbox` guard (chmod 0o555 on `spool/inbox`, 0o755 restored in `Drop`, root detected by a write probe → skip with message). `r3b-guard.log`: run with `--nocapture` as uid 1000 prints no "skipping" line, both tests execute the real unlink failure; with a `panic!` injected after the failed `send`, the temp home is gone afterwards (tempdir cleanup succeeded), proving `Drop` restored the permissions. Assertions: exit 1, one stderr line `removing …/inbox/<id>.json`, inbox file byte-identical (`fs::read`) and record-equal, `done/` present with `answered`/`rejected` + `previous_state=consent`, one outbox envelope with `answer_id` matching; retry after unlock exits 0 with the same answer id, envelope record identical, `done/` rewritten, inbox gone, no `.tmp`. `finish()` doc comment now lists both intermediate states and states the error is never swallowed — matches behaviour. `existing_answer()` moved to `cli/mod.rs`; `owl edit` exits 1 naming `outbox/<aid>.json` and `owl send <id>` when an envelope for the question exists (editor never runs, `--json` empty stdout, retry ships the first envelope); negative twin edits a different drafted record while that envelope still exists → editor runs. Mutants (`r3b-mutants.log`, `--lib`/`--bin owl`/`--test inbox` separately): M5 `remove_file` error ignored → KILLED by both unremovable tests; M5b `remove_file` never called → KILLED (4 tests); E1 edit guard removed, E2 message lacks `owl send <id>` → KILLED by `edit_after_failed_send_is_refused_because_the_answer_is_spooled`; E3 `existing_answer` matches any envelope → KILLED (4 tests incl. the negative twin). Test-guard mutants: G1 guard chmods `done/` instead of `inbox/`, G2 perms restored before the assertions → both unremovable tests FAIL as required (the guard is load-bearing). 5/5 code mutants killed | PASS |
| test-reviewer | VERDICT: OK. Real intermediate-state and retry assertions; guard lives across all assertions so `Drop` covers panics; no skip on this host; edit refusal test fails on a5ffe1a. Notes (non-blocking): the two unremovable tests also pass on a5ffe1a since `finish()` code is unchanged there (they characterise required behaviour, which is what round 3 asked for); write-probe skip on a non-root mode-ignoring filesystem would report `ok` silently; `first_done_at` is dead; negative-twin comment says "(no envelope)" though an envelope for the other question exists (the state under test is the right one) | OK |
| Hygiene | Suite green twice under full parallelism; no stray `owl` processes from this run (two `owl watch` processes observed belong to the concurrent OWL-007 worktree); `notify` N/A as before | PASS |

**Verdict: PASS** (AC3; AC1, AC2, AC4–AC7 carried from round 3 / 9bb08dd with their named tests passing in both r3b runs). Follow-up nit for the next touch of `tests/inbox.rs`, not blocking: fix the negative-twin comment and drop `first_done_at`.

FAILING: none

## Escalation — round cap reached (2026-09-02, rounds=2)
Round 2 (9bb08dd) fixed the round-1 IndexMut panic and killed all three round-1 survivors, but
verification still FAILS on:
- AC3: `cli::finish()` reorders write-state-then-rename for atomicity, yet the failure path has no
  test (mutant F1 "rename before write" survives all three binaries). Probe: block `done/<id>.json`
  with a directory → `owl send` exits 1 leaving the record in `inbox/` in state `answered` with the
  answer already in `outbox/`; retry of `send` is refused ("state answered, not drafted") and
  `draft` rejects that state too — the record is wedged. `reject` retries but overwrites
  `previous_state` with "rejected".
- AC5: `owl history` arm for `Identity::load` returning Err (no key) is untested (mutant F8).
Fix shape for the next attempt: `finish()` must either rename first and update state in place
after (or write state to the moved record), or on rename failure roll the state back; add a
blocked-destination test per caller (send, reject) asserting the retry outcome; add a history test
with no identity key. Status set to `blocked`; `orch-state reset-stale` returns it to `todo`.

PR: https://github.com/Krab00/owlpost/pull/12 (draft, branch merged with main at 0ba3bd8, 215 tests green)
