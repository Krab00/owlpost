---
id: OWL-027
title: "Plugin: incoming messages shown with an owl icon in an orange frame"
status: done
deps: [OWL-023]
source: adhoc
source_updated_at: 2026-09-06
criteria_source: derived
---
## Description
User request 2026-09-06: when a message from a peer arrives (watch event, hook counter, a
question or answer shown by `/owlpost:inbox`), display it with an owl icon and in an orange
frame so it stands out from the rest of the conversation.

Claude Code renders assistant text as terminal markdown: no colours, no borders. The frame is
therefore built from characters. Convention (docs only, plus the hook counter line):

- **Counter and watch lines** get the icon: `🦉 owlpost: 1 new question (Krzysztof 1). ...`
  The `🦉 ` prefix is added in Rust to every `--format claude` and `--format plain` counter
  sentence (`src/cli/inbox.rs`), not to the arm sentence and not to the `- <peer> ...` record
  lines. The watch script emits the plain line, so watch events carry the icon for free.
- **Framed message block**, used by `/owlpost:inbox` for every question and answer it prints,
  and by the "Showing messages" table rule when a single message is shown outside a table:

  ```
  🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧
  🦉 **Krzysztof Abramczyk** · 09:08 · github.com/Krab00/owlpost · whole repository
  ```text
  Jaki masz ostatni commit u Siebie?
  ```
  🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧🟧
  ```

  Top and bottom line: 16 × `🟧`. Header: `🦉 **<peer>** · HH:MM · <project> · <path or
  "whole repository">` (fingerprint on the header for `consent` records). Body: the text
  verbatim in a code block, exactly as today. Drafts (our own text) are NOT framed: they keep
  the plain code block, so the orange frame always means "from a peer".
- **Where it is written**: SKILL.md gets a "Framed message" subsection under "Showing
  messages" with the block above; `commands/inbox.md` steps 2, 3 and 5 say "print the framed
  message block (see the skill)" instead of "in a code block with peer name..."; `commands/watch.md`
  event line example gains the icon; the answers table (step 5) keeps the per-peer colour
  marker rule and is not framed.
- Tests: `tests/plugin.rs` pins the `🦉 ` prefix on the counter output (plain and claude
  formats, zero-count still prints nothing) and the SKILL/inbox/watch literals; `tests/docs.rs`
  pins the README line that mentions the frame.

Out of scope: real ANSI colour (Claude Code strips it), Codex/Kimi output formats.

## Acceptance criteria
- [ ] AC1: `owl inbox --count --format plain` prints `🦉 owlpost: N new ...` (icon + space prefix) when N > 0 and nothing at zero; `--format claude` carries the same prefixed sentence in `additionalContext`; the arm sentence and the `- <peer> ...` record lines have no icon (modality: test)
- [ ] AC2: SKILL.md "Showing messages" contains a "Framed message" subsection with a 16 × `🟧` line, the header shape `🦉 **<peer>** · HH:MM ·`, the sentence that drafts are not framed, and the fingerprint-on-consent rule; pinned in `tests/plugin.rs` (modality: test)
- [ ] AC3: `commands/inbox.md` steps 2, 3 and 5 reference the framed message block and no longer say only "in a code block with peer name"; `commands/watch.md` event example reads `🦉 owlpost: 1 new answer from Maciek`; pinned in `tests/plugin.rs` (modality: test)
- [ ] AC4: README describes the owl icon and orange frame in one line; pinned in `tests/docs.rs` (modality: test)
- [ ] AC5: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

## Verification — task/OWL-027-owl-frame

Verifier: verify-OWL-027, 2026-09-06, commit 9e2b0e9 vs main 2929785, verify worktree `.worktrees/verify-OWL-027` (removed). Evidence: `OWL-027.evidence/verify-*.log`.

| AC | Verdict | Evidence |
|---|---|---|
| AC1 | PASS | `verify-hook-grid.log`: plain/claude/codex/kimi at N>0 all start with `🦉 `; all four print nothing at zero; real `hooks/owl-count.sh` via `/bin/sh` + `env -i`, parsed with jq: `additionalContext` starts with `🦉 ` (raw UTF-8, not `\u`-escaped); SessionStart at N>0 carries exactly one icon, arm sentence and `- Maciek ...` lines icon-free; SessionStart at zero = bare arm sentence; `owl inbox` listing has no icon; watch.md poll script run for real emits the icon line once. Tests: `tests/plugin.rs counter_wears_the_owl_icon_arm_and_records_do_not`, `tests/inbox.rs count_and_formats` / `hook_line_singular_and_per_peer_counts` (grid incl. codex/kimi × 0/1/2/3/4). Mutants M1–M5 RED. |
| AC2 | PASS | SKILL.md `### Framed message` under `## Showing messages`: two 16×🟧 lines, header shape, example header, fingerprint-on-consent line, drafts-not-framed line (whole-line equality), answers-table-not-framed. Pinned in `tests/plugin.rs skill_documents_the_framed_message`. Mutants M6–M9 RED. Note: the example nests ```` ```text ```` inside a ```` ``` ```` fence (as the spec prescribes); a strict CommonMark renderer closes the outer fence at line 187 and flips fence parity for the rest of the file. Harmless for the LLM reading raw text; a 4-backtick outer fence would fix it. |
| AC3 | PASS | inbox.md steps 2, 3, 5 contain `print the framed message block (see the skill, "Framed message")`, old phrase gone, step 4 untouched; watch.md example `` `🦉 owlpost: 1 new answer from Maciek` ``. Pinned in `tests/plugin.rs inbox_steps_print_the_framed_block_and_watch_event_has_the_icon`. Mutants M10, M11, M11b, M12 RED. |
| AC4 | PASS | README line 59 with all four literals; `tests/docs.rs readme_describes_the_owl_icon_and_orange_frame`. Mutant M13 RED. |
| AC5 | PASS | `verify-gate.log` / `verify-test-full.log`: fmt 0, build 0, clippy -D warnings 0, `cargo test --workspace --no-fail-fast` 458 passed / 0 failed / 1 ignored; no Cargo.toml/Cargo.lock change. One `e2e manual_loop` failure in the first run was a stale shared binary (sibling verifier rebuilt `target/debug/owl` from an icon-free branch); green alone and in the full rerun. |

FAILING: none.

test-reviewer: OK (all ACs pinned with equality assertions; grid complete across plugin.rs + inbox.rs; notes codex/kimi also carry the icon, pinned as actual behaviour).
test-planner: 24 rows (8 happy / 16 edge, 6 E2E, 1 can't-induce = live Claude rendering of the frame). Gaps closed by the verifier: codex/kimi at zero (covered by `count_and_formats`), watch.md poll script executed for real, pre-existing doc tests run in the full suite.

Mutants (15/15 RED): M1 ICON="" · M2 icon on arm sentence · M3 icon on preview lines · M4 ICON without space · M5 icon stripped in plain/kimi · M6 17×🟧 · M7 example header without `🦉 **` · M7b header shape without `🦉 **` · M8 fingerprint-on-consent line removed · M9 drafts-not-framed removed · M10 inbox.md step 2 reverted · M11 step 5 reverted · M11b step 3 reverted · M12 watch.md example without icon · M13 README line removed.

Notes (non-blocking): codex/kimi counter also carries the icon (task said out of scope, not forbidden); SKILL.md "Live watch" line 122 still reads `owlpost: 1 new answer from Maciek` without the icon (left for the OWL-026 merge); fence nesting in the Framed message example (see AC2).

Draft PR: https://github.com/Krab00/owlpost/pull/33 (round 2 at 5faa971: four-backtick outer fence, mutant RED)
