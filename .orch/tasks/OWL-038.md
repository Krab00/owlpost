---
id: OWL-038
title: "Threads for people: a per-record event log in the spool, `owl thread` (peer list + chronological timeline) and the mod's inbox as a thread list"
status: done
deps: [OWL-034]
source: adhoc
source_updated_at: 2026-09-14
criteria_source: derived
---
## Description

Decision (2026-09-14): the inbox stops being a flat list of records and becomes a **per-person
thread view**. Opening a person shows the whole conversation with them, in both directions and
in one chronological order: what they asked me, what I asked them, what each of us answered,
and what happened to every request on my side — held for consent, allowed or denied, drafted by
which harness or by the human, edited, sent, rejected. Today only one kind of request exists
(a question, OWL-034); OWL-039 (content request) and OWL-040 (tool-call request) each add a
kind to the same timeline. This task builds the timeline and its storage so those two are
additive: the event `kind` is an **open string**, never an enum the renderer matches
exhaustively.

Nothing here changes the wire: no new payload field, no new route, no new state. The daemon
keeps its behaviour; the spool gains a log of what already happened.

### 1. The event log — `meta.events` on the record (`src/events.rs`, new)

**Where**: an array under the record's existing `meta` object, not a sidecar file.
`Spool::move_to` (`src/spool.rs:168`) is a bare `rename` of `<id>.json`, so a sidecar would need
its own atomic move at every `move_to`/`finish` caller and would inherit the blocked-destination
failure-path matrix of OWL-003/010/011 at each of them; `meta` already travels with the record
for free and already carries `peer`, `hash`, `answer_id`, `done_at`, `auto_error`.

```json
"meta": { "peer": "owl:…", "hash": "…", "events": [
  { "ts": "2026-09-14T10:00:00Z", "kind": "received" },
  { "ts": "2026-09-14T10:00:00Z", "kind": "held" },
  { "ts": "2026-09-14T10:04:11Z", "kind": "allowed", "by": "human", "detail": { "scope": "once" } },
  { "ts": "2026-09-14T10:04:40Z", "kind": "drafted", "by": "claude", "detail": { "redactions": 1 } },
  { "ts": "2026-09-14T10:05:02Z", "kind": "sent", "by": "human", "detail": { "answer_id": "0191…" } }
] }
```

- `src/events.rs`: `pub struct Event { ts: String, kind: String, by: Option<String>, detail:
  Option<Value> }` (`skip_serializing_if = "Option::is_none"` on both optionals, so an event
  without them is two keys), `pub fn push(rec: &mut Record, kind: &str, by: Option<&str>, detail:
  Option<Value>)` (stamps `envelope::rfc3339_now()`, appends through `answer::meta_object`, so a
  non-object `meta` is repaired exactly as today), `pub fn of(id: &str, rec: &Record) -> Vec<Event>`
  (the stored log, or the derived one below when `meta.events` is absent, empty or not an array).
- `EVENTS_MAX: usize = 200`. `push` drops the oldest event beyond the cap, never the first
  (`received` / `asked` anchors the timeline). A `state-seen` event is appended only when its
  `detail.state` differs from the previous `state-seen` of the same record, so a tight
  `owl status --wait` poll cannot fill the log.
- **Write points** — two shared helpers plus the three places a record is born, and nowhere else:
  - `Spool::set_state_with_event(dir, id, state, kind, by, detail)` — one `get` + `push` +
    `put`, used by `src/cli/allow.rs:59` (`allowed`, `by: "human"`, `detail: {"scope":
    "once"|"always"}`), `src/pull.rs:258` (`answer-received`) and `src/pull.rs:413` (`expired`).
    `Spool::set_state` stays for callers with nothing to log (`src/server.rs:740` ack).
  - `answer::finish(spool, id, rec, state, extra, event)` gains the event as a parameter (it is
    the single place a record reaches `done/`): `sent` from `answer::send`, `rejected` from
    `src/cli/reject.rs:14`, `denied` from `src/cli/deny.rs:21`, `declined` from `src/pull.rs:279`.
  - Record birth: `src/server.rs::post_question` pushes `received` and then `held` (state
    `consent`) or nothing (state `pending`); `src/pull.rs` pushes `answer-received` on a pulled
    answer; `src/cli/ask.rs` pushes `asked` on the new `asks/` record.
  - `drafted` (`by`: the harness name, or `human` for `owl draft --text`, `agent` for
    `--text --agent`; `detail: {"redactions": <n>, "status": "ok"|"timeout"|"extract_failed"}`)
    is pushed in `answer::draft_*` where `AUTO_ERROR` is already removed
    (`src/answer.rs:276`, `:334`); `edited` (`by: "human"`) in `src/cli/edit.rs`.
  - `state-seen` (`by: "peer"`, `detail: {"state": "TASK_STATE_WORKING", "text": "the owner is
    reviewing the answer"}`) is pushed on an `asks/` record by `owl status` and by
    `owl ask --wait` whenever the peer's Task text changes, and by the daemon's `pull_once`.
- **Legacy records** (nothing writes to them, `of()` derives on read — a read-only command must
  never migrate on read): one event at `rec.received_at`, `received` in `inbox/`/`outbox/`/`done/`
  and `asked` in `asks/`; plus, when `meta.done_at` is present, one event at that timestamp whose
  kind comes from `rec.state`: `answered` → `sent`, `rejected` → `rejected`, `denied` → `denied`,
  `declined` → `denied` (`detail: {"by": "peer"}`), `acked`/`expired` → `sent`, `answered` on an
  `asks/` record → `answer-received`, anything else → no second event. A record that has a stored
  log is never mixed with a derived one.

### 2. `owl thread` — `src/cli/thread.rs` (new), §9 row

**`owl thread [--json]` — the peer list.** One row per peer fingerprint seen in `inbox/`,
`outbox/`, `asks/` or `done/`:

```json
[ { "from": "owl:k7q2m3xz9pdw4hrt", "from_name": "Ania Kowalska",
    "last_ts": "2026-09-14T10:05:02Z", "unseen": 2, "open": 1,
    "last_summary": "Why is the refresh token rotated on every read?" } ]
```

`unseen` = inbox records of that peer with `seen == false`; `open` = inbox questions in state
`consent`, `pending` or `drafted`; `last_summary` = `cli::first_line(text, SUMMARY_CHARS)` of the
newest record. Sorted by `last_ts` descending, `from_name` ascending as the tiebreak (OWL-007:
the fixture must order differently under the two keys). Human format is the `owl inbox` table
renderer with the columns `PEER  UNSEEN  OPEN  LAST  SUMMARY`; nothing to show → exit 4
`no threads`.

**`owl thread <fingerprint|name> [--json] [--since <age|date>] [--context <context_id>]` — the
timeline.** The peer is resolved with `ContactBook::resolve` (§5, all four forms). Every record
of that peer in `inbox/`, `outbox/`, `asks/` and `done/` contributes its events; each event
becomes one row:

```json
{ "ts": "2026-09-14T10:04:40Z", "kind": "drafted", "dir": "in", "record_id": "0191…",
  "context_id": "0191…", "type": "question", "state": "drafted",
  "project": "github.com/company/monorepo", "path": "src/auth/session.rs",
  "text": "<the full question or answer text>", "context": "<body.context, when present>",
  "harness": "claude", "by": "human", "detail": { "redactions": 1 } }
```

- `dir` is `in` for a record the peer sent us (`inbox/`, `done/` questions, pulled answers) and
  `out` for one we produced or sent (`asks/`, `outbox/`, our answers). `text` is the **full**
  question or answer text, never cut — the mod truncates for the eye. `context`, `harness`,
  `by` and `detail` are omitted when absent. `kind` is an open string: an unknown value passes
  through `--json` untouched and renders as its own name in the human format.
- Sorted by `ts` ascending, `record_id` then the event's index in its record as tiebreaks, so
  two events stamped in the same second keep their real order.
- `--since` takes the same argument `owl history --since` takes and drops earlier events;
  `--context <id>` keeps only events of records whose payload `context_id` matches. Both filters
  may be combined; an empty result is exit 0 with an empty array (`[]`) or the header line alone.
- Human format: one header line
  `owl:k7q2m3xz9pdw4hrt — Ania Kowalska · 7 events · last 12:04`, then one block per event,
  blocks separated by one blank line:
  - an event that carries a **peer** message (`kind` `received` or `answer-received`, `dir` `in`)
    prints the `--format claude` message table of that record through `src/render.rs`
    byte-for-byte, so the §9 rule "a table always means from a peer" holds;
  - everything else — our own drafts, our sent answers, our asks, and every bookkeeping event —
    prints one line `HH:MM <kind>[ · by <by>][ · <detail k=v, …>]` and, when the event carries
    our own text (`drafted`, `sent`, `asked`), that text in a plain ```` ```text ```` block, the
    shape `owl show --format claude` already uses for a draft.
- `owl thread` is **read-only**: it never opens a socket, never runs a harness, never marks a
  record seen, never writes into the spool (no routing release, no `seen = true`, no migration
  of a legacy log).

### 3. `context_id` in the existing listings

`owl inbox --json` and `owl history --json` gain one key each, `"context_id"` (the payload's
thread id, `null` when absent), appended to `cli::summary` and to the `history` row after
`path`. Additive: no key is renamed or removed, the human tables are unchanged.

### 4. The mod — `plugins/claude-code/hooks/owlpost.tsx`

The Inbox view becomes two views, both fed by `owl … --json` through the existing `json()`
helper; Contacts and Ask are untouched, and the band above the prompt is untouched.

- `threads` (what the band's **Inbox** button and the Inbox tab now open): rows from
  `owl thread --json`, one `Box` per peer with `Text` `<name> · <unseen> unseen · <open> open`,
  a dim second line with the fingerprint and `last_summary`, and a `Button` **Open** that sets
  `openPeer = row.from` and switches to `thread`.
- `thread`: rows from `owl thread <fingerprint> --json` drawn oldest first, each event one
  `Box` — time, kind, and the text cut to three lines with `<Text wrap="truncate">`. The action
  buttons sit **next to the open request only** (the newest event of a record whose `state` is
  `consent`, `pending` or `drafted`), never on every row: **Allow once** / **Allow always** /
  **Deny** on `consent` (each label carrying the fingerprint, OWL-036), and **Draft (Claude)**
  (`$.command.run({ command: 'owlpost:draft', args: id })`) / an `Input` for the human's own
  answer (`owl draft <id> --text`) / **Send** / **Reject** on an open question. A **Back**
  button returns to `threads`.
- Only `Box`, `Text`, `Button` and `Input` from `$.ui.resolve(e)` are used, with the props the
  file already uses (`flexDirection`, `gap`, `marginBottom`, `borderStyle`, `paddingX`,
  `flexGrow`, `color`, `dimColor`, `bold`, `wrap`; `Input`: `key`, `label`, `placeholder`,
  `value`, `submitLabel`, `onInput`, `onSubmit`). No new component, no new prop.
- `refresh()` fetches the thread list next to the count and the contacts; the open thread is
  refetched by `act()` after every action, so a press shows its result without a model turn.
- The classic hook path is unchanged: `hooks/hooks.json`, `hooks/owl-count.sh`,
  `/owlpost:inbox` and the `FileChanged` wake keep working exactly as today with mods off.

Out of scope, noticed while reading (a separate task, not fixed here): the Ask view's `send()`
builds `owl ask --peer <fp> --file <path> <question>`, but §9 defines `--file` as the git-blame
peer proposal and the path as a positional argument, so the Ask view sends the wrong argv today.

### 5. Plugin text and docs

- `plugins/claude-code/commands/thread.md` (new, `allowed-tools: Bash(owl thread:*)`): run
  `owl thread` for the list, `owl thread <peer>` for one conversation, paste the output
  verbatim. Without it three parity tests fail (`tests/plugin.rs::every_subcommand_has_a_command`,
  `::commands_have_descriptions`, `::readme_and_skill_list_every_command`), so the README command
  table gains its row and `skills/owlpost/SKILL.md` names `/owlpost:thread` in the same PR.
- `commands/inbox.md` and the skill keep their flow; one sentence each points at
  `/owlpost:thread` for the history of one person. The consent rules of OWL-036 are untouched.
- `plugins/claude-code/README.md` "Claude Code mods": the pane's inbox is described as a thread
  list that opens one conversation, with the actions on the open request only.
- `docs/technical-design.md` §8 (the `meta.events` log, its kinds, the write points, the derived
  log for legacy records), §9 (the `owl thread` row, `context_id` in the two `--json` listings);
  `docs/guide.md` a new section 3.11 "The whole conversation with one person".

## Acceptance criteria
- [ ] AC1: `events::push` appends `{ts, kind}` (plus `by`/`detail` when given) to `meta.events` through `answer::meta_object`, repairs a non-object `meta`, omits absent optionals from the JSON, drops the oldest event beyond `EVENTS_MAX` while keeping the first, and skips a `state-seen` whose `detail.state` equals the previous `state-seen`'s; `events::of` returns the stored log when present (modality: test)
- [ ] AC2: a question that arrives at `consent`, is allowed `--once`, drafted with the fake harness and sent carries, in `done/`, exactly the kinds `received, held, allowed, drafted, sent` in that order, with `by` `human` on `allowed`/`sent`, `by` the harness name on `drafted` and `detail.scope == "once"` on `allowed`; the twin paths write `denied` (`owl deny`) and `rejected` (`owl reject`) (modality: test)
- [ ] AC2b: `owl edit <id>` with `EDITOR` set to `tests/fixtures/fake-editor.sh` (a script that appends one line to the file it is given and exits 0) pushes one `edited` event with `by: "human"` onto the record, between the `drafted` and the `sent` events of AC2's sequence; the editor script's own log proves it ran (modality: test)
- [ ] AC3: `events::of` on a record with no `meta.events` derives `received` at `received_at` (`asked` in `asks/`) plus, when `meta.done_at` is set, the second event of the state table (`answered` → `sent`, `denied` → `denied`, `rejected` → `rejected`, `declined` → `denied` with `detail.by == "peer"`), and a legacy record still lists in `owl thread <peer>` with that derived `received` event; reading a legacy record leaves its file byte-identical (modality: test)
- [ ] AC4: `owl thread --json` returns one row per peer with `from`, `from_name`, `last_ts`, `unseen`, `open`, `last_summary`, sorted by `last_ts` descending with `from_name` as the tiebreak (fixture ordered differently under the two keys); the human format prints the `PEER  UNSEEN  OPEN  LAST  SUMMARY` table; no records → exit 4 `no threads` (modality: test)
- [ ] AC5: `owl thread <peer> --json` on a home whose `inbox/`, `outbox/`, `asks/` and `done/` each hold a record of that peer returns every event of all four in `ts` ascending order (fixture inserted out of order, with two events sharing a second to exercise the tiebreak), each row carrying `ts`, `kind`, `dir`, `record_id`, `type`, `state`, `project`, `path` and the **full** `text` (a text longer than `SUMMARY_CHARS` comes back uncut), `context_id` and `context` where the payload has them, `harness`/`by`/`detail` where the event has them and absent where it does not; a second peer's records never appear (modality: test)
- [ ] AC6: an event whose `kind` is a string this version does not know (`"content-requested"` written into `meta.events` by hand) passes through `--json` unchanged and renders in the human format as its own name, without a panic and without dropping the events around it (modality: test)
- [ ] AC7: `owl thread <peer>` human format prints the peer header line, the `--format claude` message table of the peer's question byte-identical to `owl show <id> --format claude`, and our own draft text in a ```` ```text ```` block and never in a table; `--since` drops earlier events and `--context <id>` keeps only that thread's, each with a negative twin proving the excluded event exists in the unfiltered call (modality: test)
- [ ] AC8: `owl thread` (both forms) opens no socket, runs no harness and writes nothing: run against a home whose contact's `endpoints` point at a listener that counts connections (count stays 0), with `responder.harness = fake` and `$FAKE_HARNESS_LOG` set (the file is never created), and with every spool file's bytes compared before and after; `seen` stays `false` on an unseen record and its `spool/routing/<id>.json` and wake file survive (modality: test)
- [ ] AC9: `owl inbox --json` and `owl history --json` each carry `context_id` (the payload's value for a threaded record, `null` for one without), every pre-existing key is unchanged, and the plain tables are byte-identical to before (modality: test)
- [ ] AC10: `plugins/claude-code/commands/thread.md` exists, wraps `owl thread`, and the OWL-021 parity tests (`every_subcommand_has_a_command`, `commands_have_descriptions`, `readme_and_skill_list_every_command`) pass with `thread` added; `tests/plugin.rs` pins, single-line: in `README.md` the mods section's `the pane's Inbox is a thread list`, in `commands/thread.md` `owl thread <peer>` and `paste its output verbatim`, in `SKILL.md` `/owlpost:thread`, in `commands/inbox.md` the pointer line at `/owlpost:thread`; each pin goes red when its literal alone is removed (modality: test)
- [ ] AC11: `docs/technical-design.md` §8 documents `meta.events` (shape, the event kinds, the write points, the derived log) and §9 the `owl thread` row and the two new `--json` keys; `docs/guide.md` gains section 3.11; `tests/docs.rs` pins `meta.events`, `owl thread`, `state-seen` and `EVENTS_MAX` in the design doc, each on the executable/table line and not in prose (modality: test)
- [ ] AC12: with mods on, the pane's Inbox tab lists one row per peer, **Open** draws the conversation oldest first, the Allow/Deny buttons appear only next to a `consent` record and the Draft/Send/Reject buttons only next to an open question, and **Back** returns to the list (checked by the user in a real session; the transcript is the evidence) (modality: manual)
- [ ] AC13: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass; no new crate (modality: data)

### Open questions for the operator
1. `owl thread` shows every peer that ever wrote, including one whose contact has since been removed (the name falls back to the fingerprint). Should a removed contact's thread stay listed, or be hidden behind a flag?
2. The timeline shows the full text of every message. On a long thread the human format can run to hundreds of lines; is a `--last <n>` cut wanted now, or is `--since` enough?

## Verification — task/OWL-038-threads

Verified at `65dc2eb` (base `03310ae`) in a throwaway worktree with its own `target/`
(`CARGO_TARGET_DIR` unset). Evidence: `.orch/tasks/OWL-038.evidence/`
(`build.log`, `test-branch-full.log`, `test-base-docs.log`, `clippy.log`, `fmt.log`,
`test-names.txt`, `mutants.log`). All 24 claimed test names confirmed present via
`cargo test --workspace -- --list`.

| AC | Verdict | Evidence |
|----|---------|----------|
| AC1 | PASS | `events::tests::{push_appends_the_documented_shape_and_repairs_meta, push_caps_the_log_and_keeps_the_first_event, state_seen_dedupes_against_the_previous_state_seen_only, of_returns_the_stored_log_unchanged}`; mutants M2 (cap off-by-one), M3 (drop index 0), M4 (dedupe off), M5 (dedupe vs any event), M10 (`of` ignores stored log) all RED |
| AC2 | PASS | `tests/thread.rs::{allowed_drafted_and_sent_writes_the_documented_log, denied_and_rejected_twins}`; mutants M17 `received`, M18 `held`, M24 `detail.scope`, M25 `allowed` by, M26 `drafted` by-harness, M39 `rejected` kind, M40 `denied` kind, M41 `finish` drops event — all RED. Note: the `asked`/`answer-received`/`declined`/`expired` write points named in the Description (not in AC2's frozen text) are uncovered — see FAILING note |
| AC2b | PASS | `tests/thread.rs::edit_pushes_one_edited_event_between_drafted_and_sent` with `tests/fixtures/fake-editor.sh`; mutant M19 (drop the `edited` push) RED |
| AC3 | PASS | `events::tests::{of_derives_the_state_table_for_legacy_records, of_falls_back_when_the_stored_log_is_empty_or_not_an_array}`, `tests/thread.rs::legacy_records_derive_their_log_and_are_never_rewritten`; mutants M6 (`asks/answered`), M7 (`declined` detail), M8 (`acked`/`expired`), M9 (`asked` birth) RED |
| AC4 | PASS (with gap) | `thread_list_rows_and_sort_order`, `thread_list_without_records_exits_4`; mutants CALIB (sort asc), M1 (drop `from_name` tiebreak), M11 (`unseen`), M12 (`open`), M13 (exit 4→5) RED. Gap: M16 (swap the UNSEEN and OPEN cells in the human table) SURVIVED — only the header and the PEER column are asserted |
| AC5 | PASS | `thread_timeline_merges_four_directories_in_order`, `thread_timeline_carries_the_asker_snippet`; mutants M14 (drop `record_id` tiebreak), M15 (descending), M29 (`context_id`→null), M30 (`dir` in/out flip), M38 (drop `by`) RED |
| AC6 | PASS | `unknown_event_kind_passes_through_and_renders`; mutant M35 (drop any kind not in a known list) RED |
| AC7 | PASS | `thread_human_format_tables_peer_messages_and_fences_ours` asserts `thread.contains(show.trim_end())` over the whole multi-line `owl show --format claude` output (real byte-identity, not a substring of one line); `thread_since_and_context_filter_with_negative_twins`; mutants M36 (peer table → plain line), M37 (our text → table), M27b (`--since` comparison flipped), M28 (`--context` filter off) RED. `render::draft_block` refactor keeps the `draft:\n` prefix. Verified by reading the assertion and by mutation, NOT by a hand diff of the two commands |
| AC8 | PASS | `thread_is_read_only` binds a real `TcpListener` with an `AtomicUsize` counter and a positive control (`owl card` must raise it), arms the fake harness via `responder.harness = "fake"` + `$FAKE_HARNESS_LOG`, and diffs every spool file's bytes before/after; my mutant that makes `owl thread` push an event into each inbox record turned 7 of 14 thread tests RED, `thread_is_read_only` among them |
| AC9 | PASS (with gap) | `inbox_and_history_json_carry_context_id`; mutants M31 (`inbox` `context_id`→null), M32 (`history` `context_id`→null) RED; pre-existing keys and the plain tables are guarded by the pre-existing suites — M43 (`path`→null) and M44 (extra CONTEXT column) RED under `--test inbox --test cli`. Gap: the AC9 test itself only checks key presence with `is_some()` and matches `ctx-9` against the whole array, not the row that carries it |
| AC10 | PASS | `tests/plugin.rs::thread_command_and_docs_pin_their_literals` + the three OWL-021 parity tests; pin mutants P5 (README mods line), P6 (`paste its output verbatim`), P7 (SKILL `/owlpost:thread`), P8 (inbox pointer) RED; removing `commands/thread.md` turns `every_subcommand_has_a_command` and the pin test RED |
| AC11 | PASS | `tests/docs.rs::thread_and_event_log_pinned_in_the_design_doc`; pin mutants P1 `meta.events`, P2 `EVENTS_MAX`, P3 `state-seen`, P9 the `owl thread` §9 row (each on a `| \`…\` |` table line), P4 guide 3.11 — all RED |
| AC12 | PM-REVIEW | modality `manual`; the user checks the mod in a real session |
| AC13 | PASS (with note) | `cargo build` 0, `cargo clippy --all-targets -- -D warnings` 0, `cargo fmt --check` 0; `cargo test --workspace --no-fail-fast` = 644 passed / 5 failed, all five in `tests/docs.rs` and PRE-EXISTING: the same five fail on base `03310ae` (`cargo test --test docs` there: 13 passed / 5 failed, identical names). The branch's docs target is 14 passed / 5 failed, so the new AC11 pin passes and no failure is new |

Pre-existing `tests/docs.rs` failures (red on base `03310ae`, out of OWL-038's scope — worth a
separate task): `a2a_docs_pin_card_task_state_thread_and_context`,
`readme_answer_paragraph_names_draft_and_send`, `readme_describes_the_owl_icon_and_the_message_table`,
`readme_quickstart_steps_in_order`, `update_docs_name_the_captured_path_and_the_bootstrap`.

Mutant sweep: 44 distinct mutations / 48 runs (4 rescored after the pre-existing `tests/docs.rs`
red wrongly scored `--workspace` runs, 2 discarded as compile failures) -> 42 trustworthy verdicts,
37 RED, 5 SURVIVED — M16 (swap UNSEEN/OPEN cells in the `owl thread`
human table, AC4), M20 (delete both `events::push` calls in `src/cli/ask.rs` — `asked` and
`answer-received`), M21 (delete `answer-received` in `src/pull.rs:179`), M22 (`declined` written
with `set_state` instead of `set_state_with_event`), M23 (same for `expired`). M42 (`path`→null
under `--test thread` alone) also survived but is caught by the pre-existing inbox suite (M43).

Flakes observed under `cargo test --workspace` (unrelated to this change, not reproducible on
re-run): `notify::tests::enabled_with_binary_runs_the_override`, `doctor_reports_each_check`.

### Round 2 (`c29b1e5`)

Test-only commit on top of `65dc2eb` (`tests/ask.rs`, `tests/pull.rs`, `tests/thread.rs`; no
source file touched — `git diff --stat 65dc2eb...c29b1e5`). Verified in a fresh throwaway
worktree with its own `target/`. Evidence: `r2-test-full.log`, `r2-clippy.log`, and the
`### ROUND 2` block in `mutants.log`.

All five round-1 survivors are now RED, each killed by the claimed test:

| Mutant | Round 1 | Round 2 | Killed by |
|--------|---------|---------|-----------|
| M16 swap the UNSEEN/OPEN cells in the `owl thread` table | SURVIVED | RED | `thread_list_rows_and_sort_order` |
| M20a drop the `asked` push on the accepted-ask path | SURVIVED | RED | `tests/ask.rs::accepted_ask_is_born_with_an_asked_event` |
| M20b drop `asked` + `answer-received` on the sync-answer path | SURVIVED | RED | `tests/ask.rs::answered_ask_is_born_with_asked_then_answer_received` |
| M21 drop `answer-received` in `pull::store_answer` | SURVIVED | RED | `tests/pull.rs::pulled_answer_writes_answer_received_on_both_records` (scoped to `--test pull`) |
| M22 `declined` via `set_state` instead of `set_state_with_event` | SURVIVED | RED | `tests/pull.rs::declined_ask_carries_a_declined_event` |
| M23 `expired` via `set_state` | SURVIVED | RED | `tests/pull.rs::expired_outbox_entry_carries_an_expired_event` |

AC9 is now tied row-wise: nulling `context_id` in `src/cli/mod.rs` and in `src/cli/history.rs`
each turns `inbox_and_history_json_carry_context_id` RED, and the test uses `row_for(&inbox,
"threaded")["context_id"]` instead of the old array-wide `contains`. AC4's gap and AC9's gap are
closed; AC4 and AC9 are plain PASS.

Gates on `c29b1e5`: `cargo build` 0, `cargo clippy --all-targets -- -D warnings` 0,
`cargo fmt --check` 0, `cargo test --workspace --no-fail-fast` = 649 passed / 5 failed — the same
five pre-existing `tests/docs.rs` failures listed above, nothing new. `cargo test --test pull`
run three times: 18 passed each time, no flake.

The `acked_on` helper (`tests/pull.rs`) replaces `get(Dir::Done, &ans.id).is_some()` with
`.is_some_and(|r| r.state == "acked")` at three call sites. That is strictly narrower, so it
cannot produce a false green; it fixes a real race, since `src/server.rs` moves the file into
`done/` and only then sets the state, letting the old predicate fire one step early.
`wait_until` panics on timeout, so a narrower predicate can only fail loudly.

test-reviewer round 2: **OK** — all six gaps CLOSED, `acked_on` judged a faithful race fix with
no weakened assertion. One nit, not blocking: both new `events_of` helpers assert the event
timestamp only as "a string ending in Z".

AC12 remains PM-REVIEW (manual). AC13 remains PASS-with-note for the five pre-existing docs reds.

Draft PR: https://github.com/Krab00/owlpost/pull/55 (base `task/OWL-038-040-specs`, opened 2026-09-15)
