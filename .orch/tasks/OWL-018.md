---
id: OWL-018
title: "owl ask: the path is optional (repo-level questions)"
status: done
deps: [OWL-017]
source: adhoc
source_updated_at: 2026-09-05
criteria_source: derived
---
## Description
Finding from the second cross-machine test (2026-09-05): `owl ask` and `/owlpost:ask` require a
`<path>`, so a question about the repository as a whole ("how long is your README?", "which
branch do you deploy from?") has to be pinned to an arbitrary file. The path exists to seed the
responder's context and to let `--file` propose peers from `git blame`; it should not be a
precondition for asking at all.

Make the path optional end to end: `owl ask <peer> "<question>"` is valid, the envelope carries
`path: null` (or omits it), the responder's read-only session starts from the repo root with no
file hint, `owl inbox` / `owl history` / `owl show` print `-` where the path would be, and the
question hash covers the empty path so the dedup rule still holds. `--file` keeps its meaning
(peer proposal from blame + path). The plugin command `commands/ask.md` parses
`<peer> [path] <question...>` and no longer asks for a missing path.

## Acceptance criteria
- [ ] AC1: `owl ask <peer> "<question>"` (no path) returns `accepted <id>` — integration test `tests/ask.rs::ask_without_path` over the fixture daemons; the stored question has no path and `owl show <id>` on the responder prints `path: -` (modality: test)
- [ ] AC2: the responder's runner receives no file hint for such a question and still answers from the checkout — `tests/runner.rs` case with the fake harness asserting the prompt contains the project and question but no path line (modality: test)
- [ ] AC3: `envelope::question_hash` with `None` path is stable and differs from the same question with a path — unit test (modality: test)
- [ ] AC4: `plugins/claude-code/commands/ask.md` documents `<peer> [path] <question...>`; `tests/plugin.rs` checks the argument-hint (modality: test)
- [ ] AC5: `docs/technical-design.md` CLI contract and the question payload schema mark `path` optional; `tests/docs.rs` passes (modality: data)
- [ ] AC6: `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` pass (modality: data)

## Verification — task/OWL-018-optional-path

Head 69befde on main 45b8309; worktree `.worktrees/OWL-018` clean before and after every mutant. Evidence: `.orch/tasks/OWL-018.evidence/`.

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| G | `cargo build` / `cargo test --workspace` / `cargo clippy --all-targets -D warnings` / `cargo fmt --check` | PASS | `gate-*.txt`, `gate-rc.txt` (all rc=0; 128 lib + all integration binaries ok) |
| AC1 | `owl ask <peer> "<q>"` → `accepted <id>`; asks record has no `path` key, `meta.hash == question_hash(P, None, q)`; responder `owl show` prints `path:     -`, `inbox --json` row `path: "-"`; negative twin `owl ask Bea` → `missing <question>` (not `<path>`); positive twin with PATH unchanged | PASS | `tests/ask.rs::ask_without_path` (real binary + fixture daemon, `ac-tests.txt`); manual `manual-usage.txt`: `owl ask Bea` → rc 1 `missing <question>` + new usage line; `owl ask` → `missing <peer>` |
| AC2 | runner prompt without path has no `File:` line, byte-exact prompt pinned, fake harness argv has project + question, pwd = checkout | PASS | `src/runner.rs::prompt_without_path_has_no_file_line` (exact string), `tests/runner.rs::prompt_without_path_has_no_file_line_and_runs_from_checkout`; mutant M10 (`File: -` for None) killed |
| AC3 | `question_hash(p, None, q)` stable, ≠ `Some("f")`, ≠ other project, pinned `a4ebf08f…1c08` | PASS | unit test; pin re-derived independently: `printf 'p\n\nwhy x?' \| shasum -a 256` = `a4ebf08f97de160963f99fec4925a88637f5bc246e2836b4d5754a86bbce1c08`; mutant M02 killed |
| AC4 | `ask.md` argument-hint `"<peer> [path] <question...>"`, pinned by equality in `tests/plugin.rs::commands_have_descriptions`; body documents both invocations, no "ask for a missing part" | PASS | `ac-tests.txt`; mutant M18 (hint back to `<path>`) killed |
| AC5 | `docs/technical-design.md`: payload schema line 181 + §"body.path is optional" line 187, CLI table row 266 `[path]`, hash rule "missing path hashes as the empty string", §10 prompt "File: omitted entirely"; `tests/docs.rs` 4/4 | PASS | grep in transcript; `ac-tests.txt` |
| AC6 | gates | PASS | see G |
| W1 | Wire compat: old-peer JSON with `path` still parses (`fixed_question` golden, `tests/server.rs::question_is_accepted_and_spooled`); omitted key and `"path": null` both parse to `None` (unit); `--file <path>` still sets `Some(path)` (`file_with_peer_skips_blame_and_sends`); server accepts missing `body.path` → 202 spooled as `None`, non-string path → 400 bad schema | PASS | `ac-tests.txt`; mutants M11 (re-require path), M12 (emit `path:null`) killed |
| W2 | Every `-` fallback: show, history, `body_path` (inbox/history rows + `--format`), daemon `question_path` (notify `asks about -` e2e), auto outcome, `pull::open_ask` | PASS | mutants M03–M08 killed |
| W3 | Positional parse boundary `rest.len() >= 2` → `> 2` | PASS | M01 killed (`cli::ask` unit + `ask_without_path`) |
| W4 | Commit hygiene: files tests open are tracked, no vendored/target paths in diff, tree clean | PASS | `git status` clean; diff touches only src/tests/docs/plugins |
| M | **Mutant sweep**: 18 mutants — 13 killed, 2 equivalent (M09 `--wait` `OpenAsk.path` is not observable in the CLI path: `ingest_envelope` never reads it; M13 dropping `serde(default)` is a no-op for `Option`), **3 survived on the responder's manual/dedup path**: M14 `answer::draft` passes `path.or(Some("-"))` to the runner (a manual `owl draft`/`owl answer` on a no-path record would get a `File: -` line) — no test drives `answer::draft`/`owl draft` with `path: None` and inspects the harness prompt; M15 `answer::send` hashes `path.or(Some("-"))` — the responder cache key written by a manual `owl send` of a repo-level answer is never asserted; M16 `server::post_question` hashes `path.as_deref().or(Some("-"))` — the spooled `meta.hash` / responder cache lookup for a repo-level question is never asserted (no `cache_hit_returns_answer` row under the `None` hash, `bad_inputs` spools it but does not check `meta.hash`). All three survived the full `cargo test --workspace` (`mutants.txt`, `mutants-recheck.txt`, `mutant-M16.txt`) | **FAIL** | Fix: (a) `tests/inbox.rs`: `draft`+`send` a `path: None` record with the fake harness, assert the argv has no `File:` and the cache key is `question_hash(PROJECT, None, q)`; (b) `tests/server.rs`: assert `meta.hash == question_hash(PROJECT, None, …)` on the spooled no-path record and one `cache_hit_returns_answer` row seeded under the `None` hash (200) whose `Some(PATH)` twin is a 202 |
| R | test-reviewer verdict: GAPS — (1) responder dedup under `None` hash (= M16), (2) manual draft/send on a no-path record (= M14/M15), (3) `--wait` no-path row (= M09, unobservable), (4) HTTP-level `"path": null` only unit-covered, (5) `tests/inbox.rs` table rows assert `contains(" - ")` rather than the path column | FAIL (1,2 binding; 3–5 minor) | see M |
| P | Planner row 7: `owl ask Bea "" "q"` sends `path: ""` (hashes like `None`, but listings print a blank, not `-`) — behaviour contract for an empty path token is unspecified | PM-REVIEW | design doc says nothing about `""` |
| F | `tests/pull.rs::outbox_ttl_expires` (reported flaky by the implementer under load avg 15-20): 3/3 PASS in isolation at load avg 7.5 (0.27s, 0.15s, 0.13s); the diff touches no outbox/TTL/expiry code (`flaky-pull.txt`) | PASS |
| E | `src/cli/ask.rs:181` `--wait` `OpenAsk.path` `unwrap_or("-")`: mutant M09 (`""`) survives, but the CLI `wait_for_answer` only passes the ask to `pull::ingest_envelope`, which reads `id`/`peer`/`hash` and never `path`; the value reaches nothing observable (no notify, no log, no output) in the CLI. Equivalent mutant, no row required; the daemon-side twin (`pull::open_ask`, M08) is tested and killed | PASS (equivalent) |
| N | Pre-existing flake, not in this diff: `tests/server.rs::daemon_foreground_writes_addr_and_serves_card` (2 s daemon boot deadline) failed 4/6 isolated runs on a clean tree, passed in the workspace gate run | NOTE | `mutant-M16.txt` |

VERIFY OWL-018: FAIL (PASS 11 / FAIL 2 / PM-REVIEW 1)

### Round 2 — 7008c1c (tests only: tests/inbox.rs +56, tests/server.rs +34)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| G2 | `cargo build` / `cargo fmt --check` / `cargo clippy --all-targets -D warnings` / `cargo test --workspace` on 7008c1c | PASS | `r2-gates.txt` (build/fmt/clippy rc=0), `r2-gate-test-rerun.txt` (workspace rc=0). First workspace run failed one lib test outside the diff, `notify::tests::enabled_with_binary_runs_the_override`; it passed 4/4 in isolation and on the rerun, and the diff touches no `src/` file (`r2-notify-flake.txt`) — parallel-load flake, noted |
| M14 | `src/answer.rs` draft → `runner::draft(.., path.or(Some("-")), ..)` | PASS (RED) | killed by `tests/inbox.rs::draft_and_send_a_question_without_path` (`r2-mutants.txt`) |
| M15 | `src/answer.rs` send → `question_hash(project, path.or(Some("-")), text)` | PASS (RED) | killed by `draft_and_send_a_question_without_path` |
| M16 | `src/server.rs` post_question → `path.as_deref().or(Some("-"))` | PASS (RED) | killed by `bad_inputs_are_4xx_never_500` and `cache_hit_returns_answer` |
| R2 | test-reviewer re-look at the two new tests | OK | exact equality on the cache key (`cache_get(hash(P, None, q)).raw == outbox raw`) and on the 200 body; twins vary only the path (`Some(PATH)`, `Some("-")`, and `Some("")` correctly asserted equal to the None key); `Project:` immediately followed by `Question` pins the missing `File:` line |
| W | worktree clean after every revert | PASS | `git status` empty |

VERIFY OWL-018 (round 2): PASS (PASS 17 / FAIL 0 / PM-REVIEW 1 — the `owl ask Bea "" "q"` empty-path-token row stays PM-REVIEW, unchanged)

PR (single-PR run): https://github.com/Krab00/owlpost/pull/24
