---
id: OWL-017
title: NAT traversal: reach a peer by public key over iroh (hole punching + relay fallback)
status: in_progress
deps: [OWL-006, OWL-007, OWL-008, OWL-016]
source: adhoc
source_updated_at: 2026-09-04
criteria_source: derived
---
## Description
Finding from the first cross-machine test (2026-09-04): the only endpoint owlpost can advertise
today is a host:port that the *asker* must be able to dial (`docs/concept.md` "Addressing — hard
prerequisite"). Two people on different networks — home LAN and a corporate laptop, or Poland and
the US — have no such address unless both run a VPN or Tailscale, and the concept's open question
"which stable-DNS option exists in the company" has no answer yet. Without this, the pilot cannot
start with anyone outside one office network.

Add a second transport to the daemon and to `owl ask` / the pull loop, built on the `iroh` crate
(Rust, open source, QUIC): a peer is dialed **by its ed25519 public key**, iroh finds a direct
path by hole punching and falls back to a public relay when it cannot. Nothing new to install,
no account, no port forwarding. The fit with the existing design is deliberate:

- iroh's endpoint identity is an ed25519 key. The daemon derives its iroh `SecretKey` from the
  same 32-byte seed as `identity::Identity` (`Identity::seed()` → `iroh::SecretKey::from_bytes`),
  so the iroh endpoint id **is** the contact's `pubkey`. A contact needs no `endpoints` field to
  be reachable; the fingerprint in `.agents/peers/` is the address.
- Peer authentication stays key-pinned: an incoming iroh connection exposes the remote's public
  key; the daemon accepts only keys present in the merged contact book (the same check
  `PeerAcceptor` does for mTLS today). Unknown keys are refused at the connection, never stored.
- The HTTP contract (`docs/technical-design.md` §6) is unchanged: the same `POST /v1/questions`,
  `GET /v1/outbox`, ack, signature header and replay rules are served over an iroh bidirectional
  stream (hyper over the QUIC stream, ALPN `owl/1`). One request handler, two listeners.
- Only the *responder* must be reachable; the asker initiates both the question and the pull.
  That holds for both transports.

`owl ask` and the pull loop try transports in order: **iroh first** (always available once both
sides run this version), then the configured `endpoints` (LAN / VPN, still useful and faster
when they resolve). A contact with an empty `endpoints` list is therefore valid from now on;
`owl doctor` reports the iroh endpoint state (bound, relay connected, id) instead of warning
about missing endpoints.

Design decisions to record in `docs/concept.md` (Addressing) and `docs/architecture.md` (§1, §4
trust table): "no central server" becomes "no server that sees content": the relay carries only
end-to-end encrypted QUIC and learns which two keys talked and when. The default relay set is
n0's public relays (`presets::N0`); `config.json` gains `relay_urls: []` for a self-hosted
relay (iroh's relay server is open source) so a company can keep even the metadata in-house.
The `endpoints`-only path stays for users who refuse any relay; document the trade-off, do not
hide it. `deps` include OWL-016 because the key-pinning check moves to a second code path and
must reuse the audited SPKI-free comparison (iroh hands us the key directly; there is no
certificate to parse).

Out of scope: replacing mTLS on the host:port path, an owlpost-owned relay protocol, QR/invite
bundles (M4). Exact iroh API names (`Endpoint::builder`, `presets::N0`, `alpns`, `connect`,
`remote_id`) are to be verified against the pinned crate version's docs by the implementer;
justify the new crate per `docs/technical-design.md` §1.

## Acceptance criteria
- [ ] AC1: `owl whoami` and `owl contact export` are unchanged in shape; the daemon's iroh endpoint id, printed by `owl doctor`, equals the identity's `pubkey` byte-for-byte — unit test `daemon::tests::iroh_endpoint_id_is_identity_pubkey` (modality: test)
- [ ] AC2: integration test `tests/iroh.rs::question_and_answer_over_iroh` — two daemons in temp homes, contact books pointing at each other **with empty `endpoints`**, both bound to a local iroh relay started in the test (no internet); `owl ask` returns `accepted <id>`, `owl allow` + `owl draft --harness fake` + `owl send` on the responder, and the asker's pull loop stores the answer in `inbox/`, acks it, and the responder moves it to `done/` (modality: test)
- [ ] AC3: integration test `tests/iroh.rs::unknown_key_is_refused` — a third identity not in the responder's contact book connects over iroh; the connection is rejected before any request is read, all five spool dirs stay empty, `seen-ids.txt` is untouched, and the contact book is not modified (no TOFU on the iroh path) (modality: test)
- [ ] AC4: integration test `tests/iroh.rs::signature_and_replay_rules_apply_over_iroh` — over iroh, a body whose `X-Owl-Signature` does not verify gets the same status as over HTTPS, a replayed `id` gets the same status, and the per-peer rate limit triggers `429` at the same threshold; the three responses are asserted equal to the HTTPS suite's expectations from `tests/server.rs` (modality: test)
- [ ] AC5: transport order — integration test `tests/iroh.rs::iroh_first_then_endpoints`: with a contact whose `endpoints` holds one unreachable address and iroh available, `owl ask` succeeds without ever dialing the address (asserted by a listener on that address that records connection attempts); with iroh unavailable (relay map empty, both daemons on isolated sockets) and a reachable endpoint, `owl ask` succeeds over HTTPS; with both unavailable, `owl ask` reports offline in the same wording as today (modality: test)
- [ ] AC6: `config.json` accepts `relay_urls` (list of URLs); `owl doctor` prints `ok iroh: <id short>, relay <url>` when connected, `warn iroh: bound, no relay` when not, and no longer warns about empty `endpoints` — unit test `cli::doctor::tests::iroh_line` covers both lines (modality: test)
- [ ] AC7: `docs/concept.md` "Addressing" and "Open questions", `docs/architecture.md` §1 diagram, §4 trust table and §5 delivery table, and `docs/technical-design.md` (config field, doctor line, crate justification) describe the iroh transport, the relay's metadata visibility and the self-hosted relay option; `tests/docs.rs` still passes (modality: data)
- [ ] AC8: `scripts/e2e-real.sh` gains `OWL_TRANSPORT=iroh|https` (default `https`, unchanged behaviour) and the iroh run completes the same loop against the real harness on one machine using n0's public relays; the run is recorded once by hand in the task file with the relay URL doctor reported (modality: manual, PM-REVIEW)
- [ ] AC9: `cargo build`, `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` all pass; binary size growth is reported in the PR (iroh pulls QUIC + relay client) (modality: data)
## Design notes
- **The daemon owns the single iroh endpoint.** An iroh endpoint identity is a network
  singleton: the relay bumps a second registration of the same key. `owl ask` is a separate
  synchronous process, so it never binds an endpoint with the identity key. The endpoint is
  created in `daemon::spawn` (`iroh::SecretKey::from_bytes(Identity::seed())`, ALPN `owl/1`),
  its accept loop runs beside the mTLS listener, and both stop in `shutdown`/`wait`.
- **The CLI reaches iroh peers through the local daemon.** `ANY /v1/local/{fingerprint}/{*rest}`
  on the mTLS listener, accepted only when the client certificate is the owner's own key
  (`daemon::allowed_keys` now includes it; the CLI connects to `daemon.addr` pinned to its own
  key like `doctor` does). `rest` is limited to `v1/questions`, `v1/outbox`,
  `v1/outbox/{id}/ack`. The daemon replays method, `X-Owl-*`/`Content-Type` headers and body
  over a fresh iroh bi-stream and returns the peer's status, headers and body verbatim. A local
  failure (unknown contact, no endpoint, dial timeout 10 s, closed by peer) is
  `502 {"error":"iroh: <reason>"}`; the client treats that 502 as "iroh unavailable, try
  `endpoints`". Any other status is the peer's own answer. The route is not mounted on the
  iroh listener (`server::iroh_router`), so it is `404` there for every key.
- **Transport order** everywhere (`send_question`, `fetch_outbox`, `ack`, pull loop): iroh
  first, then `endpoints`. `client::Iroh` says how iroh is reached: `Direct` (daemon's own
  endpoint), `ViaDaemon(addr)` (CLI, from `daemon.addr`), `Unavailable(reason)` (no
  `daemon.addr`). A contact with empty `endpoints` is valid; the `no endpoints` usage error is
  gone. Both transports failing keeps the `offline: no endpoint of <name> reachable (…)`
  wording with one `transport: reason` per attempt, `iroh: …` first.
- **`relay_urls` has three states** (deviation from the "`Vec<String>`, `[]` = n0" plan):
  absent/`null` = n0's public relays + DNS discovery (`presets::N0`); a list = exactly those
  relays, no public discovery; `[]` = no relay at all. The third state is what lets the test
  fixture bind an endpoint without ever touching n0's infrastructure and what AC5 needs for
  "iroh unavailable"; with `[]` meaning n0 there would be no way to express it. `owl init`
  writes nothing for the field (absent = default).
- **Key check on the iroh path** is a 32-byte comparison of `Connection::remote_id()` against
  every contact's parsed pubkey (`iroh::fingerprint_of_key`); no certificate exists to parse.
  Unknown keys get `close(1, "unknown key")` before `accept_bi`; the contact book is reloaded
  per connection so `owl allow`/`owl deny` apply without a restart on this path.

## Verification — task/OWL-017-iroh (round 1)
Verify worktree: detached checkout of 4fef529 (7 commits on main a04bce1). Evidence: `.orch/tasks/OWL-017.evidence/r1-*`.
Gates: `cargo fmt --check` 0, `cargo clippy --all-targets -- -D warnings` 0, `cargo test --test iroh` 3/3 runs green (5 tests, ~5.1 s each), `cargo test --workspace` 0 (lib 124, bin 43, all integration binaries green). Release binary 19,563,384 B (built here) vs main 9,321,928 B (main worktree's `target/release/owl`, provenance unverified) — implementer's 9.3 → 19.6 MB confirmed.

| AC | Verdict | Evidence |
|---|---|---|
| AC1 | PASS | `daemon::tests::iroh_endpoint_id_is_identity_pubkey` compares `id().as_bytes()` to the identity key + card `iroh.id == owlpost.pubkey` + `assert_ne` vs another seed; `git diff main...HEAD` touches no whoami/contact-export code or tests (tests/cli.rs 25 green, unchanged) |
| AC2 | PASS | `question_and_answer_over_iroh`: both books empty `endpoints` (asserted), local `iroh-relay` on 127.0.0.1:0, ask → consent → allow --once → draft fake → send → A inbox → A done answered → B done; mutants M4/M7/M11 (iroh reply discarded, owner key off mTLS allow-list, pull loop without iroh) all fail this test |
| AC3 | PASS | `unknown_key_is_refused`: `ApplicationClosed(1,"unknown key")` observed with no stream opened; signed POST gets no HTTP status; `Dir::ALL` empty; `seen-ids.txt` absent; contact dir bytes equal; known key served 200 `[]`; forward route 404 over iroh. M1 (accept unknown key) and M9 (accept_bi before key check) killed |
| AC4 | PASS | `signature_and_replay_rules_apply_over_iroh`: (400,"bad signature"), (409,"duplicate id"), (429,"rate limited") + `Retry-After: 1200` at 3/h, replay of a rate-limited id stays 429 — same literals as tests/server.rs |
| AC5 | PASS | `iroh_first_then_endpoints`: decoy counter sanity-connected (1) then 0 after the ask; case 2 `relay_urls=[]` both sides → HTTPS accepted; case 3 exit 2, `owl: offline: no endpoint of Bea reachable (iroh: …; <port>: …)`; tests/ask.rs pins the no-daemon wording by full equality. M12 (502 never falls through) killed. Tidy-up: case 3 `iroh:` reason not pinned (verifier probe shows `iroh: dial: …` class) |
| AC6 | PASS | `config::tests::relay_urls_three_states` (absent/null/[]/list/non-list/non-string + save roundtrip); `cli::doctor::tests::iroh_line` pins `ok   iroh: <hex10>, relay <url>`, `warn iroh: bound, no relay`, `warn iroh: not bound`, `warn iroh: unknown (daemon unreachable)` by equality; tests/doctor.rs pins the `ok` line end to end and `ok   endpoints: none configured (peers reach this daemon over iroh)`. Verifier probe P3: fixture daemon with `relay_urls=[]` + daemon.addr → `warn iroh: bound, no relay` end to end. Literal differs from the AC only by doctor's column padding (`ok   `) which predates this task. M10 killed |
| AC7 | PASS | docs diff: concept "Addressing — the key is the address" + trade-off paragraph (relay metadata, `relay_urls` self-hosted, endpoints-only) + Open questions rewritten; architecture §1 diagram (iroh/relay box), §4 trust rows (iroh auth, relay visibility), §5 delivery rows (NAT, relay unreachable), D13; technical-design crate justification, `relay_urls` semantics, forward route row, doctor lines, test-strategy row, e2e-real `OWL_TRANSPORT`. tests/docs.rs 4/4 |
| AC8 | PM-REVIEW | Script: `OWL_TRANSPORT` guard before PATH lookup, default `https` leaves the write_config path untouched, iroh branch waits for `^ok   iroh:` on both daemons; `tests/e2e.rs::e2e_real_script_fails_fast` covers `tcp` → exit 2 naming the value and both valid values passing the guard. No manual run record with a relay URL in this file yet |
| AC9 | PASS | four gates 0 (r1-gates.log); sizes above |
| S1 tests: suite green + covered | PASS | workspace green; 3× iroh green; mutant sweep 12/14 killed by the shipped suite (r1-mutants.log) |
| S2 tests: mandatory matrix | FAIL | Mutant M5 (`client::via_iroh` ViaDaemon: `if status == 502` → `>= 400`, i.e. a peer's own 429/403 through the forward route treated as "try endpoints") SURVIVES the shipped suite; killed only by the verifier's probe `p2_peer_status_via_forward_is_final`. Same hole covers response-header replay (`Retry-After`, `X-Owl-Signature`) through the forward route and `owl ask --wait` over iroh (ViaDaemon `fetch_outbox` + `ack`), both never exercised on the branch (verifier probes P1/P2 pass on the code, so this is a missing test, not a bug). M13 (`forward` contact lookup `==` → `starts_with`) also survives — low risk, tidy-up |

Verifier probe (not on the branch, r1-verify_probe.rs, 4/4 pass): P1 `--wait 20` over iroh with a decoy in `endpoints`, asker pull parked → answer printed, B done acked, decoy 0; P2 policy limit 1 → second ask exit 3 `owl: rate limited, retry after Ns`, decoy 0; responder disabled → exit 2 `owl: unavailable: Bea does not answer questions (policy never or responder disabled)`, decoy 0; P3 doctor bound/no relay; P4 asker unknown to responder → exit 2 `… (iroh: connection closed by peer: closed by peer: unknown key (code 1))`, B spool empty.

Deviations (verifier's view): 1 `relay_urls` three-state `Option<Vec>` — accepted, necessary (the fixture must never touch n0; M14 proves `presets::N0` would fail the fixture guard); docs and config tests describe all three states. 2 `outbox_ttl_expires` now+60 s — accepted, boundary still pinned by pre-existing `pull::tests::expire_outbox_boundary`. 3 AC2 responder fixture without a policy so `owl allow --once` releases — accepted, matches the AC's own `owl allow` step. 4 forward route only on the mTLS router — accepted and pinned (unit + AC3 404 over iroh, M8 killed).

FAILING:
- S2: add to `tests/iroh.rs` (a) `peer_status_via_forward_is_final`: responder with `Peer::new(&a_id,"Ana",Some(policy(Mode::Manual,Some(1))))`, asker `endpoints=[decoy]`, ask twice → second exits 3 with stderr starting `owl: rate limited, retry after ` and `decoy.attempts()==0`; then `responder.enabled=false` → exit 2, stderr equals `owl: unavailable: Bea does not answer questions (policy never or responder disabled)`, decoy 0. (b) `wait_over_iroh_via_daemon`: asker `pull_interval_secs=3600`, `endpoints=[decoy]`, `owl ask … --wait 20` while a thread does allow/draft/send on B → exit 0, stdout has the answer, A `done/<qid>`=`answered`, B `done/<aid>`=`acked`, decoy 0. The verifier's r1-verify_probe.rs already implements both and passes; port it.
- Tidy-ups in the same round (not blocking): pin AC5 case 3's `iroh:` reason (`contains("iroh: dial")` or full equality); assert `state(&b_spool, Dir::Done, &aid)==Some("acked")` in AC2 and drop the `Outbox unacked` assert that races A's 1 s pull; optionally add a prefix-decoy contact to `forward_route_refuses_…` (M13).
