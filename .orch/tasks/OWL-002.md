---
id: OWL-002
title: Identity — keypair, fingerprint, sign/verify, `owl init`, `owl whoami`
status: done
deps: [OWL-001]
source: manifest
source_updated_at: 2026-09-01
criteria_source: structured
---
## Description
Implement `identity.rs` per `docs/technical-design.md §4`: generate an ed25519 keypair, store the
32-byte seed at `$OWLPOST_HOME/key` with mode 0600, load it, derive the `ed25519:<base64>` public
key encoding and the `owl:<16 base32 chars>` fingerprint, and expose `sign(&[u8]) -> Signature`
and `verify(pubkey, &[u8], sig) -> bool`. `owl init` creates the home, key and config
(`--name`, `--email` repeatable) and prints the fingerprint; it refuses to overwrite an existing
key. `owl whoami` prints fingerprint, pubkey, name, endpoints (text and `--json`).

## Acceptance criteria
- [ ] AC1: `owl --home <tmp> init --name Test --email t@example.org` exits 0, creates `key` with mode 0600 (unix) and `config.json` with the name/email, and prints a line matching `^owl:[a-z2-7]{16}$` (modality: test)
- [ ] AC2: running `owl init` again on the same home exits 1 with a message containing `already exists` and leaves `key` byte-identical (modality: test)
- [ ] AC3: unit test `identity::tests::fingerprint_is_stable` asserts the fingerprint of a fixed seed equals a hard-coded expected value and has length 20 (`owl:` + 16) (modality: test)
- [ ] AC4: unit test `identity::tests::sign_verify_roundtrip` verifies a signature over arbitrary bytes and rejects it after flipping one byte of the message and after flipping one byte of the signature (modality: test)
- [ ] AC5: unit test `identity::tests::pubkey_encoding_roundtrip` parses `ed25519:<base64>` back to the same key and rejects a wrong prefix (modality: test)
- [ ] AC6: `owl --home <tmp> whoami --json` prints JSON with keys `fingerprint`, `pubkey`, `name`, `endpoints` (modality: test)

## Verification — task/OWL-002-identity (94b9c7e, base task/OWL-001-cli-skeleton) — 2026-09-02
Evidence: `.orch/tasks/OWL-002.evidence/` (build/test/clippy/fmt logs, behaviour.log, ac3-recompute.log, mutants.log + mutants/mNN.{diff,log}, hygiene.log).

| Criterion | Tool | Evidence | Verdict |
|---|---|---|---|
| AC1 init creates key 0600 + config + prints `^owl:[a-z2-7]{16}$` | cli binary | behaviour.log §AC1: exit 0, 1 stdout line `owl:dpemxv4wcn5xufzm` (regex-lines=1), key mode=600 size=32, config name=Test emails=[t@example.org]; test `init_creates_key_and_config_and_prints_fingerprint` | PASS |
| AC2 second init exits 1, `already exists`, key byte-identical | cli binary | behaviour.log §AC2: exit 1, stderr `key already exists at …/key — refusing to overwrite`, stdout empty, cmp key + config identical; test `init_twice_refuses_and_keeps_key` | PASS |
| AC3 `fingerprint_is_stable` literal + len 20 | cargo test + pure-python RFC8032 recompute | test green; ac3-recompute.log: seed [7;32] → `owl:72asyextvngonlc5` len 20 MATCH (test-reviewer recomputed independently too) | PASS |
| AC4 `sign_verify_roundtrip` flips msg byte + sig byte | cargo test + mutants M15/M16 | test green; M15 (ignore msg) and M16 (always true) KILLED by this test | PASS |
| AC5 `pubkey_encoding_roundtrip` parses + rejects wrong prefix | cargo test + mutants M07–M13 | test green; padded / URL-safe / prefix / length-only / padded-decode / length-dropped mutants all KILLED | PASS |
| AC6 `whoami --json` keys fingerprint/pubkey/name/endpoints | cli binary | behaviour.log §AC6: exit 0, keys == [endpoints,fingerprint,name,pubkey], fingerprint == init output; global `--json` before subcommand identical; test `whoami_json_after_init` | PASS |
| Gate: `cargo build` | cargo | build.log Finished | PASS |
| Gate: `cargo test --workspace` | cargo | test.log 14 unit + 14 cli, 0 failed | PASS |
| Gate: `cargo clippy --all-targets -- -D warnings` | cargo | clippy.log Finished, no warnings | PASS |
| Gate: `cargo fmt --check` | cargo | fmt.log empty | PASS |
| Test-reviewer audit | orch:test-reviewer | `TEST-REVIEW OWL-002: OK`; modified OWL-001 test asserts new behaviour (exit 1, `32 bytes`, no init hint) — strictly stronger, nothing loosened; 6 non-blocking hardening notes (alphabet not pinned by literal, verify-with-other-key, invalid-point branch, non-empty endpoints, pre-existing config merge, save non-EEXIST error) | PASS |
| Matrix 2 init no flags → defaults | cli | behaviour.log §row2: exit 0, name "", emails [], listen 0.0.0.0:7411 | PASS |
| Matrix 3 repeated `--email`, no name | cargo test + M27/M28 | `init_accepts_repeated_email_and_no_name`; keep-last / keep-first mutants KILLED | PASS |
| Matrix 4 `--name` only | cli | §row4: name=Only, emails=[] | PASS |
| Matrix 6 key exists + malformed config → `already exists`, config untouched (guard precedes Config::load) | cli | §row6: exit 1 already exists; `{not json` intact | PASS |
| Matrix 7 pre-existing config, no key → merged, key created | cli | §row7: listen/endpoints/emails kept, name=N, key 0600 | PASS |
| Matrix 8 malformed config, no key → exit 1, no key created | cli | §row8: parse error naming config.json; dir has only config.json | PASS |
| Matrix 9 `--home` is a regular file | cli | §row9: exit 1 `Not a directory`, no panic | PASS |
| Matrix 10 unwritable parent | cli | §row10: exit 1 `Permission denied`, no partial files | PASS |
| Matrix 11 dangling symlink `key` bypasses `exists()`; `create_new` still refuses | cli | §row11: exit 1 `key already exists`, symlink untouched — BUT config.json was written first (home not untouched in this corner) | PM-REVIEW |
| Matrix 12/13 save nested + refuse overwrite; mode 0600 | cargo test + M20–M24 | mode 0644 / mode removed / create+truncate / wrong bytes / message mutants all KILLED | PASS |
| Matrix 14/16/17 load 31 bytes / 0 bytes / missing | cargo test + cli + M18 | `load_rejects_wrong_length`; §row16 `got 0` exit 1; M18 (pad 31) KILLED | PASS |
| Matrix 15 load 33 bytes | cli + mutant M19 | §row15 real binary rejects (`got 33`) — but mutant "truncate to 32 when len ≥ 32" SURVIVED: no test with an over-long key | FAIL |
| Matrix 19 generate randomness | cargo test + cli | `generate_is_random`; two homes print different fingerprints | PASS |
| Matrix 20/21 fingerprint algorithm (§4) recomputed from live pubkey | python | §row21: strip prefix → b64 (standard, no pad) → sha256 → base32 lower [..16] == printed fingerprint MATCH; M01–M06 KILLED (M04 padded-base32 is an equivalent mutant: padding lands after char 52, cannot affect [..16]) | PASS |
| Matrix 23 verify with a different pubkey | mutant M17 | mutant "verify ignores pubkey, uses fixed key" SURVIVED: no test verifies with a key other than the signer's | FAIL |
| Matrix 25–28 parse_pubkey/parse_sig prefix, base64, padding, length | cargo test + M09–M14 | all KILLED | PASS |
| Matrix 25b parse_* rejects a bare (unprefixed) base64 string | mutant M10 | `strip_prefix(PREFIX).unwrap_or(s)` SURVIVED: existing wrong-prefix cases (`foo:`, `ed25518:`) are killed by the `:` failing base64, not by the prefix guard; no test passes bare base64 | FAIL |
| Matrix 29 invalid curve point → `invalid ed25519 public key` | code | branch exists, untested (test-reviewer note 3); no mutant authored | PM-REVIEW |
| Matrix 30 sig roundtrip | cargo test + M14 | `sig_encoding_roundtrip`; length-dropped mutant KILLED | PASS |
| Matrix 32 whoami text lines | cli + M37 | §row32: 4 `key: value` lines; name-line-dropped mutant KILLED | PASS |
| Matrix 33 whoami with non-empty endpoints (text + json) | cli + mutant M38 | §row33 real binary: `endpoints: a.example:7411, b.example:7411` and JSON array of 2 — but mutant `join(" ")` SURVIVED: no test with non-empty endpoints (text joiner is unspecified by §4; pin whichever format is chosen) | FAIL |
| Matrix 34 whoami guards (empty / config-only / key-only / env) | cargo test + M39 | 4 OWL-001 tests green; `\|\|→&&` mutant KILLED by both halves | PASS |
| Matrix 35 garbage 1-byte key → `32 bytes`, no init hint | cargo test | `whoami_with_config_and_garbage_key_fails_without_init_hint` | PASS |
| Matrix 36 malformed config + valid key | cli | §row36: exit 1 parse error, no init hint | PASS |
| Matrix 37 global `--json` before subcommand | cli + M40 | §AC6b identical JSON; `--json ignored` mutant KILLED | PASS |
| Matrix 38 `init --json` | cli | §row38: prints plain fingerprint line, flag ignored; shape unspecified by §9 | PM-REVIEW |
| Matrix 41 concurrent `owl init` ×6 | cli | §row41: succeeded=1 refused=5, single key file (`create_new` is the real guard) | PASS |
| Design §4 conformance (pubkey `ed25519:`+std b64 nopad; fp `owl:`+16 lc base32 of sha256(raw pk); key 32 raw bytes 0600; refuse overwrite) | code + cli + python | identity.rs uses BASE64_NOPAD/BASE32_NOPAD + Sha256 on `as_bytes()`; live recompute matches; key size 32 mode 600 | PASS |
| Design §1 dependencies | hygiene.log | data-encoding, ed25519-dalek, sha2 are in §1; `rand_core 0.6` is extra — it is ed25519-dalek's own RNG trait crate (cargo tree), used only for `OsRng`; Cargo.toml carries the justification comment | PM-REVIEW |
| Hygiene: fixtures tracked, no target/ in diff, Cargo.lock committed + in sync | git + cargo | all tests use tempdir; diff paths = 6 source files; `cargo metadata --locked` ok; branch clean | PASS |
| Whoami JSON key set / values | mutants M32–M36 | drop endpoints / drop pubkey / rename fingerprint / empty name / fp=pubkey all KILLED | PASS |
| Init ordering + output | mutants M25/M26/M29–M31 | overwrite-check removed / check-after-config / name ignored / prints pubkey / labelled output all KILLED | PASS |

**Mutant sweep:** 40 authored, 35 killed, 1 equivalent (M04), 4 survivors (M10, M17, M19, M38) → FAIL rows above. All four are test gaps, not behaviour bugs: the real binary/unit code was confirmed correct for each case by hand (rows 15, 21, 33, and `strip_prefix` on bare input).

VERIFY OWL-002: FAIL (PASS 37 / FAIL 4 / PM-REVIEW 4)
FAILING: M19 load accepts 33-byte key (add a 33-byte case to `load_rejects_wrong_length`, assert `got 33`); M17 verify with a different pubkey (assert `!verify(&other_pk, msg, &sig)` in `sign_verify_roundtrip`); M10 parse_pubkey/parse_sig accept bare unprefixed base64 (assert `parse_pubkey(&s[8..]).is_err()` and same for sig); M38 whoami with non-empty endpoints untested (cli test seeding `endpoints:["a:1","b:2"]`, assert exact text line `endpoints: a:1, b:2` and JSON array).

### Implementer report (OWL-002)
**Summary** — identity.rs (generate/from_seed/load/save create_new+0600, sign, verify_strict, pubkey/sig string+parse, fingerprint; 8 unit tests), main.rs init (key check before config, --name/--email repeat, prints fingerprint) + whoami text/--json, config.rs dropped dead_code allow, Cargo.toml ed25519-dalek 2/sha2/data-encoding/rand_core; tests/cli.rs 5 new tests + 1 updated.
**Assumptions** — init without flags writes defaults; text whoami is `key: value` lines; unused sign/parse fns carry ponytail dead_code markers.
**Open Questions** — none.
**Blockers** — none.
**For human review** — rand_core dep; init error wording.

## Verification — task/OWL-002-identity (round 2) — 86c30a8, 2026-09-02
Recheck of round-1 FAIL rows only. Evidence: `OWL-002.evidence/round2.diff`, `round2-gates.log`, `mutants-r2.log`, `mutants-r2/*.{diff,log}`.

| Criterion | Tool | Evidence | Verdict |
|---|---|---|---|
| M19 `Identity::load` rejects 33-byte key | mutant (truncate ≥32) | KILLED by `identity::tests::load_rejects_wrong_length` (asserts `got 33`) | PASS |
| M17 `verify` with a different pubkey rejects | mutant (verify against fixed key) | KILLED by `identity::tests::sign_verify_roundtrip` (seed [8;32] key) | PASS |
| M10 `parse_pubkey`/`parse_sig` reject bare unprefixed base64 | mutant (`unwrap_or(s)`) | KILLED by `pubkey_encoding_roundtrip` + `sig_encoding_roundtrip` (`&s[8..]`) | PASS |
| M38 whoami non-empty endpoints text + json | mutant (`join(" ")`) | KILLED by `whoami_prints_configured_endpoints` (exact line `endpoints: a:1, b:2`, JSON array) | PASS |
| Test-reviewer audit of `94b9c7e...86c30a8` | orch:test-reviewer | `TEST-REVIEW OWL-002 round 2: OK`; test-only diff, exact assertions, tempdir fixtures | PASS |
| Gates on 86c30a8: build / test (14 unit + 15 cli) / clippy -D warnings / fmt | cargo | round2-gates.log, all rc=0 | PASS |

Round-1 PM-REVIEW rows (dangling-symlink key writes config first; invalid-curve-point branch untested; `init --json` ignores flag; `rand_core` extra dep) are unchanged and non-blocking.

VERIFY OWL-002: PASS (PASS 43 / FAIL 0 / PM-REVIEW 4)

PR: https://github.com/Krab00/owlpost/pull/3 (draft, base task/OWL-001-cli-skeleton)
